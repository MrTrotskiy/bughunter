# tests

Test-authoring doctrine for bughunter. Auto-loads when working under tests/. The point of every rule below: a test that cannot fail is worse than no test — it reports safety that is not there.

<invariants>
- IMPORTANT: FAIL-ON-REVERT is the anti-vacuous gate. Every behavior test carries `// Guards: <capability it protects>` and `// FAIL-ON-REVERT: <how to break it>`, and is PROVEN red by reverting the guarded code before it counts. Undo the mechanism → the test must go red with a useful message → restore. A test never revert-proven does not count as coverage.
- A test guards a NAMED class of regression. No guarded capability → delete the test. Keep the live-guard index at the bottom of this file current in the same change.
- Behavior or delete. No `assert(true)`, no "ran without throwing", no snapshot-only, no asserting an element merely exists. Assert the OUTCOME (attributed request, instance count, exit code, envelope field).
- Layer rule: pure function (envelope, host-policy, id ledger, url-pattern mask, classifyInitiator, settle predicates) → unit (tests/unit/, node:test, NO browser). Anything needing a real page (dom-snapshot, causal attribution, whats-new end to end) → live (tests/live/, real chromium + local fixture). Crossing the browser boundary → ONE live test, not many.
- Live tests drive a REAL chromium against a REAL local fixture. Never stub the page, CDP, fetch, or the graph. The fixture MUST exercise the real traffic classes (load-burst, background poll, click-caused request) — a happy-path-only fixture makes causal attribution vacuous.
- Prove the HARD case. The causal test MUST include the adversarial in-window background poll; asserting only the click request passes even with the token alone (the bug that killed bughunt-agents). Assert both that the raw ring WOULD miscredit it and that attribution drops it.
- Wire before DOM. Assert the attributed request (the control→request contract, bughunter's whole value) before or alongside the revealed elements.
- No sleeps as waits. Use the settle predicate (waitSettled / deriveNetworkSettled), never a timer to "let it finish". A setTimeout is allowed ONLY to force an adversarial condition (hold the window open so a poll must tick) — and then assert the condition actually occurred, else the guard is vacuous.
- Isolate. Tests write to a temp BUGHUNTER_STATE_DIR, never repo state/. Ephemeral ports (listen 0). PW_ALLOW_PRIVATE=1 for loopback fixtures. Tear down server + tmp dir in t.after.
- CDP-only mechanisms (the initiator classifier) are asserted honestly: a test must not pass on the token alone. Where a mechanism is chromium-specific, say so in the test.
</invariants>

## Layers
| Layer | Dir | Deps | Guards |
|---|---|---|---|
| unit | tests/unit/ | node:test only | pure fns: SSRF classification, id ledger, url-pattern mask, classifyInitiator over synthetic CDP stacks, settle predicates |
| live | tests/live/ | real chromium + tests/fixtures/ | dom-snapshot identity, causal attribution, whats-new end to end |

Fixtures live in tests/fixtures/ (shared). A fixture is source: it must model the real traffic classes it exists to test, documented in its header.

## Process
1. Write the failing test FIRST, with `// Guards:` + `// FAIL-ON-REVERT:` headers. Confirm it is RED against the unfixed code.
2. Implement to GREEN.
3. Revert-verify: undo the guarded code, rerun, confirm RED with the recorded message, restore. Record the sentinel fragment in the header.
4. Run `npm test` once at the end; report numeric pass counts.
5. Review against these invariants; every finding cites the rule + a 1-3 line fix shape. Code authoring is Opus; this doctrine and review are Fable.

## Live-guard index
Each live capability guard → test path (revert-verified).
- causal attribution drops in-window background polls — tests/live/whats-new.test.mjs
- two-level identity: one template, N addressable instances — tests/live/whats-new.test.mjs
- recon loop composes frontier + causal capture on a live browser: attributes the caused edge, drains the frontier — tests/live/recon-loop.test.mjs
- recon tool chain (baseline → emit → act → observe) persists causal edge + semantics and drains the observed template — tests/live/recon-sequence.test.mjs
- the fire path refuses to click a destructive control and fires no request (DANGER_FLOOR before the click) — tests/live/danger-gate.test.mjs
- one browser for the whole crawl, not one per act (launch-count guard) — tests/live/recon-loop.test.mjs
- attach() connects to the shared daemon (mode 'attached'), one process serves N acts — tests/live/daemon.test.mjs
- the CDP initiator classifier still rejects an in-window poll over a chromium.connect() session — tests/live/daemon.test.mjs
- reused page (persistentStep) does not mis-credit an earlier act's foreground path to a later act's same-path background poll — tests/live/cross-act.test.mjs
- the fire path fast-fails NOT_VISIBLE on a DOM-present-but-hidden control instead of hanging the 30s click timeout — tests/live/not-visible.test.mjs
- recon crawls multiple same-origin routes, attributes controls to the LANDED route, and never fires an off-origin link — tests/live/multi-route.test.mjs
- the off-origin skip is http(s)-scheme-gated: a javascript: anchor is fired, not dropped as external — tests/live/scheme-gate.test.mjs
- a cookie/consent overlay is dismissed so the underlying control becomes reachable; a non-consent accept-text control is left alone — tests/live/overlay.test.mjs
- the snapshot classifies each control's durable locator (testid/id/role-name/css) and gates test-id uniqueness (page-unique discriminator vs shared marker) — tests/live/locator.test.mjs
- debug capture rides an act (before/after key-frames + rect + per-phase timings) WITHOUT perturbing causal attribution (real edge credited, in-window poll rejected) — tests/live/capture-causal.test.mjs
- a SIGTERM/SIGINT-killed crawl stamps a PARTIAL conclusion on run.json (status != "running" + a `stopped` field) instead of leaving a phantom-running run with no conclusion — tests/live/signal-close.test.mjs
- request/response BODY capture (opt-in) writes REDACTED bodies to the trail (files), keeps requests[] body-free, and re-proves the in-window poll is still rejected with capture ON; default OFF captures nothing; text/html is off-allowlist — tests/live/response-body.test.mjs
- endCause freezes the kept-set (selectKept) BEFORE any body await, so a mid-await verdict flip adds no phantom edge (white-box tracker-seam) — tests/live/response-body.test.mjs
- (unit) selectKept applies token + initiator + static filters synchronously (the kept-set decision is await-free) — tests/unit/select-kept.test.mjs
- (unit) redact.mjs redacts secret KEYS and secret VALUES (JWT/Bearer/AWS/card/SSN/email under an innocent key — the H1 bypass), walks form structurally, and is LINEAR on a pathological body (no ReDoS) — tests/unit/redact.test.mjs
- (unit) the response ledger's double-gate defaults OFF (no body without the flag+run — the login state), gates the request body on the content-type allowlist (multipart skipped), and redacts request bodies at store time — tests/unit/response-ledger.test.mjs
- (unit) bodyCaptureEnabled is the double gate: half-open (flag set, no run) → false (the login-safe default) — tests/unit/initiator.test.mjs
- (unit) the debug admin serves the trail behind a loopback Host-guard + no-CORS + resolved-path containment; a foreign Host is 403'd — tests/unit/admin-server.test.mjs
- authed recon: a login storageState makes the crawl map the logged-in surface (authed-only control present) — tests/live/auth.test.mjs (T1)
- login VERIFIES success before persisting: wrong creds never write a storageState — tests/live/auth.test.mjs (T2)
- the crawl refuses to NAVIGATE to a danger route (/logout), so an authed run never logs itself out — tests/live/auth.test.mjs (T3)
- login output carries counts only — the credentials never appear in its result envelope — tests/live/auth.test.mjs (T5)
- (unit) contextOptions injects storageState at newContext, fails loud on a missing state file, anonymous forces a clean context, and every context carries a FIXED desktop viewport (reproducible NOT_VISIBLE denominator); AND readSessionEndpoint trusts a bracketed IPv6 loopback endpoint (ws://[::1]:PORT, the macOS launchServer form) but refuses a non-loopback LAN endpoint — tests/unit/session-context.test.mjs
- (unit) isLoopbackHost is the strictly-narrower daemon-endpoint trust boundary: loopback-only (127.0.0.0/8, ::1, [::1], localhost/*.localhost) accepted, every private-but-not-loopback form (RFC1918/CGNAT/link-local/metadata) + look-alike "127.evil.com" + malformed rejected — tests/unit/host-policy.test.mjs
- (unit) routeRefused classifies /logout, /account/delete, /checkout as refused; ordinary routes pass — tests/unit/danger-floor.test.mjs
- GAP 2 stay-on-page: a control behind a depth-1 modal is reached via the replay prologue, causally attributed at depth (in-window poll still dropped, raw-ring proven), the GET-only gate leaves a POST-opener's children unreachable, and a replay hop that is stale / too-deep / CYCLIC / an off-origin link / a /logout danger route is REFUSED before the click — tests/live/stay-on-page.test.mjs
- agent-path stay-on-page: whats-new (the /recon path) replays a reveal path so a modal/panel control is reached + attributed at depth, and a POST-that-READS opener's children are stamped replayable ONLY under the agent's --opener-replayable judgment (omit it → GET-only default leaves them unstamped) — tests/live/agent-stay-on-page.test.mjs
- (unit) mergeSnapshot stamps node.reveal on NEW nodes only (first-reveal-path-wins) with ZERO id churn (ledger + diffIdentity), and frontier.nextBatch carries reveal — tests/unit/reveal-annotation.test.mjs
- (unit) identity-diff catches a re-keyed OR dropped template/instance key + dropped edge, and its CLI loader fails loud on a corrupt (present-but-unparseable) state file — tests/unit/identity-diff.test.mjs
- INC.1 framework-id de-fragmentation: framework-generated wrapper ids (`rc-*`, hashed) stop anchoring identity, so 3 antd tabs collapse to ONE template with 3 instances + a role+name durable locator (not 3 per-reload-shifting id-anchored templates); a plain semantic #id still anchors (rejection is scoped) — tests/live/framework-id.test.mjs
- (unit) the schemaVersion gate: loadGraph RESETS a legacy (no-schemaVersion, framework-anchored) graph rather than co-mingling it with re-keyed ids, and preserves a current-scheme graph intact — tests/unit/graph-store.test.mjs
- opener-drain guard (agent path): whats-new REFUSES acting a proven multi-instance opener without --instance (would else act the wrong instance[0] and record against a control never clicked) — tests/live/opener-drain.test.mjs; the drain-point twin (observe refuses template-level draining a proven multi-instance opener) — tests/unit/observe.test.mjs
- (unit) frontierInstanceStats reports the honest instance-level frontier (opener siblings walkable/walked/remaining) + cappedRemainder for opener instances beyond OPENER_INSTANCE_CAP (flagged, never hidden) — tests/unit/frontier.test.mjs
- (unit) DRILL_PER_LIST honesty: a 50-row NON-opener list-row template drills 1 representative (nextBatch unchanged) and counts the other 49 rows in `drillSkipped` (the non-opener analog of cappedRemainder — counted, flagged, never walked); a non-listRow template drills 0 extra; an opener list uses cappedRemainder, NOT drillSkipped (mutually exclusive) — tests/unit/frontier.test.mjs
- (unit) mergeSnapshot sets a write-once node.listRow from an instance's el.inRow (a row-resident template), false/absent inRow leaves it unset, once-true never flips false; AND inRow/listRow add ZERO identity churn (ledger maps identical, diffIdentity ok — never an identity key) — tests/unit/graph-store.test.mjs
- depth-2 panel reach: a control present-but-hidden at baseline (pathless) that a "…more" opener uncovers acquires a reveal path via the fill (hiddenWhenSeen + now-visible), is reopened + re-emitted, and is reached by replay [More] with its GET attributed; the pure-uncover opener is flagged even though it reveals no NEW instances — BOTH DOM orderings: tabs-first (cross-batch REOPEN) AND opener-first (same-batch persistentStep graph re-read; reverting the re-read leaves the opener-first tab unreachable, tabs-first stays green) — tests/live/panel-reach.test.mjs
- (unit) the reveal fill's genuine-coverage guard: a hidden-at-baseline instance genuinely reached (explored, not unreachable, no reveal) is NEVER reset by a later opener's fill; only a NOT_VISIBLE-drained or unacted hidden instance acquires the path + reopens — tests/unit/graph-store.test.mjs
- (unit) report --unreached fail-reason histogram: every not-fully-exercised control lands in the right bucket, the trail's GRANULAR act.failed code OVERRIDES the graph's coarse reason, a danger-floor-skipped control is surfaced as NOT covered, the honesty flags (routeCollapse:'pending-INC.3' + the no-invented-never-discovered note) are emitted, the trail readers (readActFailed filters to act.failed, latestRunId picks the most recent run) parse correctly, and report() renders the block with the route-collapse tag — tests/unit/report-unreached.test.mjs
- Layer-3 replay-time write-firewall: during reveal replay a non-GET outside the opener's OWN recorded-read allowlist is ABORTED by page.route (the mutation POST never reaches the server, trackHits stays 0) and the reveal fails honestly with REVEAL_WRITE_BLOCKED → target unreachable, live account unmutated; a safe GET + an allowlisted read-POST are NOT aborted, and an all-allowed opener's replay COMPLETES and reveals its child (firewall does not break reach); a benign OFF-ORIGIN SAFE-method sub-resource is a SOFT block — ABORTED (offHits stays 0, leak prevented) but does NOT fail the reveal (the child IS revealed), so reveal-replay reach survives an app whose revealed UI pulls in a CDN asset; FAIL-ON-REVERT removes the page.route install → POST /api/track hits → trackHits>0 + no REVEAL_WRITE_BLOCKED; re-harden `find(b=>b.hard)` → `blocked.length>blockedBefore` → the off-origin GET fails the reveal (rejects + no child); drop the `|| offOrigin` abort clause → the cross-origin GET reaches the server (offHits>0) — tests/live/write-firewall.test.mjs
- WebSocket-onmessage attribution (the live-session pivot's proof-or-kill): on a REUSED page with no goto between acts, a fetch fired from a persistent WebSocket's onmessage handler ticking inside a control's causal window is NOT credited to that control — the token alone WOULD miscredit it (asserted, non-vacuous) and the CDP initiator CANNOT (it is a bare type:script frame, indistinguishable from a click's fetch), so the probe's `wsRooted` depth tag drops it; FAIL-ON-REVERT: `if (false && WSP)` in probe.mjs (fires lose wsRooted) OR drop `if (f.wsRooted) continue;` in selectKept (causal.mjs) → the WS fetch is credited to #b → "must NOT be credited" fails — tests/live/ws-cross-act.test.mjs
- EventSource/SSE attribution (the symmetric follow-up to the WS hole): on a REUSED page, a fetch fired from a persistent EventSource handler ticking inside a control's causal window is NOT credited to that control — for BOTH the unnamed 'message' event AND a NAMED ('feed') event; the token alone WOULD miscredit both (asserted, non-vacuous) and the CDP initiator CANNOT (bare type:script frame), so the probe's `wsRooted` depth tag (REUSED, no parallel counter) drops them. The NAMED case proves the all-types wrap: a server event with an `event:` field dispatches to a named listener, not 'message' (WHATWG HTML/MDN), so the ES `addEventListener` wrap must cover every function listener type. FAIL-ON-REVERT (two levers): `if (false && ESP)` in probe.mjs → "the SSE message-driven fetch must NOT be credited to #b" fails; restrict the ES `addEventListener` wrap to `type === 'message'` only → "the SSE NAMED (feed)-driven fetch must NOT be credited to #b" fails (the all-types proof) — tests/live/sse-cross-act.test.mjs
- BFS URL route-frontier (INC.1a whole-site collection): a control on a page beyond the element OPENER_INSTANCE_CAP (a wide nav's 9th tab) is reached because the route-frontier discovers + snapshot-visits its page; a 50-link listing (one toUrlPattern) yields ONE representative visited route + 49 siblings folded (census bound); the /logout danger route is never visited (routeRefused) and the off-origin link is never harvested into the queue (sameOrigin/isOffOriginHttp); and snapshot-only discovery forges ZERO edges (a pure seed-only sub-test asserts edges.length===0 while visiting the whole site, and the background /beacon poll is never causally credited during the full crawl) — FAIL-ON-REVERT: delete seedRoutes+refill in recon-run (only-p9 never discovered); remove the per-pattern census check (50 /item routes visited); drop routeRefused (a /logout route is visited); drop the scope filter (a /partner-zone route is harvested); make visitRoute open a causal window/addTrigger (edges appear); AND (security H1) a same-origin harvested link whose server 302s it to an OFF-ORIGIN host on the SAME path is refused, not snapshotted — the route reads unreachable:'redirect-offorigin' (the redirect genuinely fires, extHits>=1, but its content is never captured), FAIL-ON-REVERT: drop the sameOrigin(origin,landed) re-check in visitRoute → routeKey (path-only) matches → the off-origin sink page is snapshotted → the route reads visited — tests/live/route-frontier.test.mjs
- AGENT-PATH route-frontier (INC.1b): whats-new's baseline snapshot harvests routes into the queue so frontier-cli --emit reports pendingRoutes>0; route-cli --visit-next snapshot-visits a beyond-element-cap page (/p9) so only-p9 is discovered + a reachable frontier candidate (not explored — route-cli discovers, only an act explores); the whole agent-path route drive forges ZERO CAUSAL edges (structural page→page nav edges from the baseline a[href] harvest — nav-links.mjs, provenance:'href', non-causal — are excluded) — FAIL-ON-REVERT: comment out the whats-new baseline harvestRoutes call → pendingRoutes stays 0 → /p9 never visited → only-p9 never discovered; make visitRoute open a causal window/addTrigger → a causal (non-nav) edge appears → causalEdges>0 — tests/live/agent-route-frontier.test.mjs
- MENU-EVENT SWEEP (event-driven in-app-nav, Phase-1 close): on a CONSTANT-URL SPA whose sections are href-less onClick controls in a `<nav>` landmark, a control inside `<nav>`/[role=navigation] is stamped node.navControl (additive landmark signal, mirrors listRow, NEVER identity) and a control in `<main>` is not; frontier-cli --emit LEADS with the nav controls (navBatch) so the equally-eligible non-nav control is deferred until the sweep drains; acting a nav opener (openerReplayable, its section-load is a POST-read) reveals + causally attributes the section (POST /api/section/groups → the nav control), the section child carries the nav opener as its reveal.statePath (replayable from a COLD re-nav), the child's GET is attributed after reveal-replay, and the 150ms in-window /api/poll is never credited — reach WITHOUT graph.states{} — FAIL-ON-REVERT: drop `inNav` in dom-snapshot.mjs OR the `node.navControl` stamp in graph-store.mjs → navControl never set → "nav-groups is stamped navControl" reds; revert frontier-cli.emit to nextBatch unconditionally → the non-nav "Refresh feed" enters the first batch → "the non-nav control is NOT in the sweep batch" reds — tests/live/menu-sweep.test.mjs
- (unit) INC.1b route-drain verdict: decideProgress returns 'visit-route' (not 'drained') when the template batch is empty but pendingRoutes>0, 'drained' only when both are empty — tests/unit/loop-control.test.mjs; and frontier-cli --emit surfaces pendingRoutes/routeFrontierStats and threads pendingRoutes into that verdict — tests/unit/frontier-cli.test.mjs
- INC.2 stateful in-session backtracking: the location-aware statefulLoop drains /a's cA1 then follows the /b nav (leaving cA2 unexplored), drains cB on /b, and ONLY an in-session backtrack navigation to /a finishes cA2 (/b has no link home) — all three explored + causally attributed (GET /api/a1→cA1, POST /api/a2→cA2, GET /api/b→cB), cA2 fired AFTER cB (non-vacuous backtrack), loop drained frontier-drained, and the in-window background poll is never credited; FAIL-ON-REVERT: force the backtrack pick to null in stateful-loop.mjs (drain the current page only) → cA2 stranded → "cA2 was explored" reds — tests/live/stateful-backtrack.test.mjs
- STATEFUL LOCATION-HONESTY provenance (Blocker-4): on a CONSTANT-URL app the stateful walk reaches a 2-deep reveal chain A→B→X by accumulated in-session state and stamps each revealed control with the accumulated opener breadcrumb (reveal.statePath) marked stateful:true — so X sits behind [A,B] (length 2, last hop its opener B), B behind [A] (length 1), and frontierInstanceStats.locations.discovered is 3 (was 1 — distinct locationKeys restored) instead of collapsing the whole app to one location; GET /api/info attributed to X, the background poll never credited; the openers fire read-over-POST so allGet is FALSE (the stamp cannot come from the vacuous GET-only default, proving the method-agnostic widen); FAIL-ON-REVERT: drop `statefulProvenance === true ||` from the step.mjs stamp gate → no reveal stamped → "X carries a reveal provenance path" reds (and locations.discovered collapses to 1); revert the `stateful: statefulProvenance === true` marker to `{ revealPath }` → "X.reveal is marked stateful:true" reds; delete `chain.push(hop)` in stateful-step.mjs → X.reveal.statePath=[B] length 1 → "statePath.length === 2" reds — tests/live/stateful-location.test.mjs
- stateful reveal provenance (1-deep): a stateful walk stamps #save's reveal breadcrumb marked stateful:true with #open as its opener hop (provenance, not a replay path) — the discriminator vs. the stateless stay-on-page replay; FAIL-ON-REVERT shares LEVER A (cold re-nav) with the reach/attribution guards — tests/live/stateful-step.test.mjs
- DURABLE-LOCATOR act-time resolution (resolveHandle): on a feed that RE-RENDERS after the baseline snapshot (the walked representative's stored nth-child instanceSelector goes STALE — proven `page.$===null`, non-vacuous), the stateful walk REACHES the control via its durable role+name locator (a live representative), genuinely explores it (not unreachable), causally attributes its request (GET /api/like/alpha → the template), and records the honest `viaRepresentative` marker (report() surfaces it); the exact-match #refresh (stored selector survives) is reached via:'selector' and is NOT flagged representative (no over-count); the in-window /api/poll is never credited (causal cleanliness at the durable act); FAIL-ON-REVERT: remove the durable-locator branch in resolve-handle.mjs (after strategy (a) `return null`) → stale selector → resolveHandle null → actStep NO_INSTANCE → statefulLoop marks it unreachable → "genuine coverage, NOT unreachable" reds — tests/live/resolve-handle.test.mjs
- OVERLAY-AWARE stateful acting (the modal-heavy-site drain fix): an act OPENS a full-screen backdrop modal that OBSCURES a base-page sibling, so a RAW click on the sibling TIMES OUT (obscured, NOT hidden — resolves+visible); the overlay-aware stateful loop STUDIES the modal control M (fired before the close), CLOSES the modal mid-walk via `dismissBlockingOverlay` (Escape, under __idle__), and REACHES the sibling B (GET /api/target attributed) — A, M, B all explored/none unreachable; the in-window /api/poll is dropped at the modal depth (raw ring WOULD miscredit, asserted non-vacuous) and the Escape-dismiss forges NO request node (exactly the two real requests credited — the closer opened no causal window); FAIL-ON-REVERT: remove the dismiss-and-retry in stateful-step.mjs (catch just re-throws) AND the close-after-study in stateful-loop.mjs drainRoute (picked===null → return false) → B click-times-out → statefulLoop marks it unreachable → "#target (B) is genuine coverage, NOT unreachable" reds — tests/live/overlay-acting.test.mjs
- (unit) mutationFloor is the ADDITIVE name-level mutation CLASSIFIER: Follow/Like/Submit/Delete/followUser classify 'mutation', an ordinary control ('Search'/'Next page') is 'safe', and an icon control (no name) is 'unknown' (honestly unclassifiable, never asserted safe); it does NOT widen REFUSED, so it labels write-ness without gating anything — tests/unit/danger-floor.test.mjs
- CHURN bucket + honest termination on a re-rendering feed (blocker-6 Part B): a feed re-renders WITHOUT stable data-ids AFTER the baseline snapshot, so the walked representative's CONTENT-keyed instanceKey ("News 0") vanishes and its positional selector goes stale (proven `page.$===null`, non-vacuous); retireLeftovers reclassifies the vanished LIST-ROW instance CHURNED (not unreachable) — peeled into `churnSkipped` (distinct bucket, UNEXPLORED by design so it stays out of `walkable`) — so the STABLE control set drains to `frontierInstanceStats.remaining===0` while the churn is QUANTIFIED (churnSkipped===1, not blocking termination, not hidden); the standalone NON-listRow #ghost that also vanished stays honestly `unreachable`; GET /api/status attributed to #stable, the in-window /api/poll never credited (pollHits>=1); FAIL-ON-REVERT: remove the `if (inst.churned){churnSkipped++;continue;}` peel in frontier.mjs → the churned unexplored representative re-enters walkable → remaining===1 → "the stable control set drained (remaining===0)" reds; drop the `node.listRow===true` branch in stateful-loop.mjs retireLeftovers → "News 0" is unreachable not churned + churnSkipped===0 → "the vanished feed row is quantified in churnSkipped" reds — tests/live/churn-feed.test.mjs
- (unit) frontierInstanceStats churnSkipped PEEL: a churned (markInstanceChurned, UNEXPLORED) feed representative is peeled out of walkable/remaining and counted in churnSkipped, so the churn never inflates `walked` while `remaining = walkable − walked − unreachable` still holds. NOTE the `remaining===0` lever moved: under BOUNDARY ROW SAMPLING a list hands out up to ROW_SAMPLE indexes, so surviving clean rows are legitimately still owed and a whole-graph `remaining===0` no longer follows from the peel — that claim is now guarded live (churn-feed.test.mjs), and the unit test asserts the peel plus the arithmetic invariant instead. FAIL-ON-REVERT: remove the peel → the churned representative re-enters walkable unexplored → churnSkipped===0 → reds — tests/unit/frontier.test.mjs
- (unit) ROW SAMPLING TERMINATES ON AN ATTEMPT BUDGET, not on churn detection: my first rule counted SURVIVORS (`cleanIdx.length < 2`) and cannot work — a re-rendering feed MINTS new content-keyed instances every snapshot, so the survivor count only grows and the rule never fires on the class it names, while boundary sampling always includes `len-1`, the freshest always-unexplored row: an unbounded act source stopping only at the run budget. Counting ATTEMPTS is monotone by construction (explored/unreachable/churned are never cleared), so a list drains after ROW_SAMPLE rows however fast it re-renders; the unexamined rows stay counted in drillSkipped. Guards both directions: a few stale rows do NOT collapse a stable table (measured, four employee-table templates each held {churned:3, clean:21} with ZERO probes — 21 untouched rows apiece reported as drained), and a feed that re-rendered its walked rows away and minted ten fresh ones is DONE, not restarted. FAIL-ON-REVERT: drop the `attempted >= rowBudget` return → the growing-feed case yields a fresh row forever → "the attempt budget is spent" reds — tests/unit/frontier.test.mjs
- (unit) NO ROW IS `walked` WITHOUT AN ACT (the fabricated-coverage backdoor): `markInstanceExplored` stamps `inst.explored` AND unconditionally sets `node.explored`; `instanceDrained` then read `i===0 && node.explored` as proof instance 0 was finished. Harmless while a list handed out ONE index — the node flag could only have come from acting instance 0 — and a live defect the moment boundary sampling handed out three: acting the middle row silently drained the FIRST row, an instance nobody resolved, nobody clicked, and which did not exist in the DOM. It was counted `walked`: one fake row per multi-sampled list template, invariant #8 (`explored ⟺ observed`) failing through a side door. `templateFlagDrains` trusts the template flag only when NO instance carries one of its own. It surfaced as a CONFUSING symptom, not an obvious one — the drained representative was never enumerated for retirement, so the churn signal went silent and churn-feed.test.mjs failed on `churnSkipped===0`; that was misread as a fixture problem and a reviewer refuted it against the source. FAIL-ON-REVERT: restore the unguarded `(i===0 && node.explored)` clause → the untouched first row counts as walked → `walked===1` reds — tests/unit/frontier.test.mjs
- (unit) THE COVERAGE LEDGER partitions the denominator with ZERO residual and names an OWNER per bucket (crawler/script/policy/app), so "why is coverage 16.7%" is answered by sorting on count instead of guessed at: first measurement said `route-never-visited 61.6% [script]` — the crawler had never navigated to two thirds of the surface, while technical failures were 0.6% combined. Checking a gate refusal BEFORE `explored` immediately deflated the numerator 148→141 (6 refused controls carried `explored:true`; the gate declined, so no act occurred). Buckets are collectively exhaustive but deliberately NOT mutually exclusive — `allReasons` is a second, non-exclusive histogram that may exceed the total and is never a denominator (it showed `cannot-resolve=23` where the exclusive view showed 2). Guards: residual===0 across every bucket, owner-totals also partition, a bucket reports the owner it was CLASSIFIED with rather than one re-derived from its name, an unmapped reason stays conspicuous. FAIL-ON-REVERT: drop the gate-refusal precedence → a refused control counts as acted → "NOT counted as acted" reds (verified) — tests/unit/coverage-ledger.test.mjs
- (unit) typed terminal churn-flat release gate (decideProgress): on an empty template batch, GROWING churnSkipped (full K-window run, not all-equal) yields 'continue' (feed not yet stable), FLATTENED churn releases 'drained', thin churn history (< K windows, churnSkipped>0) continues one more window, and zero churn drains immediately; churnSkipped rides through every verdict — FAIL-ON-REVERT: remove the churn-flat gate → the growing-churn case falls through to 'drained' → "churn still growing must NOT drain" reds — tests/unit/loop-control.test.mjs
- click-time DANGER-ROUTE href gate (security H1 — protects EVERY actStep caller): an ICON-ONLY same-origin `<a href="/logout">` with an empty accessible name slips the NAME-floor and, as a plain GET link, would NAVIGATE the browser to /logout (session-ending) on click; actStep refuses the click on `routeKey(href)` (DANGER_FLOOR) BEFORE it fires, so the browser never reaches /logout (logoutHits 0). Driven UNAUTHENTICATED, so the gate is proven on its own. FAIL-ON-REVERT: `if (false && href && routeRefused(routeKey(href)))` in step.mjs → whats-new resolves (no rejection) + the click navigates → "Missing expected rejection … must reject with DANGER_FLOOR (the href route, not the name)" reds — tests/live/logout-link-gate.test.mjs
- COMMUNICATION hard-refusal (the fire-path half of the danger-floor classification; the `destructive` twin of danger-gate.test.mjs): initiating a real-time call is an IRREVERSIBLE OUTWARD side-effect — nothing downstream can undo it after the fact — so `communication` sits in the always-consulted REFUSED set and the CLICK path refuses it before the request leaves the browser. Acting "Video Call" rejects DANGER_FLOOR and the server's `callHits()` stays 0 (server-side ground truth, non-vacuous). FAIL-ON-REVERT: drop `communication` from REFUSED (danger-floor.mjs) or the `REFUSED.has(floor)` arm in step.mjs actStep → the click goes through → `assert.rejects` reds and callHits becomes 1 — tests/live/communication-gate.test.mjs
- (unit) contextOptions blocks service workers (security M2): `serviceWorkers:'block'` at the single context builder, on BOTH the default and anonymous surfaces, so a SW background-sync POST cannot escape page-level request observation — FAIL-ON-REVERT: drop `serviceWorkers:'block'` from `base` → "the default context blocks service workers" reds — tests/unit/session-context.test.mjs
- ROUTE-MANIFEST seeder (whole-site denominator, Increment #1): on a constant-URL onClick SPA whose served bundle declares its router, `extractRoutes` returns EXACTLY the declared STATIC `path:` values (a v6 RELATIVE value normalized with a leading `/`; `:param` routes split into `paramRoutes`) using ONLY the `path:"<value>"` config key — a bare leading-slash literal fallback is ABSENT, so NOISE literals NOT under `path:` (`/reactions`, `/accept` — the socket.io/redux-fragment class that inflated the real bundle) AND a minified `path:")".concat(…)` expr fragment are REJECTED (path:-only precision, the anti-inflation guard); `seedManifestRoutes` seeds the statics as `declared:true` PENDING nodes (skipping the danger `/logout` via `routeRefused`) and the count ≫ the a[href]-only harvest (the denominator EXPANDS); seeding adds ZERO elements + ZERO edges (`declared` additive, never identity; no addTrigger/causal window); a full `recon-run` crawl GENUINELY visits the real sections + `markRouteUnreachable`s the redirecting one (route-frontier `visitRoute` reused UNCHANGED), and `report --route-coverage` reports reached < declared (honest, not a bare DRAINED); the `crawl({seedManifest:false})` differential collapses `declaredManifest` to 0 — FAIL-ON-REVERT: disable the seeder → "/b was genuinely visited (not pending/unreachable)" reds; re-add a bare-literal fallback → `/accept`+`/reactions` extracted → "a bare noise literal is NOT extracted" reds; drop the expr-char filter → "/)" extracted → the expr-fragment assertion reds — tests/live/route-manifest.test.mjs
- HONEST route-coverage denominator (the over-count fix): a "reached" route is COLLECTED only when it rendered its OWN content (an element attributed to it). (Fix 1) a SAME-ORIGIN CLIENT-SIDE redirect (`/redir` → `/`, whose pre-settle URL is still `/redir` so the pre-settle guard passes it) is caught by `visitRoute`'s POST-settle re-read → `markRouteUnreachable('redirect')`, NOT visited — the landed content is already attributed to its own route, so counting it reached would double-attribute; (Fix 2) `routeCoverageOf` splits visited into COLLECTED (own content) vs VISITED-BUT-EMPTY (0 controls — `/empty`, Not-Found/content-starved) — `collected` counts ONLY routes with own elements (`/a`), the empty bucket is listed not hidden; driven by a direct `visitRoute` per route (no full crawl) so the three buckets are isolated — FAIL-ON-REVERT: neutralize the POST-settle redirect re-read in `visitRoute` → `/redir` snapshots the landed `/` DOM (phantom control) under `/redir` → "a same-origin redirect must not count as a reached section" reds; collapse the empty bucket into collected (`collected := visited`) → `/empty` inflates it to 2 → "a visited-but-empty section must not count as collected" reds — tests/live/route-manifest.test.mjs
- HONEST DENOMINATOR / phantom-route (GOAL 1, measurement gate): on a constant-URL 200-everywhere SPA the manifest seeds a denominator INCLUDING dead routes; each phantom is RELABELLED out of the collectable base, never dropped. (Q1) a LATE client redirect (`/legacy` `replaceState('/dashboard')` 200ms AFTER settle) is caught by `visitRoute`'s BOUNDED routeKey poll (POLL_MS=50, POLL_MAX=6, early-out on first divergence, never early-confirm on stability) → `unreachable:'redirect'`; a FAST redirect (`/old-home`) stays caught pre-settle. (Q2) a NEGATIVE-CONTROL probe (`probeNotFound` GETs a guaranteed-nonexistent path → `graph.notFoundSig` from a text-free/attr-free `contentSig`) labels a visited-but-empty route whose sig equals it as client-404; a REAL content-starved section (`/inbox`, DISTINCT sig) is SPARED — the case a naive "N empty routes share a sig → collapse" cluster dedup fails and the probe-anchor passes; `collectable` = declared − redirect − client-404 (all buckets still sum into `declared`, listed not hidden). Discovery + probe are EDGE-FREE (`edges===0` asserted). FAIL-ON-REVERT: replace the poll with a single post-settle read (POLL_MAX=1) → `/legacy` snapshots visited-but-empty → "/legacy LATE redirect caught" reds (verified); neuter `probeNotFound` → `/groups` not client-404 → the clientNotFound assertion reds; drop the contentSig write in `visitRoute` → collectable count reds — tests/live/phantom-route.test.mjs
- PARAM-INSTANCE harvest (GOAL 2, collect the dark `:param` patterns): the manifest seeds `/item/:id` + `/user/:handle` as `param-pattern` (counted, never navigated); the edge-free seed path harvests a concrete `<a href>` and GENUINELY visits it, and `graph-store.matchParamPattern` (SEGMENT-align, not toUrlPattern equality) + `tagParamInstance` link it `paramInstanceOf` the pattern. NUMERIC `/item/:id` → one visited `/item/1` (census bound, rows folded); STRING-KEYED `/user/:handle` → `/user/alice` linked structurally (toUrlPattern leaves a word segment unmasked, so pattern-equality would miss it) + `/user/bob` folded; `routeCoverageOf.paramCollected===2`, `collectedTotal` folds them in, the concrete proxies are NOT counted as static sections. FAIL-ON-REVERT: drop the harvest param-branch/`tagParamInstance` → paramCollected 2→0 reds; drop the string-keyed structural fold → /user/bob visited separately → the census assertion reds — tests/live/param-instance.test.mjs (fixture param-feed-app, bundle declares `path:"/item/:id"`+`path:"/user/:handle"`); pure segment-align guards in tests/unit/param-pattern.test.mjs
- PORTAL-DROPDOWN OWNERSHIP (`hunt-gate.ownsViaReveal` — the ownership proof the explore-all foreign-content rail reads back; a live target finding): AntD renders a row's Edit/Delete dropdown as a body-PORTAL detached from the post card, so `ownsTarget` on the delete button finds no marker (fails closed, blocks a legit delete-own). A portal control's ownership = its reveal-TRIGGER's item (the row's "…" more button, inside the card): resolve the trigger from the reveal path's last hop + marker-check IT. On the `portal-app` fixture (a `#portal-menu` Delete detached from two cards, one marked): ownsViaReveal via OUR marked row's "More" → owned; via ANOTHER user's row → refused; empty reveal path / no marker → fail-closed. FAIL-ON-REVERT: neuter ownsViaReveal→false → the "our marked row → owned" assertion reds — tests/live/portal-ownership.test.mjs
- REVEAL-BACKFILL preVisible transition (in-app-state reach, Fable design): a PORTAL dropdown menuitem MOUNTS on open → never in the DOM while hidden → captured only visible → write-once `hiddenWhenSeen` structurally always false → old `fillRevealIfHidden` never backfills → NO_INSTANCE on cold replay. Fix: `actStep` captures `preVisible` (controls visible before the act, read-only snapshotDom under `__idle__`) → `fillRevealIfHidden` uses `!preVisible.has(key)` (revealed-by-this-act) instead of hiddenWhenSeen, legacy-fallback when not threaded. UNIT (the real fix guard, on a KNOWN instance = the first-target case): preVisible transition backfills a first-captured-visible menuitem (FAIL-ON-REVERT: revert to `inst.hiddenWhenSeen` → no backfill → reds, verified); NO backfill when already-visible-pre-act (coincidental-control guard); legacy hiddenWhenSeen path preserved — tests/unit/reveal-backfill.test.mjs. LIVE reach (mount-on-reveal portal menuitem revealed + acted via reveal-replay, GET /copy caused) — tests/live/portal-menu-reach.test.mjs (fixture portal-menu-app; note it passes via the new-instance stamp too, so the UNIT test is the fix's revert guard)
- PORTAL-MENU IDENTITY (INC.2, CTO/Fable-validated — the reveal-backfill above shipped green but did NOT reach the live target portal Delete; the defect is UPSTREAM in IDENTITY): a body-portal dropdown (AntD `.ant-dropdown-menu-item`) mounts items into a bare `<body>`-child div → `buildPath` emits one templateSelector for every action + `rowKey()===null` forces an open-order `#N` → all actions collapse onto ONE template AND collide on the same `#N` (measured live: 4 collisions/open). Fix: `dom-snapshot.templateSelectorOf` folds the menuitem NAME into the TEMPLATE selector (` @menu(<action>)`) for a detected portal menuitem. On `portal-menu-multi-app` (own row Edit/Delete/Share, other row Share/Block/Report/Become-a-Fan(N)/My-Events[value=MY_EVENTS], bare positional portal, count grows each open): Edit/Delete/Share become 3 DISTINCT templates (not 1); Share Link is ONE template across both menus; Delete and Block account are DISTINCT instances (positional collision gone); "My Events" folds from the enum `value`; the count-badge is stripped so re-open keeps ONE "Save Item" template (no per-render explosion); a cold act on Share Link replays its stamped reveal path + resolves by role+name → GET /share (reach, not NO_INSTANCE); the 120ms /poll is never causally credited. FAIL-ON-REVERT: neuter the `@menu(...)` fold → menuitems collapse to one template + collide, the "Edit/Delete/Share all captured" + distinct-template assertions red (verified). schemaVersion 2→3 (loadGraph resets a scheme-2 graph); proven 0 folds on non-portal fixtures (byte-identical) — tests/live/portal-menu-multi.test.mjs
- TYPED FIELD ACTUATION (INC.6, the measured cause of six live crawls creating NOTHING): a create form whose REQUIRED fields are an antd Select + DatePicker (both rendered as a READONLY `<input>`) + a checkbox could never be submitted — `form-fill.fieldsFor` enumerated only `input`+`textarea` and skipped `readOnly`, and `step.mjs` put values in with `fill()` alone, so the submit was clicked, client validation refused, the server heard nothing, and the control was scored COVERED (live trail: "Create Event" clicked 3×, only the modal's own `get_status_detail` ever left the browser). Fix: `fieldsFor` classifies a `kind` (fill/select/date/check/upload/native-select) and stops dropping readonly/file, `field-actuate.actuateAll` drives each kind with the right Playwright API (fill/selectOption/setChecked/setInputFiles + the antd click-the-portal-option dance), and a non-text field carries `value:null` (never an invented string in the trail). Proven by the fixture's SERVER-SIDE create counter going 0→1. FAIL-ON-REVERT: restore the `f.readOnly` skip in fieldsFor → the select/date are never enumerated → "antd select must be found, not skipped as readonly — got [\"check\",\"fill\"]" reds (verified); make actuateAll always actuateFill → the readonly inputs reject the fill → createHits() stays 0 — tests/live/typed-form-fill.test.mjs (fixture typed-form-app)
- (unit) REVEAL-PATH INVARIANTS (INC.6): a recorded reveal path must be WALKABLE. Measured on the live graph after six runs — 494 paths, 493 stateful provenance (the stateful driver's own contract calls them "NOT replay"), 39 cyclic, 22 routed THROUGH a dismiss control (replaying tpl 933 "Group Name"'s path `[26,98,79,900 "cancel",902]` closes the modal the field lives in) — and because paths are written first-reveal-wins with NO invalidation, every one was frozen permanently. Both halves guarded: `replayRevealPath` refuses `reveal.stateful` (REVEAL_PROVENANCE_ONLY) and a dismiss-bearing path (REVEAL_DISMISS_IN_PATH) BEFORE touching the page (asserted via a page stub that throws on any click), a clean one-hop path still passes; `isDismissControl` is whole-name anchored so "Close" is a dismiss and "Close Account" is NOT (mis-classifying it would rank a destructive control last AND keep it out of reveal paths). The write side is fixed in `stateful-step.mjs` (a dismiss hop or a repeated template never enters the chain) and the 494 poisoned paths are invalidated by the schemaVersion 5→6 bump. FAIL-ON-REVERT: `if (reveal.stateful)` → `if (false)` → the provenance path is walked → "a stateful provenance path must be refused" reds (verified); drop the `^…$` anchors in DISMISS_RE → "Close Account is not a dismiss control" reds (verified) — tests/unit/reveal-path-invariant.test.mjs
- SAME-NAME REPRESENTATIVE (INC.6b — the root cause of zero created entities across seven live runs, and the most expensive bug of the project so far): when an instance's stored positional selector goes stale, `resolveHandle` falls back to a durable role+name locator — but `page.getByRole` searches the WHOLE PAGE. On the live target the button that OPENS the Create Event modal and the button that SUBMITS it share the accessible name "Create Event"; with the modal shut the submit's selector failed, the fallback resolved the OPENER, and the crawl clicked the opener while recording the act against the submit. Seven runs read as "Create Event exercised, fired only get_status_detail" — the submit was never once clicked (a hand probe clicking it fires POST /api/meetings-events and creates the event). Fix: a role+name candidate must satisfy `el.matches(node.templateSelector)`, so the resolver returns the RIGHT control or none, never a same-named impostor from another template; fail-safe when no templateSelector is recorded. Proven on a fixture whose opener and modal-submit share a name, with the SERVER-SIDE create/open counters as ground truth (resolved handle id === 'submit', createHits 0→1, openHits unchanged), plus the converse (the opener's own node still resolves to the opener — the guard does not reject everything). NOTE: this class is INVISIBLE to the trail, which records the act's INTENDED target, not the element clicked — only driving one flow by hand surfaced it. FAIL-ON-REVERT: drop the sameTemplate filter (return the first visible handle) → "the resolved handle must be the SUBMIT, not the opener that shares its name" reds (verified) — tests/live/samename-representative.test.mjs (fixture samename-app)
- REVEAL RECENCY ordering (INC.6c — the same bug three times, each fix exposing the next): (1) candidates were handed out in ascending templateId order and freshly revealed controls get the HIGHEST ids, so a modal's contents were always last — 0 of 46 revealing acts drained their own revelation; (2) marking "revealed by the LAST act" fixed that, but one non-revealing act (clicking a field reveals nothing) wiped the set, losing the modal mid-drain; (3) accumulating instead of overwriting fixed that and BROKE THE ORDERING — a nav-like act reveals a whole page (52 controls, measured live), after which everything is "revealed", the boolean orders nothing, `sort` is stable, and the walk falls straight back to ascending id. So `pickLive` ranks by WHEN a control was revealed (most recent first), dismiss controls last of all. Driven through `statefulLoop` with an injected step + fake page (control-flow unit, no browser): a nav act reveals 52 low-id page controls, a page control then mounts a 2-control modal, and the NEXT pick must be the modal's submit (id 900) rather than "Page 1" (id 11) which is equally revealed but revealed EARLIER; Cancel must not be acted before the modal is drained. FAIL-ON-REVERT: collapse the comparator back to `rank(a) - rank(b)` (boolean, no recency tie-break) → "the modal's control must be acted before the page control revealed earlier — recency, not a flag" reds (verified) — tests/unit/reveal-recency.test.mjs
- (unit) ROUTE CARRY-FORWARD across a schema reset (INC.6d — the regression that shrank the crawl to one page): a schemaVersion bump invalidates the ELEMENT identity scheme but says nothing about routes, which are URL-keyed navigation METADATA; `loadGraph` nevertheless returned a bare `makeGraph()`, so the v4→v5 bump deleted 81 `declared:true` manifest-seeded routes and the route universe collapsed from 85 distinct patterns to 3 (`/groups`, `/events`, `/chats`, `/profile`, `/setting` never queued again; two of the six user flows produced ZERO acts; the runner then printed "everything reachable is collected" over the shrunken denominator). The subtlety a naive carry-forward gets WRONG: `visited` is the ABSENCE of `pending`, so copying verbatim marks every route already-collected while its elements were just discarded — discovery kept, collection silently skipped. Guards: routes survive the re-key RE-ARMED to pending; an `unreachable` route keeps its verdict (a 404 is a fact about the server) and is not re-queued; a CURRENT-scheme graph is loaded untouched (re-arming is reset-only, never a re-walk of a good graph). FAIL-ON-REVERT (two levers, both verified): restore `return makeGraph()` → "the declared route survived the re-key" reds; drop the re-arm (`routes: raw.routes || {}`) → "a carried-forward route is re-armed for collection" reds — tests/unit/schema-reset-routes.test.mjs
- (unit) STATEFUL MODE MINTS NO REVEAL BACKFILL (INC.6d — the poisoned-path writer that survived its own cleanup): the schemaVersion 5→6 bump was made to invalidate 494 unwalkable reveal paths, but `fillRevealIfHidden` kept minting them and had written 250 more (173 onto controls never hidden). A reveal path is a COLD-REPLAY artifact and `reveal-replay` refuses a `stateful:true` path by contract, so stamping one burns the write-once slot on a path nothing can walk. Worse: the backfill clears `explored` + deletes `unreachable`, and because `preVisible` is a VISIBILITY set, a control that sat under an open modal reads as "revealed by this act" when the modal closes — so an act that FAILED on an intercepted click had its failure flags wiped, was re-ranked as freshly revealed, and was picked again to fail identically (live: tpl 25 at seq 34 and seq 44, same interception). Guards: in stateful mode the backfill is inert (no path minted, no failed act resurrected); the STATELESS panel-reach backfill it exists for still reopens a NOT_VISIBLE-drained hidden instance (the fix is mode-scoped, not a blanket disable). FAIL-ON-REVERT: delete `if (stateful) return false;` → "a failed act must NOT be resurrected by a stateful backfill" reds (verified) — tests/unit/stateful-no-reveal-backfill.test.mjs
- SAME-NAME, CONTAINER CLOSED (INC.6d — the hole through the INC.6b guard, and a doctrine failure worth recording): the INC.6b test opens the modal BEFORE resolving, so both same-named buttons are visible, the role+name branch succeeds, and control never reaches the text fallback beneath it — the test stayed green while the bug it was written for went on happening across further runs. With the container SHUT (the state every cold re-navigate and most acts leave behind) role+name finds only the opener, the `sameTemplate` guard correctly rejects it, and execution falls through to `getByText(name,{exact:true})`, which had NO structural check and handed the opener straight back; both "Create Event" templates ended up with a causal edge to the opener's own `POST get_status_detail`. Guards: EVERY name-based fallback in `resolveHandle` is structurally gated, so a stale selector yields an honest null rather than a same-named control from another template; nothing is clicked while merely resolving (server counters stay 0). LESSON: revert-proving the guard is NOT sufficient — the test must reproduce the STATE the live system is in. FAIL-ON-REVERT: drop the `sameTemplate` check in the text fallback → `got id="opener" via="text"` → "must not fall through to the text fallback and return the opener" reds (verified) — tests/live/samename-representative.test.mjs
- (unit) DISMISS BLOB classification (INC.6d): a modal's real close control often has no accessible name of its own, so the synthesizer falls back to the container's concatenated text and emits a blob — the live Create Event closer is `role=generic` named `closeSchedule a Meeting EventMeeting TitleEvent Type…`. The anchored whole-name pattern cannot match one, so under recency ordering it ranked as an ordinary fresh candidate and was picked FIRST: the pass meant to drain the modal shut it instead (seq 66 opened, seq 67 closed, seq 69 could no longer resolve a field inside). Guards: a long blob LED by a dismiss verb classifies dismiss (so it ranks last and stays out of reveal paths) while the anchored rule still governs short names — "Close Account" is NOT ranked away as harmless, and a long name not led by a dismiss verb is not a dismiss. The pattern is deliberately `\b`-free and case-explicit: the blob is concatenated (`closeSchedule`), so `\b` never fires at the camel seam, and under `/i` a `(?![a-z])` lookahead would exclude the uppercase that seam depends on. FAIL-ON-REVERT: drop the prefix/blob arm → "the modal's blob-named close control must classify as a dismiss" reds (verified) — tests/unit/reveal-path-invariant.test.mjs
- (unit) CLIENT-404 WITH A NON-EMPTY SHELL (INC.6e — a phantom route counted as a collected section): the client-404 label exists so a constant-URL SPA's declared-but-unimplemented routes leave the collectable denominator instead of masquerading as content-starved sections. Both consumers (`render-probe.deriveVerdict`, `route-coverage.routeCoverageOf`) restricted the check to visited-but-EMPTY routes, on the stated reasoning that a Not-Found page carries no controls — but the live shell renders ONE, so all 18 dead routes (`/groups`, `/feed`, `/reports`, `/engine.io`, `/articles`, … all sig 2110f3b4, all own=1/1) had content, landed in `collected`, and never reached the filter: collected 44 vs the true 26, collectable inflated 41%. The sig is now decisive and checked FIRST — `contentSig` is structural (text-free, attr-free), so matching a random nonexistent path byte-for-byte means the same shell rendered, whatever it contains. Guards: a shell-rendering route is a phantom EVEN WITH CONTROLS, in both the standalone probe verdict and the crawl's own coverage report; a real section with a distinct sig is untouched; a genuinely content-starved route stays `visited-empty` (an honest gap, never relabelled dead); every phantom stays LISTED in `clientNotFound` (the accepted risk runs toward false-dead, so disclosure is the mitigation). FAIL-ON-REVERT (one lever per consumer, both verified): restore the content-first order in `deriveVerdict` → "a Not-Found shell WITH a control is still a phantom" reds; restore the empty-only filter in route-coverage → "the shell-rendering route is labelled a phantom and LISTED" reds — tests/unit/client-404-shell.test.mjs
- OUTCOME OBSERVABLES — REFUSED vs INERT on EVIDENCE, not inference (the observation surface the probe battery will read; nothing wired into the crawl yet): each refusal tier is readable in ISOLATION — tier 1 WHATWG constraint validation (the `valueMissing` flag names WHY plus the browser's own message), tier 2 the ARIA `aria-errormessage` text a screen reader would announce, tier 3 the AntD `.ant-form-item-explain-error` blob (the only tier most React targets answer on) — a live-region toast is recognised as success with the fixture's server-side `postHits()===1` as ground truth (non-vacuous: the act really reached the server), and a page that has SAID NOTHING is NOT reported refused. That last one is the false-positive direction and the whole point: "no request fired ⇒ validation refused" was asserted across six live runs and measured flatly wrong, because the submit had never been clicked (INC.6b), so nothing had ever been validated. Tier 1 reads `:user-invalid`, NOT `checkValidity()` alone — an empty required field is invalid the instant the page loads, so `checkValidity()` reported a refusal on a page that had said nothing, the exact inference the module exists to remove; the test caught that. The success channel stands alone because the live Create Event form answers on NO refusal tier and accepts a completely EMPTY submit with a 201, announcing only "Event was successfully created". FAIL-ON-REVERT (one lever per direction, both verified): drop the tier-3 branch from `readOutcome`'s frameworkErrors query → "the AntD explain text is read" reds; make `wasRefused` fall back to "no requests fired" → "a silent page is not a refusal" reds — tests/live/observables.test.mjs (fixture outcome-app)
- (unit) THE KNOWLEDGE LADDER — coverage that cannot be earned by clicking (the METRIC only; nothing consumes it yet, `markInstanceExplored` still means what it meant): `explored` was set the moment one act returned, before the outcome was inspected, and the frontier's drain predicate read that flag — so a control clicked once that recorded nothing scored identically to one we actually understand (measured over one run: 279 acts, 27% caused any request, 43% wholly inert, 21 of 32 submit-like controls clicked with ZERO fields filled, 0 of 6 user flows completed, coverage climbing throughout). Guards: a click alone stops at L1 REACHED and every rung above it demands recorded probe rows; a field owes a probe per DECLARED constraint (`batteryFor`), so a COMPLETE BATTERY rather than an act count earns L3 CHARACTERIZED; `rejected` is a verdict distinct from `inert` (the page said no vs. the control is dead); L-1 BLOCKED stays in the denominator LISTED by name and reason instead of quietly subtracted, while a PARTIALLY blocked element still owes what remains and is never written off; L4 CONFIRMED needs the outcome reproduced or a write read back; widget chrome (INC.6f) is not an obligation. FAIL-ON-REVERT (one lever per direction, both verified): make `levelOf` return L3 whenever `node.explored` is set — precisely the old behaviour — → "a click alone is not understanding" reds; make `verdictOf` return 'inert' when no requests fired regardless of refusal → "the page said no: a working control we failed to satisfy" reds — tests/unit/knowledge.test.mjs
- THE PROBE BATTERY — learning what a field accepts without INVENTING defects (the instrument only; nothing in the crawl calls it yet): a field probe is a transaction (fill → commit → read), because a field answers nothing on its own — typing 51 characters into a field declaring a 50-character limit produces no request, no error, no observable at all, and the answer exists only after a COMMIT (the previous model clicked fields as though they were buttons, 37 of 53 such acts inert by construction). The assertion that matters most is the NEGATIVE one: a declared boundary that IS enforced yields NO conflict. Measured live — "Meeting Title" declares maxLength 50, the probe typed 51, the browser truncated to 50 natively — so judging the boundary on what we TYPED would have reported a violation against a field enforcing its limit perfectly; the verdict is judged on what the field actually HELD (`#capped` holds 10 of 11, no conflict), while a field that DECLARES a limit and does not enforce it (`#leaky`, holds 11, commit goes through) IS reported, and a required field that accepts an empty commit is reported too (the live Create Event behaviour). A field declaring nothing is not probed at all — `valueForProbe` returns null and the row reads `NOT_APPLICABLE` rather than inventing an input with no prediction to falsify. The commit is INJECTED (the caller's measured act), so the battery opens no causal window of its own, and the fixture's server-side `commitHits()` is the non-vacuous ground truth that the commits really reached the server. FAIL-ON-REVERT (one lever per direction, both verified): judge overflow on the typed length (`accepted` → `value.length`) → "an enforced limit is not a defect" reds — the false-finding direction; make `valueForProbe('fill-overflow')` return a fixed long string when no maxLength is declared → "a field that declares nothing is not probed" reds — probing without a prediction to falsify — tests/live/probe-battery.test.mjs (fixture outcome-app, extended with the three boundary shapes)
- (unit) OUTWARD-FACING acts refused on every tier (the FOREIGN_ADDITIVE gap): an act that LEAVES THE SYSTEM and reaches a person or a third party is refused `OUTWARD_REFUSED` at the head of `explore-policy.decide` — before ownership, never lifted by any mode — so "Report content" / "Block account" / "Invite by email" / "Send SMS" are refused on FOREIGN, OWN and NONE content alike. They had been allowed on another user's content as FOREIGN_ADDITIVE ("creates nothing of theirs to lose"), which is true of a like and false of a complaint: a report reaches a moderator, nothing downstream undoes it, and unblocking is a separate act the crawler will never perform. Measured: clicked dozens of times across three runs, reaching nobody ONLY because the confirmation modals were never completed — the accidental protection the probe/episode work removes. The line is the SYSTEM BOUNDARY, not the environment (a dev stand is routinely wired to a real SMTP/SMS gateway; an environment vouches for the app's own data being fixtures, never for the outgoing integrations). BOTH directions guarded: an in-app message ("Send Message"/"Add Friend"/"Comment"/"Like"/"Follow"/"Invite") is NOT outward — it is a row in the app's own database and one of the six target user flows — and payment/real-time calls are NOT folded in either, still firing on the operator's own content, because explore-all exists to classify every control by actually firing it (the operator's scoping decision, asserted from the other side so a later widening cannot quietly re-impose the blanket refusal the mode was built to remove); the surviving FOREIGN_DESTROY / restore-bracketed edit / ACCOUNT_PROTECTED verdicts are re-asserted unchanged. FAIL-ON-REVERT (one lever per direction, both verified): remove the unconditional block from the head of `decide()` → "Report content must be refused even on foreign content" reds (FOREIGN_ADDITIVE restored for moderation acts); widen OUTWARD to catch a bare "send"/"invite" → "an in-app message is not outward" reds (the over-refusal direction that would silently delete a target flow) — tests/unit/outward-refused.test.mjs
- (unit) THE ACT ON A FIELD IS A FILL, recorded by FACT not intention: 14 measured elements sat at L2 owing `fill-valid` while holding only `click` rows — an obligation `probe-kinds.satisfies()` deliberately refuses a click, and rightly, since clicking a textbox teaches nothing about what it accepts. `kindOf` now reads actStep's returned `selfFilled`, not the value we hoped to type, so a readonly input / a widget whose typed actuation failed / a control that turned out not to be a field records `NOT_FILLABLE` instead of minting a `fill-valid` row on a fill that never happened; `fillTarget` also refuses its nearest-input fallback for a self-target, because filling a neighbour answers nothing about the target and would file the neighbour's behaviour under the target's name. FAIL-ON-REVERT: pass `selfFill` (the intended value) to `kindOf` instead of `res.selfFilled` → a field that could not be filled scores CHARACTERIZED on evidence that does not exist — tests/unit/knowledge.test.mjs
- (unit) A FAILURE TO ASK IS NOT AN ANSWER — `TRANSIENT_BLOCKS`: a field owes three probes, the first commit closes its modal, probes two and three return NO_INSTANCE; with one undifferentiated blocked bucket `outstanding` empties, `terminal` reads CHARACTERIZED and the element scores L3 UNDERSTOOD having answered one probe of three. Transient codes (NO_INSTANCE / NOT_VISIBLE / ACT_FAILED / CONTAINER_CLOSED / ALIAS_COLLISION) now discharge nothing; terminal facts (NOT_FILLABLE, a policy refusal) still discharge while staying NAMED. Also: BLOCKED means UNPROBEABLE, not "we failed" — an element whose every row is transient is L1 REACHED, so 14 fixable elements stop being reported to the operator as a permanent ceiling. FAIL-ON-REVERT: delete TRANSIENT_BLOCKS from `probeStatus` → "a failure to ASK" reds (verified) — tests/unit/knowledge.test.mjs
- (unit) RETRY WHAT WE NEVER MANAGED TO ASK, and the four frontier readers agree: an act that threw still marked its instance explored, so 15 measured elements left the frontier having taught us nothing and could never be handed out again. `retryable` re-opens exactly the every-row-transient case, bounded by MAX_RETRY_ROWS=3. The subtle half is agreement: `frontierStats.remaining` and `frontierInstanceStats` computed the drain predicate INLINE, so a retryable element was handed out by `nextBatch` while the controller read `remaining===0` and printed "everything reachable is collected" — the false-done this file's own header warns about, in the opposite direction. And a POST-CLICK failure is NOT retryable: the control already fired, so a retry is a second real mutation under explore-all. FAIL-ON-REVERT (four levers, all verified): drop `&& !retryable(node)` → the transient case reds; drop the TRANSIENT_BLOCKS test → a DANGER_FLOOR/FOREIGN_DESTROY refusal is re-walked forever; drop MAX_RETRY_ROWS → the budget drains into the one element that never works; restore the inlined predicate at frontier.mjs:183 → `remaining` reads 0 while nextBatch yields 1 — tests/unit/frontier-retry.test.mjs
- THE ALIAS GATE — two instances must never be credited from one node: run probe6, template 392, instance keys 05..11 — seven acts, seven instances credited, one IDENTICAL rect on every one; stale positional selectors sent `resolveHandle` to its `getByText` fallback, which returned the same first-visible node each time, so one control was clicked seven times and written into the graph as knowledge about seven different controls (and under explore-all, seven real interactions with the operator's stand). It survived because identity was only ever checked at the SELECTOR layer, and two selectors resolving to one node is exactly what a selector-layer check cannot see. `claimNode` stamps a NON-ENUMERABLE expando on the resolved node just before the click (after every refusal path, so a node we never acted on is never claimed) — invisible to tagName/id/classList/attributes/style/rects/textContent, hence structurally incapable of perturbing identity, which is the assertion that matters most since a marker leaking into identity would be worse than the bug it fixes. A collision is TRANSIENT ("identity unproven"), never a verdict of dead. Fired 6× on the next live run — 4 of them via `selector`, not the text fallback, so the fault was wider than the forensics suggested. FAIL-ON-REVERT (two levers, both verified): make `claimNode` always return ok → the collision assertion reds; make the expando enumerable → the `Object.keys` assertion reds — tests/live/act-alias.test.mjs
- A FIELD ANSWERS FOR ITSELF, and never borrows its neighbour's error: run probe9 left four textboxes filled successfully and recorded `inert` ("Search Community", "Search your connections", "Group Name", "Search <App>") — nothing wrong with the fill, they simply have no commit control to answer to, and under a commit-only model they could never leave L2. The search says field→commit on nameless markup is unsolved (the classical crawlers take the submit for free from `<form action>` and solve only label→field; the LLM agents press Enter and hope), but the CLIENT already holds the answer: AntD's Form.Item validates on `validateTrigger`, defaulting to `onChange` (confirmed from framework source), so the error node is in the DOM after a fill and a blur with no submit anywhere. `fieldScopeSelector` + `settleField` feed `readOutcome` a scope of ONE field's own region. SCOPE IS THE LOAD-BEARING PART: page-scoped would read a neighbouring field's leftover error as this field's answer — the borrowed-evidence fallacy that once made a single toast the recorded outcome of three separate acts — so the fixture deliberately keeps an ALREADY-ERRORED field next to the one under test. A field with no region yields null (an honest "no local answer") rather than a page-wide fallback, and a valid value is not reported refused. FAIL-ON-REVERT (both verified): return 'body' when no region is found → "a neighbour's error is not this field's answer" reds; drop the `.ant-form-item` branch → "the field's own error is read from its own region" reds — tests/live/field-scope.test.mjs
- (unit) ROUTE-STAGE TIMINGS — THE RUN'S MISSING TIME IS NAVIGATION, and the trail could not say so: attributing every inter-event gap to the event that follows it, `route` events are 68.6% of `hygge2`'s 1504s wall clock (n=662) and 74.9% of `goal1`'s 2196s (n=537, mean 3063ms, max 38279ms), while the act path is ~78% self-explained by the `{actMs,settleMs,snapMs}` step.mjs already emits. `payload.timings` was undefined on all 1200 route events — `goToRoute` emitted its event with no durations — so the project's own record ("613 of 663 navigations produced neither an act nor a single new element") could be COUNTED and never DIAGNOSED: nobody could say where the time went, which this repo's log rule makes a defect to fix rather than a limitation. Guards: every `route` event carries `{gotoMs, settleMs, overlayMs, snapMs, totalMs}` under the SAME field names an act uses (ONE renderer draws both row kinds), and `totalMs` is the whole function's wall time and NEVER the sum of its parts — a summed total reports zero unaccounted work BY CONSTRUCTION, hiding exactly the gap the guard exists to expose. Timings are REPORTING ONLY: a Node-side `performance.now()` read opens no causal window, so attribution stays token + CDP initiator and is never fed by a wall clock (injected fakes, no browser). FAIL-ON-REVERT: drop the `timings` field from goToRoute's traceEvent (stateful-loop.mjs) → "the route event must carry stage timings" reds; fabricate `totalMs` by summing the parts → the unaccounted-gap assertion degenerates to equality; remove the goto bracket → "the navigation stage must be measured into gotoMs" reds — tests/unit/route-timings.test.mjs
- (unit) THE FAILED ACT'S TRAIL ROW — the 15% of every run the trail could not describe: three defects in the ONE payload `stateful-step.recordFail` writes. (1) THE WRONG EVENT KIND — it wrote `kind:'act'` while `trace.readActFailed` (the reader `report --unreached` PREFERS over the graph's coarse unreachable reason) filters on `kind:'act.failed'`, so every run made by the LIVE driver (recon-run → statefulLoop → statefulStep) returned `[]` from it and the granular NO_INSTANCE / NOT_VISIBLE / ALIAS_COLLISION / REVEAL_* codes were computed, written down, and discarded by the only consumer that exists for them (`hygge2`, `goal1`, `goal2` and every `explore*` run contain ZERO act.failed events). (2) NO TIMINGS, NO RECT — 54 failed acts of 355 in `hygge2`, 56 of 408 in `goal1`, all with `shots:null` and no `timings` key, though `actStep` captures the before-frame (shot + rect + viewport) under `__idle__` BEFORE every failure path, so it EXISTS and the throw simply dropped it. (3) ONE COLUMN, TWO MEANINGS — the success payload writes `route` = where the act LANDED, the failure payload wrote `route` = where it was AIMED (divergent on 67 of 301 acts in `hygge2`, 199 of 352 in `goal1`), so a viewer with a single "route" column silently mixed intent with fact — the same class as the trail that recorded an act's INTENDED target instead of the element clicked and hid a wrong-control bug for seven runs; `requested` is the vocabulary `route` events already used. Plus `probeKind` (WHICH battery obligation the act was discharging), threaded through every recordProbe call and emitted on 0 of 355 and 0 of 408 acts, so the trail could say a control was clicked but not what question the click asked. NO BROWSER (layer rule): a page stub works because every `page.evaluate` on both paths is catch-wrapped, so a stub that refuses to evaluate degrades exactly as a dead page would. FAIL-ON-REVERT (four levers, each independently verified): `'act.failed'` → `'act'` in recordFail → "readActFailed must return the stateful driver's failures" reds; drop the `beforeFrame` capture (or the `shots` field) → "a failed act must carry the rect the before-frame already captured" reds; `requested:` → `route:` → "the AIMED route is `requested`" reds; drop `probeKind` from the success payload → "a successful act names the obligation it discharged" reds — tests/unit/act-failed-parity.test.mjs
- (unit) THE RUN-PIPELINE MODEL — where the wall clock went, which an act-only view cannot show: `deriveSteps` answers "which acts have frames" and drops every `route` event (663 of 1018 and 538 of 946 events on two audited runs; 68.6% and 74.9% of elapsed time, 89.9% on `probe10`), so `derivePipeline` is a SIBLING rather than a change to it. Guards: route + act + act.failed + UNKNOWN kinds all become rows (an unrecognized kind is labelled with its raw kind — dropping one is how 70% of a run went missing the first time); `durMs` is the gap BEFORE an event and the first row INVENTS nothing; Σ durMs is EXACTLY `last.ts - first.ts` (conservation — no time silently unattributed); `idleMs` is the honest "nobody explained this" bucket and is never clamped away; a route's own `totalMs` beats the sum of its parts; a declared > measured payload surfaces as `overDeclared` rather than a negative or silently-zeroed idle; `requested` reports only a DIVERGENCE from the landed route; and a malformed trail degrades (non-array → [], missing ts → 0, never NaN) instead of throwing. FAIL-ON-REVERT: filter derivePipeline to `kind === 'act'` (the deriveSteps behaviour) → "every event that consumes wall clock is a row" reds with 2 instead of 5; give the first row a non-zero durMs, or clamp idleMs to >= 0 without the overDeclared flag → the conservation and overDeclared assertions red — tests/unit/pipeline-model.test.mjs
- (unit) THE ADMIN SCRUBBER'S GEOMETRY + STEP DERIVATION, shared source with the viewer (admin.html imports the same module, so the page and the test cannot drift): `indexFromClientX` round-trips `dotLeftPct` and clamps at both ends (a click past the right edge lands on the LAST dot); `boxFromRect` scales the rect highlight from viewport-CSS-px to rendered px DPR-independently; `deriveSteps` yields one step per act in seq order, pairs the observe verdict by templateId, and flags a route change. AND (the regression guard) `deriveSteps` KEEPS `kind:'act.failed'`: the node loop records a failure as `kind:'act'`+`error`, but the live driver records `act.failed` so `readActFailed` can see its granular codes — and the viewer's "Прогоны" walk RENDERS failures deliberately (`outcomeOf` reads `error`; there is blocked/failed styling), so an `act`-only filter makes 15% of a run vanish from the walk (54 failed acts of 355 in `hygge2`, 56 of 408 in `goal1`). The failure payload also uses DIFFERENT field names — `requested` (aimed) not `route` (landed), and on the whats-new path only `message` — so both fall back, else the row lands on a phantom "—" page and renders as a blank SUCCESS, which is worse than not rendering at all. FAIL-ON-REVERT (four levers, each independently verified): `indexFromClientX`'s `count-1` → `count` (the classic off-by-one) → the right-edge assertion reds; drop the observe pairing → "observe folded onto its act" reds; narrow the filter to `e.kind === 'act'` → "a failed act is a step: an act-only filter loses 15% of the walk" reds with 2 of 3; drop the `p.requested` / `p.message` fallbacks → "a failed act stamps `requested`" / "message backs `error` so the row is not a blank success" red — tests/unit/scrub-math.test.mjs
- (unit) THE REFUSED HOST — a decision logged without its evidence: run `raw1` (108 acts) refused 22 reveal paths reading `fired a firewall-refused request POST /api/addview (off-origin)`, and nothing in the run could say WHICH host that was — every request record carried a path-shaped `urlPattern` only, so aggregating all 18 captured requests yielded exactly one bucket, "path with no host", and a genuine cross-subdomain call was indistinguishable from a firewall misfire on the app's own origin. The ledger knew the answer and threw it away: CDP reports an absolute url (the fire ring holds whatever relative string `fetch` got), and `onRequest` kept only `pathKey(url)`. Now the ledger records `origin` + `startedAt` (WALL-CLOCK epoch ms from CDP `wallTime`, joinable against the trail's own `ts` — deliberately NOT the monotonic `timestamp` that derives durations), `endCause` joins both onto the frozen kept-set in PASS 2 (additive; `selectKept` untouched), and the firewall records BOTH origins its verdict is computed from so `describeBlock` can name the origin refused AND the page origin it was judged against. ORIGIN, not the full url: security review [L1] forbids persisting a raw url (secret-in-query) and an origin carries no query, while the query KEYS already survive masked in `urlPattern` — so the diagnostic is recovered without reverting the redaction fix. `urlPattern` is ADDED to, never replaced: a 50-row listing still folds to one pattern, and the origin is never smuggled into the census key. FAIL-ON-REVERT (three levers, each independently verified): drop `origin: originOf(url)` from the ledger entry → "the captured request record must name the HOST" reds; return the monotonic `startTs` as `startedAt` → "must carry a wall-clock timestamp" + "the monotonic domain time is never passed off as a timestamp" red; revert `describeBlock` to the bare `(${b.reason})` → "the refusal message must name the REFUSED ORIGIN" reds — tests/unit/request-origin.test.mjs
- DOM SKELETON on the FAILURE path (the schematic stand-in for a key-frame): measured on run raw1, screenshots exist on 140 of 141 SUCCESSFUL acts and 0 of 146 FAILURES — structural, since every pre-click gate in step.mjs throws before `capture.before` and the reveal failures throw from `applyReveal` before the capture collaborator exists — so `lib/graph/dom-skeleton.mjs` is built to be called FROM A CATCH BLOCK. The skeleton's `vis` verdict AGREES with dom-snapshot's `visible` element-for-element across every branch two restated predicates could fork on (display:none / visibility:hidden / opacity:0-is-VISIBLE / zero-AREA / ancestor-inherited) — the skeleton cannot import that closure, so this test is the ONLY thing holding the two definitions together; an over-cap page emits EXACTLY the cap with the remainder COUNTED in `truncated` (never a silent drop) and the score-ranked cap keeps the page's control over 3000 filler divs; and the IDENTITY GATE holds — capturing skeletons (including across a SCROLL that provably moves every rect, asserted non-vacuously) leaves mergeSnapshot's output BYTE-IDENTICAL, the ledger unmoved and diffIdentity ok, because rects in the identity path are the 148-phantom-template failure mode (decisions.md INC.4) one level over. FAIL-ON-REVERT (all verified): `r.width > 0 && r.height > 0` → `>= 0` reds "Zero area"; adding an `opacity === '0'` clause reds "Opacity zero"; `kept = seen` (drop the cap slice) reds "emits exactly the cap"; hard-code `truncated: 0` reds "the drop is COUNTED, never silent"; delete captureSkeleton's catch → a closed page rejects → "returns null, never throws" reds; fold a skeleton rect into `templateSelector` before the merge → ledgers diverge + diffIdentity churns → the byte-identical assertion reds — tests/live/dom-skeleton.test.mjs
- (unit) the DOM-skeleton artifact's WRITE + SERVE halves: `trace.writeSkeleton` returns a `skel/<stem>.json` ref and SANITIZES the stem (a caller-interpolated `../shots/pwned` — the shape that escapes into an EXISTING sibling dir and so actually succeeds unsanitized — stays inside the run's own skel/), is BEST-EFFORT (a null skeleton, the module's degraded catch-path return, writes nothing and returns null rather than throwing); admin-server serves it through the SAME fence as shots/ (access token, loopback Host, `safeArtifact` REUSED UNCHANGED). NOTE which traversal spelling proves what: raw `..` is collapsed by WHATWG URL normalization and percent-encoded `..%2f` fails the SKEL filename regex — BOTH stay refused with `safeArtifact` deleted, so on their own they would make the containment guard vacuous; the case that actually exercises it is a SYMLINK planted in skel/ resolving outside the run dir (well-formed name, passes the regex, caught only by the realpath half). FAIL-ON-REVERT (all verified): drop the stem sanitizer → `writeSkeleton` returns null for a hostile stem → "a hostile stem must still produce a skel/-relative ref" reds; delete the `seg[3] === 'skel'` branch → "the skeleton is served" reds; swap `safeArtifact` for a raw `path.join` → the planted symlink is served 200 with foreign bytes → "the resolved-path containment guard is load-bearing" reds — tests/unit/skeleton-serve.test.mjs
- (unit) RECORD THE ATTEMPT, NOT ONLY THE OUTCOME — the evidence a decision was made on, destroyed at the moment it became interesting. Measured on run `raw1` (287 acts, 146 failures): ZERO `act.failed` events, so `readActFailed` returned [] and `report --unreached` bucketed the whole run as `other:109` instead of splitting it; ZERO failures carrying `code`; `shots` HARDCODED null though actStep captures the before-frame under `__idle__` before every failure path; and `via` absent on 141 of 141 SUCCESSFUL acts, so the trail could not say how ANY element was located. `stateful-step.mjs` documents this exact defect class, fixed it in that file, and the fix was never propagated to `recon-run.persistentStep` — the driver producing the DEFAULT (stateless) crawl, which wrote 8 fields where its sibling writes 16. Separately `resolveHandle` returned a bare `null` after running six strategies, so "getByRole found ZERO" (a coverage gap) and "getByRole found THREE and the structural guard rejected all three" (a resolver bug) were one undifferentiated bucket — opposite diagnoses. Guards: the resolver's attempt record (`{strategy, ran, raw, visible, sameTemplate}` per strategy, with SKIPPED distinguished from ran-and-missed, and `sameTemplate:null` where the guard does not apply so a fabricated 0 never reads as a rejection that did not happen); the legacy `resolveHandle` null-on-failure contract (stateful-loop.mjs tests it for BARE TRUTHINESS at :90 and :389, so an always-object failure shape would report every unreachable control as reachable and invert the retire/churn logic); a node-loop failure readable by `readActFailed` with its granular code, `requested` (aimed) never `route` (landed), plus timings + instanceSelector; a successful node-loop act naming `via`/`representative`; and a NO_INSTANCE envelope carrying structured evidence through `envelope.target` — including `hadRevealPath`, which separates "we never knew how to reach it" from "the recorded path BROKE" under one shared code. `envelope.target` is validated object-or-null so evidence can never silently degrade back into prose. NO BROWSER (layer rule): waitSettled/dismissOverlays/harvestRoutes/harvestLinks all swallow a refusing `page.evaluate`, so a stub degrades exactly as a dead page would; only `handle.evaluate` is answered, which is the seam reaching a real success with no causal window. FAIL-ON-REVERT (four levers, each independently verified): `blankAttempts()` → `[]` (shape kept, evidence discarded — precisely the defect) → "the resolver must report WHAT IT TRIED" reds with 4 of 5; `traceEvent(runId,'act.failed',…)` → `'act'` in recon-run's recordFail → "readActFailed must return the NODE loop's failures" reds; drop `via: res.via || null` from the success payload → "a successful act must name HOW the element was found" reds; drop `target: evidence` from the NO_INSTANCE throw → "a NO_INSTANCE envelope must carry structured evidence" reds — tests/unit/attempt-record.test.mjs
- (unit) A CONTROL THAT ALREADY ANSWERED MUST LEAVE THE FRONTIER, and the incremental form ladder must not be collateral damage. Measured on two completed runs on disk (`state/runs/raw3`, `state/runs/hunt1`): 30% and 34% of ALL acts were repeats on a control that had already given its answer — "Enter your email id" acted 8 times and DISABLED all 8, "Group Name" the same, template 1290 `fill-valid`→NO_INSTANCE 8 times, template 1143 ACT_FAILED 5 times, template 27 `navigate` 8 times. Three DIFFERENT generators, and a terminal-code list sees only the first: the second is a code that is transient BY PRINCIPLE and proved terminal in practice, the third carries no block code at all. Root cause of the 8s: `recordProbe` files a blocked row under the kind the act HAPPENED to be (`click`) while a field's battery owes `fill-valid`/`fill-overflow`, so `blocked` never intersected `owed`, `outstanding` never emptied, and `batteryOwing` re-emitted the element to MAX_BATTERY_ROWS — OVERRIDING the 3-attempt bound `retryable` already enforced. Guards: an element-scoped refusal (DISABLED / DANGER_FLOOR / OUTWARD_REFUSED / FOREIGN_DESTROY / ACCOUNT_PROTECTED — each decided from the element's own live state or from policy over its identity, never from whether we reached it) discharges EVERY owed kind with its reason attached; a repeated identical (kind, outcome) signature retires the control at the rung `levelOf` itself calls reproduced (2 for an answered verdict, 3 for a transient block so the established retry budget is NOT tightened); the retired control stays counted in `walkable` and is DISCLOSED as `answeredNotExplorable` rather than vanishing; and DISABLED is recorded as a `control-not-operable` FINDING (docs/GOAL.md — the app rendered an affordance it will not honour) while a POLICY refusal is not, since our own choice not to fire is coverage accounting and reporting it would bury real findings. THE DISCRIMINATOR IS PROGRESS, NOT THE CODE: the probe KIND is part of the answer signature precisely so a form's `submit-empty`/`submit-req-1`/`submit-req-2` rungs — different questions that legitimately share one verdict — never collapse into a repeat. NOT_FILLABLE is deliberately NOT element-scoped: knowledge.test.mjs holds the counter-example (a field whose `fill-valid` WROTE and whose `fill-overflow` returned NOT_FILLABLE), so the code is about one probe, not the element. FAIL-ON-REVERT (four levers, each independently verified): delete `terminallyAnswered` from `instanceDrained` → "the same answer 3 times running retires the control" + "navigated identically twice ... must retire" red; drop the probe kind from `answerSig` → the ladder is retired mid-climb, "a form mid-ladder must still be emitted — it still owes submit-req-2" reds (THE critical guard); drop `answeredNotExplorable++` → "must be disclosed as answered-not-explorable" reds; drop the `elemBlock` expansion in `probeStatus` → "a DISABLED element must not keep owing fill-valid/fill-overflow it can never be asked" reds — tests/unit/terminal-answer.test.mjs
- (unit+live) STATE IS NOT A DECLARATION — a transient UI state recorded as a permanent verdict: `mergeSnapshot` merged `fieldFacts` write-once ("an existing non-null fact is never overwritten"), which is right for `maxLength`/`pattern`/`required`/`type` and wrong for `disabled`, a live IDL property the app flips as the user works (a wizard field disabled until the previous step, a Save disabled until dirty). The first sighting became permanent: measured on runs raw3 + hunt1, "Enter your email id" (tpl 1141) and "Group Name" (tpl 1020, a field INSIDE a create-group modal) each latched `disabled:true` with 8 `click:DISABLED` probe rows in both runs' final graphs — 8 DISABLED is the second-largest outcome class of raw3. It cost twice: the control is unstudiable forever, and docs/GOAL.md's finding ("a control disabled where a working one is expected") is conflated with "disabled because the wizard has not reached this step", which is the distinction that MAKES it a finding. `FIELD_STATE_KEYS = {disabled, readOnly}` re-read on every snapshot, everything else untouched; the replaced reading survives in an additive `node.fieldStateHistory[key] = {values,flips,truncated}` (consecutive duplicates collapsed, capped at 8 with the truncation DISCLOSED, current value always in `fieldFacts`), seeded from a carried graph's latched fact so the transition is not lost at the upgrade; `fieldStateCleared(node,key)` is the disabled→enabled predicate the retirement counterpart reads. READONLY IS ON THE STATE SIDE deliberately — it looks like a declaration and this repo leans on it as one ("Event Type is a readonly select"), but that permanence belongs to the AntD widget, not the attribute: `el.readOnly` is toggled by ordinary code exactly like `el.disabled`, an always-readonly widget simply re-reads true forever, and the asymmetry decides it (a stale `true` deletes coverage permanently, a fresh `false` costs one honest NOT_FILLABLE probe). `required` stays write-once for a STRONGER reason than the others: our reading is a three-way OR including AntD's `.ant-form-item-required` wrapper class, which an early snapshot can miss, so re-reading would UNSET it and silently delete the `fill-empty` obligation. The dom-snapshot half is load-bearing and easy to miss — `disabled: el.disabled || null` collapsed `false` to `null` and the merge skips null, so "enabled" was UNREPRESENTABLE and the graph-store fix alone is inert; guarded LIVE since it runs in the page. WHICH ASSERTION CATCHES AN IDENTITY LEAK, recorded because the obvious one does not: a fact leaking into identity mints a NEW ledger key and the ledger is APPEND-ONLY, so nothing is re-mapped or dropped and `diffIdentity` reports ok:true calling it an ADDED template — the leak is caught by the COUNTS (one template, one instance after the flip, asserted FIRST so the red names the phantom rather than a downstream symptom) and by the single append-only ledger's id map being unchanged; identity-diff is still asserted for the re-key/drop direction it is built for. FAIL-ON-REVERT (six levers, each independently verified): drop the `FIELD_STATE_KEYS.has(k)` branch in mergeSnapshot → "a state key must be RE-READ, not latched" reds; drop the `prevValue` seed in recordFieldState → "the carried reading seeds the history" reds; add `maxLength` to FIELD_STATE_KEYS → "a declaration is write-once, not last-wins" reds; add `required` to FIELD_STATE_KEYS → "required survives an explicit later reading of false" reds (NOTE: the `required:null` case does NOT red under this lever — null is skipped before the state branch either way, so the explicit-`false` assertion is the real guard and the null one only guards the skip); fold `fieldFacts.disabled` into idify's template key → "the flip minted NO phantom template" reds; restore `disabled: el.disabled || null` in dom-snapshot → the enabled reading is null → the latched true stands → "must be re-read as ENABLED" reds — tests/unit/field-state.test.mjs + tests/live/field-state.test.mjs (fixture disabled-app: a Group Name field disabled until a terms checkbox is ticked, carrying a real `maxlength=50` in BOTH states so one page exercises both sides of the merge). COUNTERPART NOT MADE (knowledge.mjs was off limits) — and it is TWO rules, not one, which is the part that is easy to get wrong: `answeredTerminally` retires a flipped control by BOTH of its arms. (1) `elementBlockedBy` treats a `DISABLED` row as an answer ABOUT the element ("re-asking cannot change it" — true of a policy refusal, false of a state), and `probeStatus` uses the same call to discharge every owed kind. (2) even with that skipped, the no-progress rule retires on a tail of 3 identical `click:DISABLED` signatures, which a carried graph already holds 8 of. So the fix must make BOTH arms disregard pre-flip `DISABLED` evidence when `fieldStateCleared(node)` is true — e.g. `answeredTerminally` returns null when the only terminal/no-progress evidence is `*:DISABLED` and the state has since cleared, and `elementBlockedBy` gains the node so `probeStatus` stops discharging the battery. Self-limiting: the control is re-emitted, and if it answers DISABLED again the no-progress rule fires on the NEW tail and retires it after 3. Studiability then follows without touching the drain flags — `batteryOwing` re-fills `outstanding`, so `instanceDrained`'s `drained && !retryable && !batteryOwing` no longer holds. Deliberately NOT done in graph-store: clearing `explored`/`unreachable` from mergeSnapshot would leave the control counted open by `frontierStats` while `terminallyAnswered` still refused to emit it — the phantom-stall failure mode frontier.mjs warns about, where remaining never reaches 0.
- (unit) THE COUNTERPART IS MADE — a control disabled once is asked again after it flips, and it is TWO rules, not one. `mergeSnapshot` now re-reads `disabled`/`readOnly`, so `fieldStateCleared(node)` answers "was it disabled, is it enabled NOW"; `knowledge.mjs` acts on it at BOTH arms, because either alone leaves the control retired and reads green. ARM 1 — `liveRows(node, probes)` drops pre-flip `DISABLED` rows for `probeStatus`, so the element-scoped discharge stops emptying a battery with a code the element no longer returns and `outstanding` refills (restoration comes through `batteryOwing`, NOT by clearing `explored`/`unreachable` — that is the phantom-stall mode where the control is counted open by `frontierStats` while `terminallyAnswered` refuses to emit it). ARM 2 — `answeredTerminally` returns null when the identical tail it would retire on IS `*:DISABLED` and the state has cleared; raw3 and hunt1 each hold EIGHT such rows per control. THE TAIL IS CHECKED IN PLACE, NOT REBUILT, and that is the load-bearing subtlety: filtering the rows before taking the tail lets `[answer, DISABLED, answer]` collapse into a repeat that never happened, so a rule meant to STOP a retirement could CAUSE one — the ladder test interleaves exactly that shape. SELF-LIMITING: a control that answers DISABLED again re-reads `true`, `fieldStateCleared` goes false, every row counts and the no-progress rule retires it on the NEW tail; scoped to `DISABLED` alone, since DANGER_FLOOR/OUTWARD_REFUSED/FOREIGN_DESTROY/ACCOUNT_PROTECTED are verdicts over IDENTITY that no snapshot can clear. Reporting follows the same fact: a flipped control is L1 REACHED not L-1 BLOCKED (a fixable gap must not be presented as a permanent ceiling), and it is no longer a `control-not-operable` finding — it was disabled because a precondition was unmet, which is the distinction that MAKES the remaining cases findings. FAIL-ON-REVERT (both verified, each red on its own arm): restore `const rows = (probes||[]).filter(Boolean)` in `probeStatus` + drop the `node` from `elementBlockedBy(rows, node)` → "ARM 1: a control that is enabled again must owe its battery" reds; delete the `fieldStateCleared(node) && tail[0].blocked === 'DISABLED'` guard → "ARM 2: a tail of DISABLED answers is pre-flip evidence" reds; EITHER lever also reds "a re-enabled control is emitted by the frontier again" + the denominator assertion, which is what proves the arms COMPOSE — tests/unit/disabled-flip.test.mjs
- (unit) THE ADMIN WALK STOPPED LYING ABOUT OUR OWN CRAWLER (ADMIN-TRUTH-PLAN Stages 0-1) — every text-producing function extracted out of the 1277-line `admin.html` into `walk-view.mjs` as PURE `(step, graph, run) → string`, because the six audited false claims on run `fix1` were each an inconsistency BETWEEN the writer, the projection (`scrub-math.deriveSteps`) and the renderer, and while the renderer sat inline no `node:test` could execute the three together. Guards, each asserting the exact string/number against fix1-shaped payloads: (1) the KPI HEADLINE is the INSTANCE number `148/693`=21.4% with the owner split (walked / genuinely-owed 141 / policy-declined 373 / churn / unreachable), NOT the TEMPLATE count `118/295` the old caption inflated — the numbers come from `frontier.frontierInstanceStats` (computed server-side, the viewer re-derives no cap/sample rule) and the partition sums to the denominator with ZERO residue (a non-zero residual renders a loud «Разбиение не сходится», never hides); (2) the failure card renders the SIX per-strategy resolver `attempts` — `deriveSteps` must carry `target`, the field it dropped while sibling `derivePipeline` carried it (dropping it → «список попыток не записан» on 39/39 failures + the walk/pipeline tabs contradict each other on `hadRevealPath`); (3) a request row carries its response `status`/`durationMs` (fix1's 5×500 + 1×422), the anomaly framed as Phase-2 DATA never our defect; (4) the blank stage is explained at RUN level (`BUGHUNTER_VIEW=1` unset → 0 of 200 frames) not blamed on the step, and the DOM skeleton is the failed-act stage; (5) `verdictOf` respects the four `failure-hints` tones — a DISABLED FINDING and an OUTWARD_REFUSED POLICY refusal are NEVER labelled «недостижим», and the count is the instance-level unreachable total not the failed-act count. FAIL-ON-REVERT (four levers, each verified red by hand): drop `target` from `deriveSteps` → the six-attempts test reds naming the count; point `kpiHtml` back at `run.stats` template counts → the instance-headline test reds; restore the unconditional «Отмечен недостижимым…: N» on every class → the DISABLED-not-unreachable test reds; drop `status` from `requestRowsHtml` → the request-status test reds — tests/unit/walk-view.test.mjs; and the `walk-view.mjs` fixed-basename allowlist branch (a missing branch 404s the module under `script-src 'self'` and the walk mounts nothing) — tests/unit/admin-server.test.mjs
- (unit) A REOPEN IS WORTH WHAT THE ACT AFTER IT DELIVERS — telemetry that recorded the intention instead of the outcome. `recoverGated` called `relocMemo.record(…, re.ok)`: the RELOCATION's verdict, decided before the act it exists to enable had run. MEASURED, run state/runs/hunt1 seq 491-521: EIGHT consecutive `reopen{ok:true, code:REOPEN_OK, hops:1, rung:"in-place"}` on template 1290 instance #1, each immediately followed by `act.failed NO_INSTANCE` — the container really did reopen and the control really was not in it — while the run's `reloc-census` printed `{attempted:12, succeeded:10}`, an 83% success rate for a mechanism that had just delivered nothing eight times running. Same class as the trail that recorded an act's INTENDED target rather than the element actually clicked and hid a wrong-control bug for seven runs. The memo is now TWO-PHASE: `record` opens a PENDING entry and returns an idempotent `settle` the caller closes with the act's outcome, so a crash between the phases leaves the entry disclosed as `pending` rather than defaulting to success (an unresolved reading beats a flattering one), and a reopen whose act failed is memoized as a FAILURE — deterministic in the same sense as a path that will not replay, and the reason hunt1 bought the same empty container eight times. `stats()` gains `pending` + `deliveredNothing` (additive; `attempted/succeeded/refusedRepeat/distinctFailed` unchanged for the `reloc-census` event and `loop.reloc`), and a `reopen-delivered` event carries the half the pre-act `reopen` event structurally cannot. Guarded at BOTH layers, because a memo with the right rules and a caller that never settles is exactly as wrong as before: the semantics in tests/unit/relocation-memo.test.mjs, the WIRING by driving `statefulLoop`'s recovery pass against a stub page whose hop click really does open the panel (NO BROWSER — `waitSettled` returns on its own evaluate, `settleAnimations` catches, `resetTrackerVerdicts` is a Map lookup, `inOwnableItem` reads the handle). Both directions: a reopen whose act SUCCEEDS still counts, or the fix would just call every relocation a failure and strand containers that legitimately need re-entering. FAIL-ON-REVERT (verified): restore the single-phase `record` (count `succeeded` at phase 1, discard the return) → "the same empty container must not be re-bought" reds with the target acted 12 times (the REOPEN_BUDGET) and "a reopen whose act failed is not a success" reds with succeeded:12 — tests/unit/reopen-delivers.test.mjs + tests/unit/relocation-memo.test.mjs
- (unit) THE «Покрытие» SCREEN ANSWERS "почему не нажал остальное" BY OWNER, not as data to interpret (ADMIN-TRUTH-PLAN Stage 5) — pure `(instanceStats, instanceBuckets) → string` in `coverage-view.mjs`, executed against the real fix1 graph (`tests/fixtures/trail-golden/graph.json`: 693 instances, 148 walked). Guards: (1) the partition walked + genuinely-owed 141 + policy-declined 373 + churn 13 + unreachable 18 sums to 693 with ZERO residue, and a non-zero residual RENDERS a loud «Разбиение не сходится», never hides; (2) "we declined on purpose" (the four policy buckets under ONE 373 subtotal) is kept UNCONFUSABLE from "we could not" (unreachable 18) and from churn (13) by STRUCTURE — physically separate `data-owner` sections each with its own `data-subtotal`, never merged, never distinguished by colour alone; (3) every owner (наше правило / поломка / перерисовка страницы / реально должны) and every policy bucket carries its OPERATOR-facing label (siteRemainder→«лимит представителей на страницу», drillSkipped→«прорежённые строки списка», widgetSkipped→«внутренности виджета», cappedRemainder→«сверх лимита открывашки» — each verified against what frontier.mjs actually counts, since the code's own bucket names lie); (4) the drill-down `frontier.frontierInstanceBuckets` (the ONE read-only export added for this screen — bucket membership IS the sampling rule, so it lives beside the rule, never re-derived in the viewer) attributes each instance to exactly one bucket, so its per-bucket counts sum to the `frontierInstanceStats` totals. FAIL-ON-REVERT (four levers, each verified red): drop a term from `coveragePartition.summed` → "zero residue" reds; set `declinedSubtotal: declined + unreachable` → the policy section renders 391 → "policy subtotal is 373, not merged" reds; remove an owner label → "every owner is labelled" reds; drop a bucket push from `frontierInstanceBuckets` → "the drill-down partitions the same total" reds — tests/unit/coverage-screen.test.mjs; and the admin-server serves `coverage-view.mjs` (fixed-basename allowlist branch) + ships `instanceBuckets` beside `instanceStats` on `/api/runs/:id` (a dropped branch 404s the screen module, a dropped field strips the drill-down) — tests/unit/admin-server.test.mjs
- (unit) THE OWNERSHIP MARKER NEVER MALFORMS A SHAPED FIELD — under explore-all `step.stampOwnership` appends an invisible zero-width mark to every self-fill; appended to a value the browser parses BY TYPE (`"0"` into `input[type=range]`, a date into `type=date`) the result is no longer valid for that type, so `handle.fill` throws "Malformed value" and the probe fails on a purely cosmetic marker (measured: every malformed-fill in the trails was a shaped field carrying the mark). `stampOwnership` now skips `isShapedType(factsKind)` — a text/textarea value is still marked (hunt-gate reads it back), a shaped value is left bare (ownership of a shaped field is proven through its record, not its scalar). FAIL-ON-REVERT: delete `if (isShapedType(factsKind)) return field;` → "a shaped field is left UNMARKED" reds (the value gains the mark) — tests/unit/ownership-shape.test.mjs
- A SHAPE FIELD'S fill-valid PROBE DRAINS UNDER EXPLORE-ALL — the live half of the marker/shaped fix: on a `input[type=range]` crawled with explore-all armed (marker active), the fill-valid probe ("0") LANDS instead of throwing "Malformed value", so the obligation drains off `probeStatus(#vol).outstanding` instead of staying owed forever behind a transient ACT_FAILED (fill-valid is not the wrong-shape probe, so stateful-step never records it NOT_FILLABLE; the malformed throw falls through to the transient ACT_FAILED that discharges nothing). Guards non-vacuity (the range owes fill-valid + a row was recorded), the landing (no ACT_FAILED, self-filled), and the drain. FAIL-ON-REVERT: delete the shaped-skip in step.stampOwnership → the marker malforms "0" → the fill-valid row records ACT_FAILED → "must NOT record the transient ACT_FAILED a malformed marker would cause" reds — tests/live/marker-shape-fill.test.mjs (fixture marker-range-app)
- A TRANSIENT `-dragged`/`-dragging` STATE CLASS DOES NOT FRAGMENT IDENTITY (the ALIAS_COLLISION denominator fix, INC.6 class one library over): react-draggable adds `react-draggable-dragged` to its wrapper after a drag, so a positional (id-less) button THROUGH that wrapper had two template selectors — `div.react-draggable > button` and `div.react-draggable.react-draggable-dragged > button` — and fragmented into two templates for one control (measured live as ALIAS_COLLISION, tpl 1066 colliding onto tpl 41). `dom-snapshot.isStateClass` now strips the `-dragged`/`-dragging` suffix (the structural `react-draggable` base class is KEPT as the anchor); SCHEMA_VERSION bumped 7→8 so a graph carrying the fragmented keys is reset, not co-mingled. The fixture button carries NO id on purpose (an id would anchor the selector and hide the bug). FAIL-ON-REVERT: drop `|dragged|dragging` from the isStateClass suffix regex → the dragged wrapper mints a second template selector → "the draggable control is ONE template across the drag flip (got 2)" reds (verified) — tests/live/draggable-state-class.test.mjs (fixture draggable-app)
- ANTD WIDGET DURABLE LOCATOR (CLASS 2 — the biggest real-coverage NO_INSTANCE fix, run-fix-designer plan): an antd Select's inner <input> is bare (no testid, no stable id, empty role/name), so its recorded locator is the POSITIONAL css path and it went NO_INSTANCE the instant antd re-mounted the widget's internals. `dom-snapshot` now classifies a durable `type:'widget'` locator (the `.ant-select-selector`/`.ant-picker` clickable affordance scoped by the form-item LABEL text — derived from fieldFacts, ADDITIVE, never an identity input), and `resolve-handle` resolves it at act time. Resolving to the clickable also clears the CLASS 1a click-interception (the display span over the inner input) and is exactly what actuateSelect/actuatePicker click to open — one durable locator, two failure classes. Guards: durable classification at baseline (type 'widget'); durable reach after a re-mount makes the stored positional selector STALE (page.$ === null, non-vacuous); attribution (GET /api/options bound to the select); causal cleanliness (the in-window /api/poll never credited). FAIL-ON-REVERT: delete the `loc.type === 'widget'` branch in resolve-handle.mjs → the bare input has no durable handle → NO_INSTANCE → the select is unreachable → "genuine coverage, NOT unreachable" reds (verified) — tests/live/antd-widget-locator.test.mjs (fixture antd-select-app)
- (L1) THE REVEAL-REPLAY OUTCOME IS CARRIED INLINE ON A RECOVERY act.failed — the fix1/dashproof audit found the 22 reveal-required NO_INSTANCE/ALIAS failures were the one class the trail could not explain from the failure row alone: a reader could not tell "the container never opened" from "it opened and the control STILL would not act", because the reveal-replay diagnosis lived only in separate `reopen`/`reopen-delivered` events `readActFailed` never joined. `stateful-loop.recoverGated` now stamps `target.revealReplay = {replayed:true, ok:true, code, rung, hops}` before its recovery act, `stateful-step.recordFail` emits it on the act.failed (default `{replayed:false}` for the live-state main pass), `scrub-math.deriveSteps` carries it, and `walk-view.failurePanel` renders "контейнер переоткрыли … акт всё равно не прошёл" for the informative `replayed:true` case only. LIVE guard: a hand-built graph + fixture where the container reopens successfully (REOPEN_OK, the target resolves) but the recovery act's click is intercepted → the recovery act.failed carries `revealReplay.replayed:true, ok:true`. FAIL-ON-REVERT: delete the `target.revealReplay = {...}` stamp in recoverGated → the failure falls back to `{replayed:false}` → "carries replayed:true" reds (verified) — tests/live/reveal-replay-outcome.test.mjs (fixture reopen-act-fail-app). UNIT guard (the projection + render): deriveSteps carries `revealReplay` and failurePanel renders the reopened-but-failed line only for replayed:true, nothing for a main-pass failure. FAIL-ON-REVERT: drop `revealReplay` from deriveSteps OR the `rr` block in failurePanel → "the reopened-but-failed line is rendered" reds (verified) — tests/unit/walk-view.test.mjs
- (L2) THE DEFAULT report HANDS THE OPERATOR THE FINDINGS SUMMARY — the capability (findings.httpAnomalies elevating a 4xx/5xx to a finding keyed to its causing control) existed only behind `report --findings`, so a `/recon` run's own conclusion never surfaced the repeatable 500 the fix1 audit measured (docs/GOAL.md: an anomalous response is the most valuable thing a crawl can find). The default `report()` now appends `renderFindings(graph)` to the text output and `rep.findings` to the JSON. Guard: a graph with a control causing POST /app/save→500 makes the DEFAULT report (no --findings) carry the server-error finding line naming the control, and the JSON default carry the findings product. FAIL-ON-REVERT: drop the `renderFindings` append + `rep.findings` assignment in report.mjs's default return → the default output has no server-error line / no findings → reds (verified) — tests/unit/report.test.mjs
- (CLASS 1b) A CONTROL AN UNRELATED ACT LEFT OBSCURED IS SURFACED AS A FINDING — a prior act's modal covering a base-page control is recovered by closeAppModal (reach restored), but recording the obstruction only-on-failure would erase the finding the moment recovery works (the "interesting event vanished" trap). At the interception site stateful-step records an ADDITIVE `node.obstructions` entry (never a probe row → never touches the knowledge ladder / a block code, never identity), deduped by the obscuring modal; findings.obstructedControls surfaces it as `obstructed-control` (medium), naming the modal where known. Guard (e) on the modal-cover crawl: the obstruction of "Create Event" is surfaced as an obstructed-control finding. FAIL-ON-REVERT: neuter the `node.obstructions` record block at the interception site → no obstruction → the finding is absent → reds (verified) — tests/live/modal-cover.test.mjs
