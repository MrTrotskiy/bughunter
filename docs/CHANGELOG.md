# Changelog

## 2026-07-14
- Founded `bughunter` — ground-up rebuild of `bughunt-agents`. Recorded founding decisions in `decisions.md` and `docs/ARCHITECTURE.md`.
- Built the keystone vertical slice: `whats-new` (DOM-diff + causal-token capture) on a live local fixture. Ported hardened plumbing (`envelope`, `host-policy` SSRF gate, causal init-script). `npm test` green.
- Proved causal request attribution needs TWO mechanisms (in-page token + CDP initiator classifier), not either alone; added a regression test for the in-window background-poll race that the token alone leaks.
- Proved two-level element identity (template shared across rows + distinct per-instance keys).
- Adopted a test doctrine (tests/CLAUDE.md) from aeye-os: FAIL-ON-REVERT + Guards headers, unit/live layer split. Retrofitted the keystone test and added unit tests for the anti-vacuous mechanism (classifyInitiator), the SSRF gate, and url-pattern masking. 14 tests green; every guard revert-proven.
- Started Phase-1 loop (blocker #1). Built the pure core: `lib/recon/frontier.mjs` (template-level receptive-field selection + honest discovered/explored/remaining denominator) and `lib/recon/recon-loop.mjs` (loop-driver: nextBatch → act → markExplored → stop on empty-frontier/budget; browser step + LLM judge injected). Added `explored` tracking to the graph (`markExplored`). Unit-tested + revert-proven; 22 tests green. Live browser step + CLI still to wire (dovetails with the persistent-session task).
- Authored three read-only review subagents via the writing-subagents skill (`.claude/agents/{backend,frontend,bughunter}-reviewer.md`, all opus): generic backend + frontend reviewers grounded in the project's conventions, and a bughunter-specific reviewer that enforces the 9 founding invariants. Reviewed the Phase-1 + semantic-recon session code with the backend + bughunter reviewers and fixed every finding: the node loop marked cold-start-unreachable controls as `explored` (invariant #3 coverage overstatement) → now marks them `unreachable`, and `frontierStats` reports `{discovered, explored, unreachable, remaining}` with `explored` excluding unreachable; `reconLoop` starved the frontier under a filtering judge (now drains dismissed items), treated a NaN `--steps` as a false 'frontier-drained' success (now clamps non-finite to unbounded), and `actStep` stranded the causal cause token on an early throw (now resets it — protects the future persistent session, invariant #1); `observe --template` bare flag silently wrote template 1 and `--state-change=false` coerced to true (both now validated); the four CLIs align on the shared `exitCode()`; the graph/ledger now persist after each loop step (crash-resumable). 7 new FAIL-ON-REVERT guards, all revert-proven; 42 tests green.
- Built the semantic recon layer — the "LLM judge" (blocker #2). Chose agent-as-driver (design in `docs/draft/recon-agent-design.md`, by Fable): a fresh Sonnet subagent `.claude/agents/recon.md` drives thin CLIs — `lib/recon/frontier-cli.mjs` (emit the receptive-field batch, no browser), `whats-new` (act+capture, reused), `lib/recon/observe.mjs` (record purpose/danger/effect + markExplored; explored ⟺ observed). Added `node.semantics` + `recordSemantics` reducer, and `lib/recon/danger-floor.mjs` — a deterministic safety backstop `observe` enforces (refuses an acted observation on destructive/logout/payment controls). The proven node loop/frontier are untouched (deterministic identity-judge fallback stays). Tests: unit for danger-floor, recordSemantics, frontier-cli (size clamp + honest denominator), observe (drain + backstop); one live tool-chain test (baseline → emit → act → observe persists edge + semantics and drains). All 5 new guards revert-proven; 35 tests green.
- Finished Phase-1 loop wiring (blocker #1 done end-to-end). Extracted the causal act+capture into a shared `lib/recon/step.mjs` (`snapshotStep`/`actStep`) and refactored `whats-new` onto it (keystone live test stays green, no contract change). Added `lib/recon/recon-run.mjs` — the loop runner CLI driving a COLD-START browser step (fresh page per act; graph = cross-step memory). Added a live end-to-end test (`tests/live/recon-loop.test.mjs`): the loop drives a real browser, persists the causal `#search → GET /api/search` edge, leaves config/ping uncredited, discovers the revealed Edit template, and drains the frontier. Revert-proven (drop `addTrigger` → edge absent). 23 tests green. Known boundary: cold-start cannot reach controls behind in-app state (revealed Edit button) — surfaced as a step error, lifted by the persistent-session + State-node work.
- Phase 1 made RUNNABLE END-TO-END. Added `lib/recon/report.mjs` — renders `state/graph.json` into the honest coverage denominator (explored/discovered/unreachable/remaining), per-route controls with danger/effect/purpose, and the causal control→endpoint map (the Phase-2 input); `--json` for the structured object. Added `.claude/commands/recon.md` — the `/recon <url>` run orchestrator the main Claude executes: start the shared daemon → baseline → loop `frontier-cli --emit` + the `recon` subagent one receptive field at a time until the frontier drains (20-iteration cap) → always stop the daemon → print the report. Validated live: full orchestrated run on the search fixture drained the frontier in one agent pass and produced the report (2/3 explored, 1 unreachable-coldstart, causal `[Search] → GET /api/search` edge), zero orphaned processes. New revert-proven `report` unit guards (coverage + bidirectional causal map). Boundaries stated in the command: cold-start reach only (no stay-on-page), no login/auth, no destructive/fuzz; opaque regions counted but not yet persisted in the graph. 55 tests green.
- Three-reviewer pass (bughunter + backend + security) on the persistent-browser layer, all findings fixed. MUST FIX (backend, causal regression): the reused page accumulated STICKY CDP-initiator verdicts across acts, so a path an earlier act click-rooted could get a later act's same-path background poll mis-attributed to it — a phantom causal edge (the bughunt-agents failure class). Fixed with `resetTrackerVerdicts` (initiator.mjs `reset()` + causal.mjs) called per act in `persistentStep`; deterministically live-guarded (tests/live/cross-act.test.mjs — clicked-and-polled shared path + slow second request holds the window; revert-proven the phantom edge appears). Security (session.json trust boundary): `readSessionEndpoint` refuses non-loopback endpoints (no rogue-server redirect), `recon-session` only signals a pid whose command still matches what we started (`killOwned` + `ps`; no arbitrary-kill on crash+pid-reuse), `isAlive` rejects pid ≤ 0, session.json/daemon.log written 0600, session.json written atomically. Robustness: no browser leak on partial-wiring failure, `release()` swallows dead-connection errors, `stop()` escalates to SIGKILL, `start()` reaps on timeout + closes the log fd + accepts the daemon pid, `observe` forces acted=false on unreachable-coldstart, `whats-new` uses `exitCode()`. Accepted tradeoff (M2): one shared chromium process per run trades process-level isolation between successive hostile targets (contexts still isolated, sandbox on by default). Two new revert-proven safety unit guards (tests/unit/daemon-safety.test.mjs). 53 tests green.
- Ran the real `recon` Sonnet subagent end-to-end against a live fixture through the daemon (baseline → emit → judge → act → observe, looped to frontier-drain). It worked, and surfaced a coverage-accounting bug the automated tests missed: `observe` marked a cold-start-UNREACHABLE control (effect=unreachable-coldstart, acted=false) as `explored`, so the agent path reported 3/3 explored while the node-loop path flags the same never-reached control `unreachable` (2 explored, 1 unreachable). Fixed `observe` to `markUnreachable` on effect=unreachable-coldstart — honest-coverage parity between both front-ends (a deliberate danger-skip on a reachable control still counts as real coverage). New revert-proven unit guard in `observe.test.mjs`. 50 tests green.
- Persistent browser — one chromium per run, not one per act (resource fix; design by cto/Fable, code Opus). Node loop (`recon-run`) now holds ONE browser for the whole crawl (launch/close hoisted to `crawl`, `persistentStep` reuses one page, re-navigate per act) instead of launching per act. Agent path (each `whats-new` is a separate process) gets a browser DAEMON: `lib/recon/browser-daemon.mjs` (`chromium.launchServer`, publishes `state/session.json`) started/stopped/probed by `lib/recon/recon-session.mjs --start|--stop|--status` with a stale-session reaper; `lib/browser/session.mjs` grows `attach()` which `chromium.connect()`s to the daemon when up (fresh context+page+probe+tracker per act on the shared PROCESS) and cold-launches when absent (backward compatible). Empirically verified one launchServer survives N sequential `connect()` clients at a stable pid. Causal invariant untouched — attach wires the probe + initiator tracker BEFORE navigation, so the CDP classifier still rejects an in-window poll over the connection. New live guards (`tests/live/daemon.test.mjs`: attached-mode + one-process-serves-N; in-window poll rejected over a connected session) and a launch-count guard in `recon-loop.test.mjs`; all three revert-proven. `recon.md` doctrine gains an optional `recon-session --start`/`--stop` run bracket. Chose re-navigate-per-act over stay-on-page (keeps acts independent, causal walk single-sourced); stay-on-page (reaching in-app-state controls) deferred to a separate ADR. Rejected `connectOverCDP` (lower fidelity), daemon-owns-causal-walk, and lazy auto-start (orphan risk).
- Three-reviewer pass (bughunter + backend + security) on the overlay + locator changes, all findings fixed. MUST FIX (bughunter/backend, causal): `whats-new` clicked the consent-accept button before its act but — unlike `persistentStep` — never reset the initiator tracker, so the accept-click's sticky foreground verdict could let a same-path background poll in the act window forge a phantom edge (the bughunt-agents failure class); `whats-new` now `resetTrackerVerdicts` after a dismissal (mirrors the persistentStep fix already revert-guarded by cross-act.test.mjs). MEDIUM (security): `dismissOverlays`'s in-page `el.click()` could follow a framework-id `<a href>` off-origin (e.g. to a metadata host), bypassing the SSRF gate a browser link-follow evades — it now refuses to click an off-origin anchor. LOW (security): `safeLocatorValue` now also rejects C1 control bytes (U+007F–U+009F) for parity with the report sanitizer, so no control byte is persisted in a locator value. Robustness (backend): a `waitSettled` after a successful dismiss (the banner-removal re-render settles before the snapshot/act). Documented: `locator.unique` is a first-observation hint (not rewritten on later merges); the shared-test-id fixture rows dedup to one instance (the gate still sees both pre-merge). 66 tests green.
- LOCATOR preference — recon now records the most DURABLE handle for each control (the Phase-2 input, so a generated spec targets `[data-testid="save-btn"]`, not a brittle nth-child path; design by cto/Fable adapting the sibling bughunt-agents' `preferredSelector`/`isStableId`/uniqueness-gate, code Opus). `dom-snapshot` classifies a per-element `locator` down a ladder — authored test-id (a SEPARATE broadened `TESTID_ATTRS`: data-testid/data-test-id/data-test/data-cy/data-qa/data-automation-id/data-pw) > strictly-stable `#id` (`stableIdForLocator`, tighter than the path-anchor rule) > role+name > css instanceSelector — with a two-level UNIQUENESS GATE: a page-unique test-id is an instance discriminator (`unique:true`), a test-id shared across a template's rows is a MARKER (`unique:false`), one spanning multiple templates is unusable and falls through. The template node carries the locator KIND, each instance its concrete handle. CRITICAL constraint (Fable): locator is a DERIVED attribute — identity still keys on the selector string (`ids.mjs`), so `templateId`/`instanceId`/causal edges have ZERO churn (proven by the existing identity live tests staying green). The broadened `TESTID_ATTRS` feeds ONLY the classifier; `DATA_ATTRS` (which drives `instanceKey`/`instanceSelector`) is byte-unchanged. `report` surfaces the KIND (`loc=<type>`). Rejected (Fable): re-keying identity onto the test-id (the reference's approach) — collapses the two-level model and churns the append-only ledger + edges. New revert-proven live guard (`tests/live/locator.test.mjs`). 66 tests green.
- Cookie/consent OVERLAY dismissal (the next real-site blocker after multi-route — a consent wall intercepts pointer events, so every underlying control fails its click and an unauthenticated public site maps almost nothing). New `lib/recon/overlays.mjs` — `dismissOverlays(page)`: a curated high-precision consent-framework accept-selector list (OneTrust/Cookiebot/Osano/Didomi/Usercentrics/Quantcast/TrustArc/Axeptio) plus a conservative accept-TEXT fallback gated on a consent-scoped ancestor (id/class matching cookie|consent|gdpr|cmp|privacy), so it never dismisses a real control. Runs after every navigation BEFORE any causal window opens (the accept click's request fires under `__idle__` and can never forge a causal edge), best-effort and bounded, and every dismissal is logged. Wired into `recon-run` (baseline + per-act) and `whats-new` (surfaced as `result.overlayDismissed`). Reference-informed: the sibling bughunt-agents had NO cookie-specific dismissal (only a generic Escape→re-toggle→reload panel-reset ladder + a name-regex that refuses to click close/submit/destructive controls); the curated consent list is the improvement, and its false-positive discipline (consent-scoping) adapts their denylist idea. New revert-proven live guards (`tests/live/overlay.test.mjs`: overlay cleared → underlying control fires its request; a non-consent "OK" is left alone). 65 tests green. Deliberately NOT a generic Escape/click-away (would close exploration-worthy modals); the Escape→reload ladder for STAY-ON-PAGE panel reveals is deferred to that ADR.
- Three-reviewer pass (bughunter + backend + security) on the multi-route changes, all findings fixed. MUST FIX (backend): a `javascript:`/`mailto:`/`tel:` anchor has an opaque origin, so the naive off-origin test dropped it as an "external link never fired" — losing the common `javascript:void(0)` button idiom AND inflating coverage with a control never exercised; the skip is now gated on http(s) cross-origin only (`isOffOriginHttp` in `scope.mjs`). HIGH (security): a same-origin `//evil.com` path key reconstructed via `new URL(key, origin)` as a PROTOCOL-RELATIVE url (→ https://evil.com/) and `gotoGated` allows public hosts — escaping origin scope onto attacker infra; `routeKey` now collapses leading slashes to one and `persistentStep` refuses any reconstructed navUrl that is not same-origin (defense-in-depth). SHOULD FIX (backend): `fillTarget`'s `fill()` calls were unbounded (a hidden/disabled sibling input hung the 30s default timeout — the same stall the click gate kills) → both bounded to 5s; an off-origin JS-redirect landing now reports the in-scope start route, not the foreign pathname. LOW (security): `report`'s `oneLine` now strips C0/C1 control bytes before rendering agent-/error-derived text (no terminal-escape injection), and `purpose` is sanitized; the internal validation hostname was genericized out of this changelog. New revert-proven guards: `scope` unit (leading-slash collapse + `isOffOriginHttp` scheme gate) and `tests/live/scheme-gate.test.mjs` (a javascript: anchor is fired, not skipped). ACCEPTED (documented, mostly pre-existing): a same-origin control whose JS onclick redirects off-origin still navigates before we can refuse it — inherent to driving untrusted pages (any visited page can script-navigate on load); the after-merge guard keeps it out of the graph and the next act re-navigates back. 63 tests green.
- Real-site generalization: MULTI-ROUTE recon (run on any same-origin site, collect more than the entry page; design by cto/Fable, code Opus). `actStep` now derives the after-snapshot route from `page.url()` (the route the act LANDED on) and merges revealed controls under it — simultaneously the correctness fix for nav acts AND the multi-route discovery engine: new routes' controls enter the graph with the right `node.route` and the revert-proven global template frontier hands them out unchanged. The persistent step re-navigates per act to the TARGET's own `node.route` (a control first seen on /products is reached by loading /products). New `lib/recon/scope.mjs` owns route identity (`routeKey` — drop query + plain #anchor, keep path-like SPA hash, keep the CONCRETE navigable path) and origin scope (`sameOrigin`, RFC 6454 scheme+host+port). Off-origin links are NEVER fired: `actStep` reads an `<a>`'s href and records an off-origin one as `external` (effect `external-link`, counted, not clicked) — the SSRF gate only guards our navigations, so this is the scope boundary for acted links. `frontierStats.routes` reports routes mapped (surfaced in `report`). New guards: `scope` unit (routeKey normalization + sameOrigin port sensitivity) and one live crawl (`tests/live/multi-route.test.mjs`) proving all three mechanisms — multi-route reach, landed-route attribution, off-origin never fired (partner server gets zero hits) — each revert-proven. 60 tests green. Deferred to the stay-on-page ADR: BFS route queue + per-route budget, eager href harvest, per-(route×template) causal keying, and overlay/cookie-consent dismissal (a consent banner still leaves controls NOT_VISIBLE).
- Real-site hardening #1 (found by a live public-site validation run): the fire path stalled the full 30s Playwright actionability timeout on every DOM-present-but-invisible control (a responsive layout's mobile menu is `display:none` on desktop, yet dom-snapshot captures it — by design, it does not filter by visibility). On a real site that alone cripples a run. `actStep` now `isVisible()`-fast-fails such a control with `NOT_VISIBLE` before the click (and bounds the click itself to 5s so a transiently-unclickable control fails in seconds, not 30). `observe` gains a `not-visible` effect that `markUnreachable`s — honest-coverage parity, so a page full of hidden/duplicated controls does not inflate the explored denominator. `recon.md` maps `NOT_VISIBLE ⇒ not-visible` in its effect derivation. New revert-proven live guard (`tests/live/not-visible.test.mjs`: hidden `#hid` rejects `NOT_VISIBLE` in <2s while the visible sibling still acts) + unit guard (`observe.test.mjs`). 57 tests green.
- Security pass before the first commit (security-reviewer). Moved the destructive/auth/payment refusal onto the FIRE PATH: `actStep` now refuses to fill/click a floored control and throws `DANGER_FLOOR` before touching the page, so every path (whats-new `--act-template`, the loop, a mis-judging agent) is guarded — not just `observe`'s after-the-fact record. Hardened `dangerFloor` (camelCase/snake/kebab normalization so `deleteAccount` can't slip the whole-word net, extended vocabulary, icon-only/no-name → `unknown` not `safe`). Chromium sandbox now ON by default (`--no-sandbox` opt-in via `PW_NO_SANDBOX=1`). Hardened the `recon` agent prompt (page-derived CLI values are DATA → single-quote; a page embedding shell metacharacters is an injection attempt). Escaped the `data-id` value as a proper CSS string in `dom-snapshot`. Documented two accepted/known items: forgeable in-page causal globals (inherent — the CDP initiator is the second, page-unreachable mechanism) and DNS-rebinding SSRF (pre-existing in the ported host-policy). New live guard `tests/live/danger-gate.test.mjs` (act on Delete → rejects + zero `/api/delete`) and extended `danger-floor` unit guards; both new guards revert-proven. 46 tests green.
- DEBUG CAPTURE + local admin viewer — so a Phase-1 run can be inspected visually (the user's ask: "graph, how the agent walks, what it tests, logs, speed — with screenshots"; architecture by cto/Fable, code Opus). New `lib/debug/trace.mjs` is the SINGLE writer of a per-run trail under `state/runs/<runId>/`: an append-only `events.ndjson` of `{seq,ts,kind,payload}` (`route`/`frontier.emit`/`act`/`observe`), per-step `graph/<seq>.json` snapshots (graph GROWTH), and before/after viewport key-frames + the target rect + per-phase timings on each act. CAUSAL SAFETY (the biggest risk, mitigated structurally): the capture collaborator runs ONLY while the cause token is `__idle__` (before `beginCause` / after `endCause`) and every frame is `fullPage:false` (scrolls nothing), so it fires no page request and can never forge a phantom edge — proven by `tests/live/capture-causal.test.mjs` (capture ON, the in-window `/api/shared` poll still rejected, both key-frames + rect + timings produced; revert-proven). Trail is OPT-IN via `BUGHUNTER_RUN_ID`: absent → the recon CLIs are byte-identical to before. Wired into both front-ends — the agent path (`whats-new` act + baseline `route`, `frontier-cli` emit, `observe` verdict, each a separate process appending to the shared file) and the automated `recon-run` loop (opens/closes the run, per-act event + snapshot; marks the template explored before snapshotting so the graph view never lags a step). `lib/debug/trace-cli.mjs` brackets a run (`--open` mints the runId + run.json, `--close` stamps final coverage stats). `lib/debug/admin-server.mjs` is a zero-dep `node:http` viewer bound 127.0.0.1 ONLY, with a loopback Host-guard (DNS-rebinding defense), NO CORS header ever (a foreign tab's fetch is same-origin-blocked), and a resolved-path artifact allowlist (runId/seq/filename regex-validated + containment check) — patterns adapted from the sibling repos' collector + artefact server. `lib/debug/admin.html` is a self-contained vanilla page: Walk (event timeline + focused act detail with before/after screenshots, a red rect box over the acted target, the caused control→endpoint list, and per-phase timing bars), Graph (route→control→endpoint tree with a step scrubber over the snapshots), Log (kind-filtered event table), Speed (per-act stacked timing bars) — live-polls so it fills in as the crawl runs. `/recon` now opens the run, boots the admin (http://127.0.0.1:7666/), threads `BUGHUNTER_RUN_ID` through every acting command (baseline, frontier-cli, and the recon subagent), and closes the run at teardown. New guard `tests/unit/admin-server.test.mjs` (serves the trail behind the loopback + no-CORS + containment fence; a foreign Host is 403'd; revert-proven on the Host-guard). Validated end-to-end against a live fixture (real screenshots + graph snapshots + causal `GET /api/other` edge served) and the UI rendered headless with zero console errors. 68 tests green.
- Three-reviewer pass (bughunter + backend + security) on the debug-capture feature; verdicts SHIP / SHIP-AFTER-FIXES / 0-crit-0-high; all findings fixed. Keystone confirmed provably safe (bughunter): every key-frame is taken on an `__idle__` token and is `fullPage:false`, and the throw path re-throws before the after-frame — capture is inert, not assumed. HONEST-COVERAGE fix (backend SHOULD, treated as MUST): the trail's write path could throw INTO the crawl — an unguarded `traceEvent`/`ensureDirs` I/O failure (full disk/EACCES) with `BUGHUNTER_RUN_ID` set would, in whats-new, skip the final `saveGraph` (dropping a causal edge) and, in recon-run, trip `reconLoop`'s catch (falsely marking an acted control `unreachable`, collapsing the denominator). Fixed: `trace.mjs` writes are now best-effort (try/catch, return a sentinel), same discipline as the screenshots/snapshotGraph — the diagnostic can never perturb the crawl. TRAIL COMPLETENESS (backend + bughunter CONSIDER): a FAILED act (`NOT_VISIBLE`/`OFF_ORIGIN`/`DANGER_FLOOR`) left no timeline entry on the node-loop path — `recon-run` now emits an `act` event with `{error}` on the throw (then re-throws so coverage is unchanged), and the admin renders it as a red "failed" step; whats-new's baseline `route` event now writes a graph snapshot too, so the scrubber has no 404 gap. SECURITY (M1 medium): the Host allowlist stops a browser but not a local non-browser client (another user's `curl`) — the trail can hold logged-in session screenshots, so `admin-server` now gates every `/api` route with a per-startup ACCESS TOKEN (`crypto.randomBytes`, constant-time compare) that rides in the URL the operator opens (`…/?t=<token>`, Jupyter-style); `admin.html` threads it through every fetch + `<img>`; `/recon` surfaces the tokenized url. SECURITY (low): added `X-Content-Type-Options: nosniff` + `X-Frame-Options: DENY` on every response and a strict CSP on the page (no framing, no MIME-sniff, CSP backstop); the 500 handler no longer reflects `err.message` (path-leak) and guards `res.headersSent`; `safeArtifact` adds a `realpath` containment check (refuses a symlink escaping the run dir) atop the lexical one. Defense-in-depth: `esc()` in the viewer now also escapes single quotes (the two single-quoted `data-rect`/`data-vp` attrs; numeric-only today); a REDACTION-POINT comment marks the trail's `fill` field for when auth/login-fill lands. New revert-proven guard: the admin test now also asserts a no-token `/api` request is 403'd (token gate) alongside the foreign-Host 403. Re-validated end-to-end with the token: no-token → 403, tokenized url renders all four tabs with screenshots loading through the gate, zero console errors; the graph scrubber shows acted controls as explored (markExplored-before-snapshot). 68 tests green.

- Fixed a false-STALL in the drained-or-stalled loop (adversarial-review CONFIRMED; design Fable, code Opus). `decideProgress` keyed the stall on flat instance `remaining`, but `remaining = walkable−walked−unreachable` is FLAT in a balanced window (walk N controls + those acts reveal N new instances) even though the crawl IS progressing → the loop false-terminated a still-progressing run. The stall now keys on the MONOTONE `progress = walked+unreachable+walkable` (grows on explore, on a failed act via unreachable, and on discovery via walkable; a panel-reach REOPEN dip only breaks flatness → resets stall accumulation, conservative) over K=3 WINDOWS — still a window count, never a clock. `trace.mjs readFrontierProgress` replaces `readFrontierRemaining`; `frontier-cli` passes the progress history to `decideProgress`. Supersedes the first-stone D5 remaining-based stall (see decisions.md). Revert-proven: `loop-control.test.mjs` (the balanced-plateau repro reds under the old remaining-based stall), extended `frontier-cli.test.mjs` (a progress≠remaining discriminator). Suite 167 → 169 green.
- Built the Layer-3 replay WRITE-FIREWALL — closes a pre-existing HIGH gap (design Fable, code Opus, security-reviewed + hardened). Reveal-path replay clicks run under `__idle__`, which suppresses only the GRAPH edge — the outbound non-GET STILL hit the LIVE authed account (a reveal opener firing a mutation, or an adaptive-server swap). Now `replayRevealPath` installs a `page.route` firewall that ABORTS any non-GET outside the opener's OWN recorded-triggers allowlist (POST-reads only, never PUT/PATCH/DELETE), torn down (`page.unroute`) in a `finally` BEFORE the measured act (a firewall left installed would abort the act's own legitimate non-GET); a blocked write → `REVEAL_WRITE_BLOCKED` → unreachable, account unmutated. Security review found 0-crit / 1-high / 2-med / 2-low, ALL fixed: [H1] canonicalize the live request IDENTICALLY to the allowlist provenance (`toUrlPattern(req.url())`, full URL — a pathname-only strip let `POST /api/item?_method=DELETE` match an allowlisted `POST /api/item` and execute the delete); [M1] the handler catch fails CLOSED for non-safe methods (a classification error must never pass a write); [M2] off-origin / danger-route (`routeRefused`) is checked for EVERY method incl. GET, so a programmatic `fetch('/logout')` during authed replay is blocked (the `<a href>` guard alone missed it); [L1] log only `{method, urlPattern}` (query values already masked), never the raw URL (secret-in-query); [L2] allowlist POST only. Revert-proven: `tests/live/write-firewall.test.mjs` + `firewall-app` fixture (server counters as levers); blast-radius on `stay-on-page`/`agent-stay-on-page` 7/7.
- Built the LOCATION DENOMINATOR — the cto-corrected INC.3 first slice (design Fable, code Opus). A PURE `lib/recon/location-key.mjs` `locationKey(route, statePath)` digests the ALREADY-captured `reveal.statePath`, giving every POST-nav section that collapses under one `routeKey` a distinct identity with ZERO body dependency — the correction to the plan's original body-`payloadShape` key, which is DARK on the default `/recon` run (bodies are double-gated off, so it would degenerate to `routeKey`). `frontierInstanceStats.locations.discovered` counts distinct locations; `report --unreached` graduates the placeholder `routeCollapse:'pending-INC.3'` → `'split-by-location'` (a discovered-unexplored control is now honest BUDGET), the `note` still flagging UNDISCOVERED locations as structurally-uncountable never-discovered (no overclaim). Invariant #2: locationKey is a frontier/reporting HINT — never a node id / instanceKey / reqKey / edge, `diffIdentity`-proven zero churn. Scope-disciplined: reach BEHAVIOR is unchanged (per-location re-drill + markOpener-on-new-location are deferred to their own reviewed cycle, with the write-firewall as their safety net). Revert-proven: `location-key.test.mjs`, `frontier.test.mjs` locations tally, `report-unreached.test.mjs` (graduation + identity proof).
## 2026-07-15
- Redesigned the debug admin VIEWER to a Playwright-trace-style single-screen scrubber (design iteration with the user, referencing aeye-os's test-run view). Top tabs `[Прогоны|Тесты]`; a left recon-runs list; a centered before/after screenshot with a bottom filmstrip of DOTS (one per act) that click-jumps, drag-scrubs (pointer capture), and keyboard-steps a single shared step cursor; a right steps list with inline per-step detail (caused control→endpoint, timings, observation). The graph lens was removed for now (deferred, not deleted from the trail). Extracted the scrubber geometry into a pure `lib/debug/scrub-math.mjs` (`deriveSteps` + dot/index/box math), served same-origin token-free so the page's CSP `script-src 'self'` can import it; `admin.html` rewritten as the tabs/runs/steps layout; `admin-server` gains the `/scrub-math.mjs` route. New revert-proven unit guards (`scrub-math.test.mjs`; the admin test asserts the module is served). Iterative per the user ("потом будем доделывать") — Phase-2 polish (hover thumbnails on dots, Tests-mode content, a long-run minimap) deferred.
- Ran the first full Phase-1 recon against a LIVE public site (dev.rawcaster.com) end-to-end through `/recon`: 9 agent batches drained the frontier, 6 same-origin routes mapped (`/`, `/dashboard`, `/nuggets`, `/support`, `/faq`, `/termsandconditions`), 44 control templates (31 explored / 13 unreachable-coldstart), 5 causal control→endpoint edges, auth/off-origin/destructive controls correctly recorded-not-fired, and the debug trail (88 events, 36 key-frames, 83 graph snapshots) replayed in the admin viewer with zero console errors. Surfaced three real items: an app surface (`/dashboard`) reachable and hitting data APIs WITHOUT login (a pre-auth exposure to check in Phase 2), a sticky-topbar overlay that intercepts clicks on nav items, and a Phase-1 tool gap — `whats-new --fill` focuses an input but issues no Enter/submit, so a search endpoint is never captured.
- AUTHENTICATED recon built (design by cto/Fable, code Opus) — lifts login/auth from the "unbuilt" list. New `lib/recon/login.mjs`: a login PRE-STEP that reads creds from the operator env (`BUGHUNTER_LOGIN_USER`/`_PASS`, never argv or a committed file), drives a CLEAN chromium (`launch({anonymous:true})`), fills the form (email/password/submit heuristic + `--*-selector` flag overrides), VERIFIES the session started, and persists a Playwright storageState (`state/storage-state.json`, 0600). It is SETUP not a measured act — opens no debug trail, so the credential-filled frame is never captured and causal attribution is provably untouched; the result envelope carries counts only (never the creds). `session.mjs` grows `contextOptions()` — the SINGLE storageState injection point applied at BOTH `newContext` sites (cold-launch + daemon-attach) via `BUGHUNTER_STORAGE_STATE`, so one change authenticates the whole crawl (the graph/causal/coverage machinery is unchanged); a set-but-MISSING state path FAILS LOUD (`STORAGE_STATE_MISSING`), never a silent logged-out crawl; `opts.anonymous` forces a clean context. New `routeRefused()` in `danger-floor.mjs` (single `REFUSED` set now shared with `step.mjs`'s click gate) — a route-level guard in `recon-run` `persistentStep` refuses to NAVIGATE to a destructive/auth/payment route (the GET `/logout` the name-only click gate cannot see), so an authed crawl never ends its own session; reconLoop marks it unreachable (honest). `/recon` gains an optional "Authenticated mode" preamble (fresh graph → login → export `BUGHUNTER_STORAGE_STATE`); the "No login" boundary is updated. New auth fixture (`tests/fixtures/auth-app`, models the real traffic classes) + guards, all revert-proven: T1 storageState makes the crawl authenticated (drop it → bounced to /login), T2 login verifies before persisting (wrong creds never write a state), T3 the route guard prevents self-logout (drop it → `/logout` hit), T5 no credential leak in login output; unit `contextOptions` (inject/fail-loud/anonymous) + `routeRefused`. 83 tests green. Deferred (honest): multi-step/SSO/OAuth/2FA login, mid-crawl session-expiry re-login, the click-navigates-to-/logout class, an auth-liveness probe.
- RESPONSE METADATA capture — the causal request channel now records each request's response status / mimeType / resourceType / duration, not just method+URL (GAP 1 of the information-collection architecture; design by cto/Fable Option A1, code Opus). The insight: the CDP session `initiator.mjs` ALREADY holds (for the classifier) is grown into a per-`requestId` LEDGER — new `lib/browser/response-ledger.mjs` (`makeLedgerTracker`) appends on `Network.requestWillBeSent` and fills status/mime on `responseReceived` + duration on `loadingFinished`, on the SAME session (no second channel). `beginCause` snapshots a ledger cursor (`markCursor`) beside the existing `seq0`; `endCause`, AFTER the kept-set is decided by the unchanged three filters (token cause+seq0, initiator verdict, static), joins each kept fire to a ledger entry by ordered `(method, pathname, index≥cursor)` take-and-mark and attaches the response fields. CRITICAL (the killer invariant, reviewer-confirmed SHIP): the KEPT SET is byte-identical — response data is attached to already-kept fires only, the join has no `continue`/drop/add, and `classifyInitiator` + the pathname verdict map are untouched; nothing introduces a wall-clock window (durationMs uses CDP monotonic timestamps, for reporting only). METADATA ONLY by design — no `getResponseBody`, no headers, no bodies persisted anywhere, so zero secret exposure (bodies + redaction are the next scoped task). `addTrigger` accumulates observed `statuses {code:count}` + `resourceType` on the request node (status is NOT identity — reqKey stays method+urlPattern, zero id/edge churn); `step.mjs` carries the fields into the per-act `requests[]` so the debug trail preserves the request↔response pairing; `report` renders `[3] → GET /api/list  200 xhr` and adds the fields to `--json`. Ledger `reset()` clears per act (bounded on a reused page); CDP-failed → inert tracker → metadata simply absent (token-only behavior as before); cap 1000 (2x the 500 fire-ring, shift-oldest with cursor-decrement). Same-`(method,pathname)` pairing in one window is order-approximate (metadata only, never attribution). New guards, all revert-proven: `tests/unit/response-ledger.test.mjs` (cursor scoping + ordered take-and-mark + duration + reset), `tests/live/response-meta.test.mjs` (+fixture — status attached to the caused request AND the in-window poll STILL rejected with capture ON + vacuity guard; two same-path requests pair in fire order; actStep→graph carries the fields), extended `report.test.mjs`. 94 tests green. Both new lib files single-responsibility & <200 lines (initiator 126 / response-ledger 86).
- REQUEST/RESPONSE BODY capture — GAP 1 step 2, the follow-on to response metadata: the causal channel can now sample request + response BODIES (payload/response shape for Phase-2), OPT-IN and REDACTED (operator-chosen policy; design by cto/Fable, code Opus, two full review rounds). DOUBLE GATE, off by default: bodies are fetched/retained only when `BUGHUNTER_CAPTURE_BODIES=1` AND a run is active (`BUGHUNTER_RUN_ID` set) — the run-gate means bodies only exist when there is a trail to hold them, and `login.mjs` now `delete`s both env vars from its own process before wiring the browser, so the credential exchange is STRUCTURALLY uncapturable (not merely ordering-dependent). New PURE `lib/browser/redact.mjs` (the security-critical core, 169 lines, unit-tested): redacts at CAPTURE TIME before anything touches disk — KEY-level (secret vocabulary + a bare `…_key`/`…_token`/`…_secret` segment) AND VALUE-level on every string leaf (JWT, `Bearer`, AWS `AKIA/ASIA`, Stripe `sk_(live|test)_`, GitHub `gh[posru]_`, Slack `xox[baprs]-`, Google `AIza…`, card, SSN, email) so a secret in a VALUE under an innocent key is caught; a card-shaped JSON number leaf is redacted too; form-urlencoded is walked STRUCTURALLY (URLSearchParams); `normalizeKey` handles camel/snake/kebab + the `APIToken` acronym split; identity keys (username/email/login) masked in form request bodies. ALL patterns LINEAR (upper-bounded quantifiers) + a 1 MB DoS WORK-BOUND before any parse/regex, and the 64 KB OUTPUT cap runs AFTER redaction (a secret straddling the cut is fully `[REDACTED]`, never severed) — no ReDoS (measured 270 KB pathological body 3825ms→19ms after the fix). Response body is fetched via `Network.getResponseBody` AT `loadingFinished` (eviction-safe); request body is the requestWillBeSent postData, gated on content-type (multipart/binary skipped). CAUSAL INVARIANT preserved AND hardened: the first review caught that awaiting a body INSIDE endCause's kept-deciding loop let a later fire's initiator verdict flip mid-decision (a phantom-edge regression) — fixed by splitting endCause into a pure synchronous `selectKept` (token cause+seq0 + initiator verdict + static, ZERO await, byte-identical to the metadata path) then an additive Pass-2 enrichment that awaits bodies over the FROZEN set (Promise.all, per-promise ~500ms bound, `.catch`-guarded so a body fetch can never collapse coverage). Bodies reach the TRAIL only, NEVER stdout/graph/events.ndjson: `actStep` keeps the metadata `requests[]` body-free (it flows to stdout + the graph node, which stays method+urlPattern), and redacted bodies are written to files under gitignored `state/runs/<runId>/bodies/` (0700 dir / 0600 files) with only refs in the act event — mirroring how screenshots are stored. Two review rounds (bughunter + security ×2): the causal race + a JWT-in-value redaction hole + a ReDoS + provider-secret/opaque-key gaps + the login-gate + file perms all found and fixed; final security verdict 0 critical / 0 high, containment confirmed. New revert-proven guards: `tests/unit/redact.test.mjs` (key/value/provider/segment/acronym/number/straddle/ReDoS/allowlist), `tests/unit/select-kept.test.mjs` (synchronous kept decision), `tests/live/response-body.test.mjs` (redacted body → trail with capture ON, none with it OFF, off-allowlist skipped, the in-window poll still rejected, a white-box two-pass race seam with an explicit vacuity guard, requests[] body-free), + request-allowlist/form-redaction/half-open-gate units. 122 tests green; every touched lib file <200 lines. Still DEFERRED: exposing bodies in the admin viewer; an operator "disable redaction" escape hatch; the durable (non-trail) body home — that is the LLM-first graph-schema task.
- Validated authed recon on the LIVE site (dev.rawcaster.com): `login.mjs` logged in through the real form (`--user-selector=#rawcaster_login_username`, `--submit-selector` targeting the Login button past the Google/Apple OAuth submits), captured 6 cookies into a 0600 storageState, and a fresh `/recon` on `/dashboard` mapped the LOGGED-IN surface (the composer greets "hey Stierlitz…" — proof of session) with real authed causal edges (`For You`/`Following` → POST /rawcaster/listnuggets + addnuggetview + get_ad). Confirmed the boundary honestly: the app is an antd SPA, so its tabs/feed live behind in-app client state → `unreachable-coldstart` (the same STAY-ON-PAGE limit, now on the authed surface); new "routes" are client-side, not direct-navigation reachable.
- Three-reviewer pass (bughunter + backend + security) on authenticated recon; verdicts SHIP-AFTER-FIXES / SHIP-AFTER-FIXES / 0-crit-0-high; all findings fixed. SECURITY: [M1] storageState had a 0644→0600 world-readable window (co-tenant could read the live session) → the file is now pre-created 0600 so Playwright's truncate-write preserves the mode; [M2] a compromised/XSS'd login page could exfiltrate the plaintext credentials via an off-origin `<form action>` → login now refuses an off-origin form action BEFORE typing the creds (new T6, revert-proven); [M3] `routeRefused` missed `sign off`/`signoff` → added to the AUTH regex; [L1] the chromium child inherited the password in its env → `login.mjs` passes a credential-scrubbed `env` to `launch()`; [L3] a percent-encoded danger route → `routeRefused` now decodes first. CORRECTNESS (backend): [B1] the field-discovery heuristic used false document-order `querySelector` semantics (a decoy newsletter form before the login form would capture the fill/submit) → the password is now the anchor and the username+submit are scoped to ITS `<form>`, username selectors tried in priority order (the fixture gained a decoy form so T1 revert-proves it); [B2] `ROUTE_DANGER`/`OFF_ORIGIN` pre-nav refusals left no debug-timeline entry and a comment mis-claimed `actStep` throws `OFF_ORIGIN` → both now emit a failed-`act` trail event, comment corrected. DEFENSE-IN-DEPTH (bughunter): the route guard was only on the node-loop path, but `/recon` drives the agent path → the same `routeRefused` gate added to `whats-new` before nav; recon.md's self-logout claim scoped to both paths. Plus C-items: a failed login now removes any stale storageState, a custom `--out` dir is created, and a unit test pins that `anonymous` short-circuits before the existsSync check. 85 tests green; every new guard (T6, routeRefused sign-off/percent-decode, contextOptions anonymous-missing, B1 decoy) revert-proven. Documented-deferred: an unset (vs missing) `BUGHUNTER_STORAGE_STATE` still silently crawls anonymous; `routeRefused` conservatively over-refuses `/reset-password`-style routes (a Phase-2 allowlist may relax it).
- STAY-ON-PAGE reach — GAP 2 FIRST SLICE (depth-1): reach + collect controls revealed ONLY by an in-page action (modals/dropdowns) — the last collection gap (ADR design by cto/Fable, code Opus, bughunter review SHIP-after-fixes). Built the read-only `lib/graph/identity-diff.mjs` gate FIRST — `diffIdentity(before,after)` → `churnedTemplates`/`churnedInstances`/`droppedTemplates`/`droppedInstances`/`droppedEdges` (all MUST be []); CLI `--before/--after`, non-zero exit on churn — so "an identity-adjacent change churns zero ids/edges" is a one-command fact, not an assertion (retires the probe deferred in the 2026-07-14 locator decision). IDENTITY: a control revealed by an in-page act carries an ADDITIVE `node.reveal = { route, statePath:[{templateId,instanceKey}] }` annotation (operator chose the ANNOTATION, NOT a first-class State node — that stays the deferred LLM-first graph-schema task), stamped by `mergeSnapshot` on NEW nodes only (first-reveal-path-wins). ZERO identity churn — ids still minted from selector strings before merge, `revealPath` never enters a key (proven: same elements with/without revealPath → identical ids + `diffIdentity` ok:true). RE-DRIVE: `lib/recon/reveal-replay.mjs` — `persistentStep` re-navigates to the route, REPLAYS the reveal-path clicks under `__idle__` (their fires excluded by the token filter), `resetTrackerVerdicts` between each + before the measured act, then acts, so a modal control is now genuine coverage, not `unreachable-coldstart`. CAUSAL SURVIVAL at depth proven: a shared-path background poll ticking INSIDE the measured modal act is STILL rejected by the CDP initiator — asserted in-window via the raw fire-ring (a `/api/poll` fire carrying the act's cause is dropped while `/api/modal-save` is kept), not just an aggregate count. SAFETY: `dangerFloor` re-checked on EVERY replay step (whole path refused if any floors → unreachable); a replay click that changes route aborts (no longer stay-on-page); and a NON-GET (mutating) opener's children are NOT stamped/replayed (GET-only replayability — a "Save draft" POST opener is never re-fired), they stay honestly unreachable. FRONTIER: `nextBatch` carries `reveal`; `frontierStats` UNCHANGED (denominator stays template-count — no per-state identity, so it never multiplies). SCOPE: NODE-LOOP path only (`recon-run`) — the agent path (`whats-new`/`observe` via `/recon`) does not yet thread the reveal-path, so an agent-driven run still leaves modals `unreachable-coldstart` (documented in CLAUDE.md status). New revert-proven guards: `tests/unit/identity-diff.test.mjs` (churn / dropped-key / dropped-edge / empty-before), `tests/unit/reveal-annotation.test.mjs` (stamp + first-path-wins + zero-churn via diffIdentity + frontier carries reveal), `tests/live/stay-on-page.test.mjs` (modal Save is genuine coverage not unreachable, `reveal.statePath=[Open]`, its `GET /api/modal-save` causally attributed, the `/api/poll` poll rejected in-window, a POST-opener's child stays unreachable) + fixture `tests/fixtures/modal-app`. `recon-loop.test.mjs` updated to the improved behavior (search-app's Edit, revealed by the `#search` GET, is now genuinely reached — unreachable 1→0, explored 2→3 — reviewer-verified a REAL coverage gain, not a loosened test). 134 tests green; every touched lib file <200. Still deferred: agent-path stay-on-page, depth-N / amortized in-app DFS / depth-cap / per-row instance reveal-paths; the durable State node = the LLM-first graph-schema task.
- Three-reviewer pass (bughunter + backend + security) on the GAP 2 stay-on-page slice; verdicts SHIP-after-fixes / SHIP-after-fixes / 0-crit-1-high-2-med; all findings fixed. SECURITY [H1, the key fix]: replay CLICKS ran raw `handle.click` with only the stored-name danger floor, so on an authed crawl against an ADAPTIVE server a reveal hop could self-logout / follow an off-origin or private-host link / hit a destructive GET BEFORE the post-hoc route-change abort — reopening, for the replay path, the self-logout hole `routeRefused` was built to close. Fixed by mirroring `actStep`'s PRE-CLICK guards on the LIVE resolved element: an off-origin href (`isOffOriginHttp`) → `REVEAL_OFFORIGIN`, a danger-route href (`routeRefused(routeKey(href))`) → `REVEAL_DANGER`, both refused BEFORE the click; plus a `REVEAL_MAX_DEPTH=10` cap (`REVEAL_TOO_DEEP`). BACKEND: `lib/graph/identity-diff.mjs` now FAILS LOUD (INTERNAL, non-zero exit) on a PRESENT-but-unparseable `--before/--after` instead of reading a corrupt state as an empty first-run (which made the churn gate falsely green); `statePath || []` robustness. BUGHUNTER: the GET-only stamp gate, the in-window causal-survival vacuity guard (asserts the poll ticked inside the measured window via the raw fire-ring, not an aggregate count), and the identity-diff dropped-key pass were all confirmed CLOSED. New revert-proven live guards for every replay safety branch (STALE / DANGER / OFFORIGIN / NAVIGATED / TOO_DEEP — a live test injects an off-origin and a `/logout` hop and asserts the click is NOT followed, extHits/logoutHits stay 0) + the corrupt-state unit gate; the live-guard index in tests/CLAUDE.md updated. 136 tests green. KNOWN residual (decisions.md): an adaptive server swapping a recorded-GET opener to a POST at replay fires under `__idle__` uninstrumented (caught only if it also becomes a logout/off-origin/danger link) — revisit with the depth-N ADR.
- WHOLE-SITE REACH on a constant-URL SPA — agent-path stay-on-page + POST-read replayability + fixed viewport (root cause found on a live `/recon` run of dev.rawcaster.com; ADR design cto/Fable, code Opus). A full authed `/recon` drained the frontier but mapped ONE route (14/47 explored, 33 unreachable, routes=1): the SPA serves its ENTIRE app under `/dashboard` — nav is `<a href="/dashboard">` swapping content client-side WITHOUT a URL change (probe: urlChanged=false), tabs are in-app `<div>`s, "read" queries fire over POST. Recon's route==URL + re-nav-per-act model = one route, everything in-app-state `unreachable-coldstart`. TWO blockers fixed: (a) the AGENT path (`whats-new`, which `/recon` drives) never threaded the reveal path — now it imports the SAME `applyReveal` prologue the node loop uses (~6 lines mirroring `persistentStep`; reset point = the baseline nav; causal discipline inherited by construction), so a modal/panel control is reached + attributed on the agent path too; (b) the GET-only stamp gate refused a POST-firing opener — now `actStep` takes an `openerReplayable` opt (`stamp = revealPath && landed===route && (allGet || openerReplayable)`) that `whats-new --opener-replayable` sets ONLY from the agent after judging the opener's POST is a READ (list/search/detail), never a mutation; the NODE loop never sets it, so its deterministic GET-only ceiling (and guard (e)) is unchanged. Plus a `REVEAL_CYCLE` cycle guard in `replayRevealPath` (a statePath revisiting a template is refused before walking) and a FIXED `viewport:{1440,900}` at the single `contextOptions()` injection point (reproducible NOT_VISIBLE denominator vs Playwright's implicit 1280x720). New revert-proven guards: `tests/live/agent-stay-on-page.test.mjs` (agent-path replay reaches + attributes a control behind a POST-read opener; the widen is load-bearing — omit `--opener-replayable` → child unstamped), the `REVEAL_CYCLE` case + the viewport unit case; fixture gained a POST-read `#open-read`→`#expand` opener. `recon.md` gains the read-vs-mutation judgment. 139 tests green; every touched lib file <200. KNOWN residuals (decisions.md): the agent's read-vs-write call is trusted (a non-floored mis-judged write could be replayed — bounded by the danger-floor, a per-opener cap is a candidate follow-up); rawcaster's hidden-panel tabs are a depth-2 chain that may leave a correct-but-unsatisfying `unreachable` residual until a live re-run. Deferred still: amortized in-app DFS, per-row instance reveal-paths, the durable State-node graph-schema task.
- Fixed `login.mjs` for SPA async-redirect logins (incidental, required to authenticate the rawcaster crawl at all): the success signal was checked ONCE right after `waitSettled`, but a SPA that resolves auth via an async XHR and THEN client-redirects lands AFTER the network idles, so a slow redirect read as a false `LOGIN_FAILED`. Replaced with a bounded poll (≤8s, early-exit on first success); verification semantics unchanged (still gates persistence before writing the storageState, never a fixed sleep).
- INC.1 — framework-noise ids stop anchoring element identity (Fable research + CTO-directed; decisions.md). `dom-snapshot.mjs isGeneratedId` (the selector-path anchor) and `stableIdForLocator` (the sole-`#id` locator) now reject framework-generated wrapper ids (`rc-*`/`ant-*`/`radix-*`/`headlessui-*`/`mui-*` prefixes + hashed-hex-with-digit runs) via one shared `isFrameworkNoiseId` predicate. Ant Design tabs (`rc-tabs-0-tab-N`) that fragmented into N per-reload-shifting templates now collapse to ONE structural template with N addressable instances and a STABLE role+name locator — fixing the antd `NOT_VISIBLE`/`NO_INSTANCE` residual AND the stale-selector breakage in the reset-and-replay reveal chain. Framework CLASSES (`ant-tabs-tab`) are deliberately KEPT (the stable structural anchor). This is an INTENTIONAL identity re-key (the one class `identity-diff` protects) — measured, not zero-gated; a no-op on non-framework fixtures (suite stays green).
- Added `graph.schemaVersion` (=2) + `loadGraph` reset-on-mismatch (graph-store.mjs): a pre-INC.1 graph (framework-anchored ids) can never co-mingle with re-keyed current-scheme ids; state/ is ephemeral so a reset is safe. Bump on any future identity change.
- Tests: new `tests/fixtures/framework-id-app/` + `tests/live/framework-id.test.mjs` (de-fragmentation: 3 antd tabs → one template/3 instances/role-name locator; hashed-id branch; semantic-id negative control — revert-proven) and a `graph-store.test.mjs` schemaVersion guard (legacy graph reset, current preserved — revert-proven). Suite 140 → 142 green.
- INC.1 verified LIVE on authed dev.rawcaster.com (scheme-1 → scheme-2 before/after): the 8 antd nav tabs that were 8 `#rc-tabs-0-tab-0…7`-anchored 1-instance templates (fragmented, per-reload-shifting) collapse to structural templates with addressable instances; 10 framework-anchored templates → 0. De-fragmentation is a prerequisite fix (stable addressability so replay won't go stale); the panel-hidden tabs' actual explored-reach still needs the separate depth-2 panel chain INC.1 now unblocks.
- Three-reviewer pass (security / backend / invariants) on the whole inc.2 + INC.1 block before commit: security CLEAN (0/0/0 + 1 informational); backend + invariants SHIP-AFTER-FIXES. Fixed the one MUST FIX — page-derived `--instance` was shown unquoted in the recon.md command templates (an agent-path shell-injection surface, regressing the 2026-07-14 CLI-arg-quoting hardening): now single-quoted in every template + added to the page-derived-DATA list. Remaining SHOULD/CONSIDER items (opener-drain loud-error, instance-level `frontierStats` tally, a helper extraction for the 3 identity/step files now just over the 200-line guideline, ledger schema coupling, agent-path in-window poll assert) recorded as tracked follow-ups in decisions.md — all bounded, none a coverage/correctness lie.
- Instance-frontier honesty (two review follow-ups resolved same session): (1) OPENER-DRAIN LOUD-ERROR — `whats-new` (act) and `observe` (drain) now throw `USAGE` when a proven opener with >1 instance is acted/observed WITHOUT `--instance`, instead of silently acting the representative `instances[0]` (the wrong control, recorded against an instance never clicked); the node loop is immune. (2) FRONTIERSTATS INSTANCE BLINDNESS — new additive `frontierInstanceStats(graph)` reports the honest instance-level frontier (`walkable`/`walked`/`unreachable`/`remaining` mirroring nextBatch) + `cappedRemainder` (opener instances beyond `OPENER_INSTANCE_CAP`, flagged not hidden); `report.mjs` surfaces it as an `Instances:` line + `instanceCoverage` JSON. `frontierStats` untouched (exact-match tests stay green). New revert-proven guards: `tests/live/opener-drain.test.mjs`, extended `observe.test.mjs` + `frontier.test.mjs`. Suite 142 → 145 green.
- Depth-2 "panel reach" — fill-if-absent reveal for baseline-hidden controls (Fable-CTO Option A; decisions.md). A control PRESENT at baseline but HIDDEN behind an overflow panel (an antd "…more") is discovered PATHLESS, so first-reveal-wins locked it `NOT_VISIBLE`-unreachable with no path to replay. Now: `dom-snapshot collect()` records a per-element `visible` (Playwright-`isVisible` parity — additive, never identity); `mergeSnapshot` stamps write-once `hiddenWhenSeen` and, when a STAMPED opener act makes a pre-existing hidden-at-baseline instance visible, FILLS its reveal path + REOPENS it (clears `explored`/`unreachable` + the template flags if representative, since `reconLoop` marks every act explored) so the frontier re-emits and reaches it via replay; `step.mjs` counts the fill toward the reveal effect so a pure-uncover "…more" (revealing NO new instances) is still `markOpener`'d; `persistentStep` re-reads `inst.reveal` from the graph (not the stale batch item) so an opener-first-in-batch ordering reaches the child same-batch. Replay machinery + causal `__idle__` discipline reused unchanged; a tab with no opener stays honestly unreachable. Rejected: a NOT_VISIBLE→open-parent runtime retry (inverts the frontier/act split) and structural ARIA opener inference (heuristic the invariants reject). New revert-proven guard: `tests/live/panel-reach.test.mjs` + `tests/fixtures/panel-tabs-app/`. Suite 145 → 146 green. Deferred: a `report.mjs --unreached` fail-reason histogram; the live authed rawcaster reach number (separate validation — antd overflow may render a dropdown-portal copy or hover-trigger, settled by a safe agent-path diagnostic).
- Panel-reach review pass (bughunter + backend, both SHIP-AFTER-FIXES / 0 MUST FIX) applied before commit. [Backend SHOULD] the fill trusted "hidden-at-baseline + visible-now", so a control that appears by its OWN means (deferred/animated) and is genuinely acted (`explored`, not `unreachable`, `reveal` still null) could be reset by an unrelated later opener + stamped a bogus reveal path (first-reveal-wins then locks it in) → the fill now also requires `!(inst.explored && !inst.unreachable)`, protecting genuine coverage (unit-guarded in `graph-store.test.mjs`, revert-proven). The fill logic was extracted to a named `fillRevealIfHidden` helper (homes the guard, shrinks `mergeSnapshot`). [Both SHOULD] the same-batch persistentStep graph re-read was not revert-proven (the tabs-first fixture exercises only the cross-batch reopen path) → added an `/app-opener-first` fixture route (#more before the tabs → same batch, opener first) + a second `panel-reach` test; reverting the re-read now reds ONLY the opener-first test, tabs-first stays green. [Bughunter SHOULD] `panel-reach` guard (d) was a coarse no-misattribution sanity mislabeled as causal survival (it never forces the poll in-window) → relabeled honestly, pointing at `stay-on-page`/`whats-new` for the in-window adversarial proof; the FAIL-ON-REVERT header corrected (`tab.explored` STAYS green on revert — the loop drains it; `!tab.unreachable`+`tab.reveal`+`more.opener` are what go red). [Backend CONSIDER] `isVisible` switched to `getBoundingClientRect().width/height>0` (Playwright's box test — a 0×0 box reads hidden) with the comment softened from "parity" to "approximates"; optional chaining added to the persistentStep re-read. Suite 146 → 148 green (the opener-first live test + the genuine-coverage unit guard); every touched lib file behavior unchanged on non-framework fixtures.

## 2026-07-16
- Built the Layer-1 exhaustive-traversal engine — the `/recon` loop now runs until DRAINED-or-STALLED, not a `cap 20` budget (the plan's "first stone"; design Fable, `cto`-validated across D1–D5, code Opus). KEY correction from the cto: DRAIN was already instance-honest (the real gate is an empty `nextBatch`, whose eligibility predicate `frontierInstanceStats` mirrors — batch-empty ⟺ `remaining===0`, the 204 opener-instances included), so the genuine gap was STALL detection, not drain. New pure `lib/recon/loop-control.mjs` (`decideProgress` → continue|drained|stalled); `frontier-cli --emit` now also returns `frontierInstanceStats` + a `progress` verdict and records `instanceStats` into its `frontier.emit` trail event (per-window history on disk), reading the last-K `remaining` back via a new `trace.mjs readFrontierRemaining`. STALL keys on a FLAT instance-level `remaining` over K=3 WINDOWS (never wall-clock — a timer would be the first crack toward the forbidden wall-clock attribution class): `remaining` shrinks on both explore AND unreachable-mark, so an honest unreachable-tail burn is not a false stall, and a reveal that GROWS the frontier is discovery, also not a stall. DRAINED carries `cappedRemainder` so it never reads "done" while beyond-cap opener siblings remain (honest-denominator applied to the termination signal). The prerequisite `act.failed` seam: `whats-new`'s main-catch now writes a best-effort `act.failed {templateId, code}` trail event before re-throwing — a failed act previously wrote to NEITHER graph nor trail (the granular reason vanished to stderr), so this is the deterministic source the histogram's granular buckets read. `.claude/commands/recon.md` rewritten to obey `progress.action` + a 200-iteration RUNAWAY safety-ceiling, print `report --unreached`, and stop citing the misleading template-level `stats.remaining`. Rejected (cto): a separate loop-control CLI / a persistent `recon-progress.json` (the graph already resumes the crawl; stall history is honestly per-run, a fresh counter per `/recon` is correct) / widening `observe`'s enum to carry granular codes (agent-fidelity vs a deterministic trail write) / splitting route-collapse from budget now (impossible pre-INC.3). Plan doc reconciled: `docs/PHASE1-COLLECTION-PLAN.md` §Tool (the histogram is NOT a pure graph function — it needs the trail; "MUST distinguish route-collapse" → "must FLAG pending-INC.3") + §9 ("depth-2 contradiction" resolved as a naming collision, depth-1 uncover built vs depth-N chain unbuilt). Revert-proven guards: `tests/unit/loop-control.test.mjs`, extended `frontier-cli.test.mjs`, `tests/live/act-failed-trail.test.mjs`. Suite 154 → 167 green.
- Built `report --unreached` — the fail-reason histogram (the one-command go/no-go artifact; design Fable, code Opus). Classifies every not-fully-exercised control into a fail-reason bucket so an operator sees WHY anything is left after a crawl (budget vs a real reach/identity gap). New pure classifier `lib/recon/unreached.mjs` (`buildUnreached` + `renderUnreached`) over (graph, the trail's `act.failed` events, `frontierInstanceStats`); precedence per template: danger-floor-skipped (observed but refused-to-fire → NOT covered) → genuine coverage → unreachable (bucketed by the GRANULAR trail code, which OVERRIDES the graph's coarse `unreachable-coldstart`/`not-visible` reason since the precise code lives only in the trail) → unexplored. Two new trail readers in `trace.mjs`: `latestRunId()` (the lexicographically-largest run dir) + `readActFailed(runId)` (the ordered `act.failed` fire-failures). `report.mjs` gained `--unreached` + `--run=<id>` (JSON + text render), resolving the runId as `BUGHUNTER_RUN_ID` env > `--run` > latest-on-disk. Honesty preserved: `routeCollapse:'pending-INC.3'` NAMES the budget-vs-route-collapse gap the current graph cannot yet split (no location identity beyond routeKey) rather than faking a split, and the `note` refuses to invent a never-discovered count. Factored the shared `oneLine` sanitizer into `lib/core/text.mjs` (both report.mjs and unreached.mjs import it — no cycle, report.mjs stays 173/200; its C0/C1 strip re-expressed as `\p{Cc}`). New unit test `tests/unit/report-unreached.test.mjs` (5 revert-proven FAIL-ON-REVERT guards: trail-code precedence, danger-floor bucket, the two trail readers, the route-collapse tag); report.test.mjs stays green.
- IPv6-loopback trust-boundary fix in the shared-daemon attach path (found while bringing up deps on this macOS box; design Fable, code Opus). `readSessionEndpoint()` (session.mjs) validated the daemon's published wsEndpoint as loopback with a hand-rolled `LOOPBACK.has(host) || host.startsWith('127.')`, but on macOS `localhost` resolves to `::1` first, so `launchServer` publishes `ws://[::1]:PORT` and `new URL(ws).hostname` returns the BRACKETED `[::1]` — never matching the unbracketed `::1` in the set. Result: `attach()` rejected the daemon's OWN endpoint and cold-launched a private chromium per act, silently defeating the one-browser-per-run design (and reding both `tests/live/daemon.test.mjs` cases with `mode:'cold'`). This is exactly the "drift between callers" `host-policy.mjs` exists to prevent — that file already has a bracket-stripping `normalizeHost()`. Fix: new narrow `isLoopbackHost(hostname)` export in `host-policy.mjs` (reuses `normalizeHost`; accepts `localhost`/`*.localhost`, the whole `127.0.0.0/8` range via a proper 4-octet IPv4 literal, `::1`, `0:0:0:0:0:0:0:1`, and IPv4-mapped loopback forms `normalizeHost` flattens to `127.x`); `readSessionEndpoint()` now calls it and the dead local `LOOPBACK` set is removed. Deliberately NARROWER than `isPrivateHost` — RFC1918/CGNAT/link-local/metadata are private but NOT loopback; accepting a LAN host would let a tampered session.json route our pages + typed credentials through an attacker's Playwright server. Also closes a latent hole: the old `startsWith('127.')` string check would have trusted a look-alike hostname like `127.evil.com`. New deterministic (platform-independent) revert-proven guards: `isLoopbackHost` accept/reject sets in `tests/unit/host-policy.test.mjs`, and two `readSessionEndpoint` end-to-end cases in `tests/unit/session-context.test.mjs` (`ws://[::1]:…` trusted, `ws://192.168.1.50:…` rejected → null). Suite 150 → 154 green; both daemon live tests now `mode:'attached'`.
- Built the DRILL-PER-LIST honesty increment — a 50-row data list is 50 instances of ONE template, but the frontier only ever walks (and, pre-increment, only ever COUNTED) instance[0]; the other 49 rows were neither walked nor counted anywhere, silently vanishing from the honest denominator (`cappedRemainder` covered only OPENERS). Now those rows are HONESTLY COUNTED — one representative drilled, the rest flagged `drillSkipped` (the non-opener analog of `cappedRemainder`: counted, flagged, never walked). Additive only, zero identity churn: `dom-snapshot.mjs` emits a per-element `inRow` (sits in a genuine `li/tr/[role=row|listitem]` ancestor — narrower than `rowKey`'s data-id/testid set); `graph-store.mjs mergeSnapshot` stamps a write-once `node.listRow` from any row-resident instance (absent `inRow` → unset, like `hiddenWhenSeen`); `frontier.mjs` adds `DRILL_PER_LIST=1` and accumulates `drillSkipped += N - DRILL_PER_LIST` for a non-opener `listRow` template (mutually exclusive with the opener `cappedRemainder` path via `else if`), returned alongside the byte-identical existing fields; `unreached.mjs`/`report.mjs` surface it on the Instances line. `nextBatch`'s handed-out set and identity minting (`idify` keys on selector+instanceKey only) are UNTOUCHED — `inRow`/`listRow`/`drillSkipped` are never identity inputs. Revert-proven guards: `tests/unit/frontier.test.mjs` (49-drill-skipped, scoping, opener-exclusivity) + `tests/unit/graph-store.test.mjs` (write-once `listRow`; ZERO identity churn via `diffIdentity` — folding `inRow` into the id key reds it). Changed test files green (21) + `report-unreached.test.mjs` green (5).
- First full AUTHED `/recon` run on dev.rawcaster.com (the POST-nav archetype the whole-site-reach work targets) — 20 iterations to a CLEAN DRAIN (0 unexplored). Mapped 4 same-origin routes (`/dashboard` + 3 `/viewprofile/...`), 73 control templates, 25/74 instances walked, 14 endpoints, 37 causal control→endpoint edges (incl. a 2-hop chain: Read Out → POST /nuggetcontentaudio → GET converted_audio.mp3). Validated live: causal token+CDP-initiator POST attribution, multi-route reach, opener-replayable read children, danger-floor (7 mutations refused — never published/deleted/followed), external-link, drill-cap (28-row feed → drill 1 + 4 drillSkipped), honest NOT_VISIBLE at the fixed 1440×900 (mobile-only widgets), the IPv6-loopback daemon fix (`ws://[::1]` used the whole run), and the `--tick` stall fix (no false stall across 20 iterations). Login form is at `/login` (root is marketing); authed app at `/dashboard`. Findings recorded to project memory: the reveal-firewall off-origin over-block (fixed below), pathless modal children on an unstamped-then-stamped POST-opener, and visibility-toggle menu reveals of pre-rendered nodes not captured as new controls.
- Ran the live-session pivot's proof-or-kill and CLOSED its causal blocker — a WebSocket-onmessage attribution hole (design Fable-directed, code Opus; decisions.md). A 5-agent workflow NO-GO'd the "drop per-act re-nav, walk one stateful page" pivot but named ONE causal killer: a fetch fired from a persistent WebSocket's `onmessage` is script-rooted (no timer/parser), so `classifyInitiator` KEEPS it and it inherits the active cause = a phantom edge that re-nav only incidentally hid by tearing the socket down (rawcaster is a WS feed). Empirically confirmed the hole is real AND unfixable at the initiator layer — CDP reports the WS fetch and a click's fetch as the SAME bare `type:script` frame. Fix moved to the probe: `probe.mjs` now wraps the app's own `message` handlers (both `addEventListener('message')` and `onmessage=`) with a `try/finally` `__bughuntWsDepth` counter, `record()` stamps `wsRooted` on fires made while a handler is on the stack, and `selectKept` (causal.mjs) drops `wsRooted` fires as a third filter beside token+initiator+static. The wrap is cached on the listener so `removeEventListener` stays intact, preserves this/args/return, never preventDefault/stopPropagation (observational contract held), and is a prototype-method wrap so `instanceof WebSocket` is untouched. Rejected the first attempt (a passive constructor-added listener + `queueMicrotask` reset) — microtask checkpoints run BETWEEN listeners, so depth was already 0 by the app handler. Revert-proven guard `tests/live/ws-cross-act.test.mjs` (token-only keeps 8× `/api/ws-driven` in #b's window; real kept = only `/api/b`; both levers red on revert). Residual: deferred (setTimeout/await) fetches, object `handleEvent` listeners, and SSE (`EventSource`) are not covered — noted follow-ups. Overall pivot verdict stays NO-GO (2 remaining blockers: live-account mutation ceiling + reproducibility/state-identity — both safety/honesty, not causal). Touches invariant #1 (the causal substrate) — pending cto/bughunter-reviewer + security review before commit. Suite 185 → 186 green.
- Fixed the reveal-firewall off-origin over-block the rawcaster run surfaced (design Fable, code Opus; decisions.md). `replayRevealPath` failed the WHOLE reveal on ANY aborted request, so a benign SAFE-method OFF-ORIGIN sub-resource (a CDN image/font/pixel the revealed UI pulls in) killed the reveal with `REVEAL_WRITE_BLOCKED` — breaking stay-on-page reach on any app with off-origin assets (4 composer/Live-events targets died this way live). Now every block is HARD (a write of any non-safe method, or a danger-route hit of any method — a `/logout` GET stays HARD, M2 preserved) vs SOFT (a safe-method off-origin sub-resource — aborted to prevent the leak, but the reveal PROCEEDS since a GET has no side effect and the revealed state is still reached); `replayRevealPath` fails only on a HARD block. Blast radius contained to the reveal feature (firewall installed only during replay). New revert-proven guard `tests/live/write-firewall.test.mjs` (6) — `#open-offorigin` fires a cross-origin safe GET to a second fixture server: the reveal completes + reveals its child (reach) AND `offHits` stays 0 (leak still prevented); both levers revert-proven, the 5 existing write/H1/M2/L2/reach guards stay green. Suite 184 → 185 green.
- Built the BFS URL route-frontier (Increment 1a node-loop core, whole-site collection — design Fable, `cto`-validated, code Opus). A same-origin `a[href]` harvest + snapshot-ONLY route queue living INSIDE `graph.routes` (`pending`/`pattern`/`siblings`/`unreachable` flags — no second store, no `sitemap.json`) reaches pages a control or the element `OPENER_INSTANCE_CAP` alone never touches (a wide nav's 9th tab, a listing's detail page). New `lib/recon/route-frontier.mjs` (`harvestRoutes` + `seedRoutes` + `nextPendingRoute`/`visitRoute` + `routeFrontierStats`, 138 lines): discovery is phantom-edge-safe BY CONSTRUCTION — it never calls `addTrigger`/`beginCause` and never opens a causal window, so a route node asserts ONLY exists/reachable and navigation adds ZERO edges (request attribution stays token+initiator, never a side effect of loading a page). SEED-THEN-LOOP: `recon-run` BFS-drains the discoverable queue up front (edge-free), then `reconLoop` acts over the seeded template frontier with an injected `refill` hook (`recon-loop.mjs`, backward-compatible — an undefined refill stops on an empty batch exactly as before) draining routes discovered only by acting; `persistentStep` harvests the act's landed page. A url-PATTERN census bound (the ONE existing `toUrlPattern` masker) keeps ONE representative concrete route per pattern — a 50-link listing is 1 visit + 49 siblings folded, never 50. Every gate reused with NO bypass: `routeRefused` (never navigate `/logout`), `sameOrigin`/`isOffOriginHttp` (scope), `navigateGated` (a new SSRF-gated sibling of `gotoGated` that RETURNS the nav Response for the 404/redirect reachability check — `gotoGated` now delegates to it, so no caller breaks), `routeKey`+`toUrlPattern`. A harvested URL that 404s/redirects is `markRouteUnreachable` (counted discovered-but-unreachable, never dropped/covered); `frontierStats.routes` now counts only NON-pending (snapshotted) nodes; `report` renders a route-frontier line. New revert-proven live guard `tests/live/route-frontier.test.mjs` (5 guards over a `wide-nav-app` + off-origin fixture: wide-nav reach, census bound, danger-route refusal, off-origin refusal, ZERO-phantom-edge via a seed-only sub-test + a background-beacon poll). Suite 186 → 188 green.

## 2026-07-17
- Fixed a route-coverage OVER-COUNT caught by independent live verification (honesty of the coverage denominator, invariant #3; code Opus). On a live rawcaster run `report --route-coverage` claimed "61 of 61 declared sections reached · 0 unreachable", but an out-of-band audit proved only 19 of the 61 rendered their OWN content — 42 were empty (~4 SAME-ORIGIN-redirect to /dashboard, ~4 render Not-Found). ROOT CAUSE: `visitRoute` read its redirect guard's `landed = page.url()` at `navigateGated`'s `domcontentloaded`, so it caught an HTTP-302 (goto follows it) but MISSED a client-side React-Router redirect firing DURING `waitSettled` — the URL was rewritten to /dashboard AFTER the guard passed, and /dashboard's DOM was snapshotted under /events. FIX 1 (`route-frontier.mjs visitRoute`, +11 lines): after `waitSettled`, re-read `page.url()` and re-apply BOTH guards (off-origin → `redirect-offorigin`; same-origin different route → `redirect`) — additive to the pre-settle checks; visitRoute stays snapshot-only/edge-free, no double-attribution, no new import. FIX 2 (`route-coverage.mjs`): `routeCoverageOf` now splits visited into COLLECTED (≥1 element with `el.route===rk`) vs VISITED-BUT-EMPTY (0 controls); `renderRouteCoverage`/`routeCoverageLine` lead with "collected (own content)" and list both the empty and unreachable sections (gap visible, never hidden) — the old "all reached" headline is gone; `reached` kept for back-compat. The `onclick-spa` fixture gained `/redir` (client `replaceState` to `/` after a `/slow` fetch — fires only post-settle, so the pre-settle guard cannot catch it) + `/empty` (0 controls). New revert-proven test (`tests/live/route-manifest.test.mjs`): a same-origin redirect → unreachable (not reached), an empty page → visited-but-empty (not collected), collected == own-content routes only; FAIL-ON-REVERT — neutralize the POST-settle re-read → "a same-origin redirect must not count as a reached section" reds; collapse the empty bucket → "a visited-but-empty section must not count as collected" reds. Suite 213 → 214 green.
- Wired the BFS URL route-frontier into the AGENT PATH (Increment 1b — design Fable-directed, code Opus), so whole-site route collection now works on the path the OPERATOR runs (`/recon`, Sonnet-driven), not only the node-loop (`recon-run`). `whats-new` now calls `harvestRoutes(page, graph, origin)` at the SAME two `__idle__` points `persistentStep` uses — after the baseline `snapshotStep` (no `beginCause` yet; the overlay accept was `resetTrackerVerdicts`'d; the snapshot is read-only) and after the act's `endCause` has reset the cause to `__idle__` — so the harvest is metadata-only + edge-free (writes `graph.routes[*].pending` flags, never an element or a causal edge). New `lib/recon/route-cli.mjs` (`--visit-next`, 109 lines): the deterministic route-visit DRIVER the `/recon` loop calls BETWEEN agent acts — attach → `visitRoute(page, graph, ledger, nextPendingRoute(graph), {origin})` → persist → print `{visited, routeStats}`; a null `nextPendingRoute` is an idempotent no-op that acquires no browser. The LLM subagent keeps ACTING (`whats-new`); the driver NAVIGATES routes, snapshot-only, so the agent never runs its own BFS. `frontier-cli --emit` now surfaces `pendingRoutes` + `routeFrontierStats` in its envelope and threads `pendingRoutes` into the verdict; `decideProgress` gained a `pendingRoutes` input (default 0, backward-compatible) and a new precedence: `batchLen===0 && pendingRoutes>0` → `action:'visit-route'` (drain a queued page), `'drained'` ONLY when BOTH the template frontier AND the route queue are empty — the honest whole-site terminal. `/recon` updated: on a `visit-route` verdict the driver runs `route-cli --visit-next` then re-emits, looping under the same 200-iteration runaway ceiling until a true `drained`. Reused every gate with no bypass (`routeRefused` danger, `navigateGated` SSRF, `sameOrigin`/`isOffOriginHttp` scope, `routeKey`/`toUrlPattern` identity), and `Explored ⟺ observed` holds (`visitRoute` snapshots/discovers but NEVER marks an element explored — only an act does). New revert-proven guards: `tests/unit/loop-control.test.mjs` (empty batch + pending routes → `visit-route`, not `drained`), `tests/unit/frontier-cli.test.mjs` (emit envelope carries `pendingRoutes`/`routeFrontierStats` and yields the `visit-route` verdict), `tests/live/agent-route-frontier.test.mjs` (agent-path baseline harvest → `pendingRoutes>0`; `route-cli` snapshot-visits a beyond-element-cap page (`/p9`) so `only-p9` is discovered + reachable; the whole drive forges ZERO edges — all over the reused `wide-nav-app` fixture). Suite 188 → 191 green.
- Fixed a security finding (HIGH H1) in the route-frontier surfaced by the pre-commit `security-reviewer` pass (code Opus). `visitRoute` (`route-frontier.mjs`) followed redirects via `navigateGated` but its only post-navigation guard was `routeKey(page.url()) !== rk`, which is PATH-ONLY (`scope.mjs` strips host/scheme) — so a hostile crawl target could `302`-redirect a harvested same-origin href to a private/foreign host on the SAME path and get the internal response `snapshotStep`-captured into `graph.routes[rk]` (the act path already guarded this with `sameOrigin(landedUrl)` at `step.mjs:175`; the new snapshot path had dropped it). FIX: before snapshotting, re-check `!sameOrigin(origin, landed) || isOffOriginHttp(origin, landed)` → `markRouteUnreachable(rk, 'redirect-offorigin')`, so an origin-leaving redirect is refused and its content never captured (the blind GET already fired — the accepted DNS-rebind residual — but capture is now prevented). Revert-proven guard (6) in `tests/live/route-frontier.test.mjs` (a same-path off-origin 302 sink: the redirect genuinely fires — `extHits>=1` — yet `/redir-evil` is `unreachable:'redirect-offorigin'`, never visited; dropping the re-check reds it). Full `security-reviewer` verdict: 0 critical / 1 high (this, fixed) / 1 medium (M1: deterministic auto-GET of harvested routes is gated only by the ENGLISH danger-floor regex with no LLM behind it — a non-English destructive-GET could fire under an authed crawl; documented residual, see decisions.md + task #9) / 2 low. Suite 191 → 192 green.
- Built the LOCATION-AWARE stateful in-session backtracking driver (Increment 2, the per-location frontier + backtracking driver — design Fable-directed, code Opus). New `lib/recon/stateful-loop.mjs` (`statefulLoop`, 137 lines) is the stateful twin of the location-BLIND `recon-loop.mjs` (left byte-untouched): it implements the operator's actual model — ONE session opened ONCE, walked page-to-page IN-SESSION with per-location memory of what is left, backtracking to pages with unfinished controls until every route's remainder hits zero. It DRAINS the current route to fixpoint (act every unexplored candidate whose `node.route === routeKey(page.url())` AND that RESOLVES on the live DOM, re-evaluating after each act so reveals are picked up), FOLLOWS a nav act to the landed page (`routeKey` change → drain the new page — the natural "went to page 2" move), and when the current route is drained BACKTRACKS via `gotoGated` (SSRF-gated, in-session, session/cookies preserved — NOT the stateless per-act cold reset) to the lowest-keyed route still holding work, snapshots it, and keeps draining. The blind spot INC.1's `statefulStep` had is closed: `reconLoop` handed statefulStep any unexplored template regardless of the live route (cross-page targets failed NO_INSTANCE); the location-aware selection + in-session navigation fixes it. Wired into `recon-run.mjs crawl()`'s `--stateful` branch (replacing the `reconLoop` call with `statefulLoop`, still `statefulStep` as the injected step) — the DEFAULT stateless path stays byte-unchanged (opt-in). Termination is triple-bounded: natural (empty routesWithWork everywhere = the zero-new oracle → `frontier-drained`), a per-route MAX_REVISITS=3 stall guard (a no-progress route is retired → `stalled`, its remainder left honestly `remaining`), and MAX_BACKTRACKS=500 / maxActs runaway backstops (never a wall-clock ceiling). HONEST: a control that never resolves even after a fresh nav to its own route is `markInstanceUnreachable` (drained, never counted covered) — a control behind a now-collapsed reveal on a backtracked page is honest-unreachable-for-now (reveal-replay recovery on backtrack is the named follow-up). Causal discipline preserved: statefulStep owns the ONLY causal window; the driver's gotoGated + snapshotStep run under `__idle__` and are edge-free, so attribution is unchanged (invariant #1 untouched). New revert-proven live guard `tests/live/stateful-backtrack.test.mjs` over a new `tests/fixtures/backtrack-app` (page /a: cA1→GET /api/a1, a /b nav, cA2→POST /api/a2 slow + a 100ms background poll; page /b: cB→GET /api/b, no link home) — the greedy walk acts cA1 then the /b nav (leaving cA2), drains cB, and ONLY the driver's backtrack to /a finishes cA2: asserts all three explored + causally attributed, cA2 fired AFTER cB (non-vacuous backtrack), the loop drained frontier-drained, and the in-window background poll is never credited; FAIL-ON-REVERT (force the backtrack pick to null → cA2 stranded → "cA2 was explored" reds). Suite 194 → 195 green.
- Built DURABLE-LOCATOR act-time instance resolution (the "resolveHandle" layer — Increment 2 follow-up, code Opus), the fix that makes the stateful walk actually work on a DYNAMIC page. On a live authed rawcaster stateful smoke the loop drained at 7 explored vs 31 for the stateless path: the stateful step resolved a control by its STORED POSITIONAL nth-child selector (`instanceSelector`), and rawcaster's feed reshuffles WHILE the walk stays on the page, so the selector went stale → `resolvesLive=false` → `retireLeftovers` prematurely marked the control unreachable. New `lib/recon/resolve-handle.mjs` `resolveHandle(page, inst, node)` re-locates a LIVE handle when the positional path fails: stored selector (via `selector`) → page-unique testid / stable id (via `id`) → role+name first-visible match (via `role-name`, `representative:true`). Wired at the SHARED resolution point in `actStep` (`step.mjs`) so BOTH modes benefit; when a REPRESENTATIVE is acted the danger-floor + off-origin PRE-CLICK guards RE-RUN on the live handle (its name may differ from the stored one), and the acted instance carries an honest `viaRepresentative` marker (surfaced by `report`) so the template reads covered without claiming that specific vanished instance was individually tested. `stateful-loop.mjs`: `resolvesLive` uses `resolveHandle`; `retireLeftovers` softened to mark unreachable ONLY after `resolveHandle` fully fails, never on a merely-stale nth-child. Reuses the existing durable `locator` classification — zero identity re-key, no new normalizer; causal attribution untouched (resolveHandle opens no window). Guarded by `tests/live/resolve-handle.test.mjs` (a feed that reshuffles after baseline; revert-verified). Full suite 196/196 green.
- Built OVERLAY-AWARE stateful acting (code Opus), the real fix for the stateful walk regressing on a MODAL-HEAVY site. On the live authed rawcaster stateful run the loop drained at 7 explored (vs 31 stateless) with 17 of 35 unreachable being `elementHandle.click: Timeout 5000ms exceeded`: the controls RESOLVED fine, but an earlier act had opened a MODAL/backdrop that OBSCURED the rest of the page, so every sibling behind the overlay click-timed-out → mass unreachable → premature drain. A STATE-MANAGEMENT gap (open/close), not a resolution one (durable-locator had already been added and did not help). New `lib/recon/overlay-dismiss.mjs` `dismissBlockingOverlay(page)`: run under `__idle__` (no `beginCause`), press Escape + click a curated close-affordance / known backdrop (`[aria-label=Close i]`, `.ant-modal-close`, `.modal-close`, `[data-dismiss]`, `.ant-modal-wrap` …), overlay/dialog-class only, off-origin anchor refused, never navigates; returns whether the blocking-overlay signature actually dropped. Wired STATEFUL-ONLY (stateless `persistentStep` untouched): (1) `stateful-step.mjs` — a RAW click failure (no `.envelope`, i.e. an obscured-click timeout, unlike our structured NO_INSTANCE/NOT_VISIBLE/DANGER_FLOOR) triggers `dismissBlockingOverlay` + `resetTrackerVerdicts` + a SINGLE retry of the SAME target (bounded: dismiss returning false → no retry → honest unreachable); (2) `stateful-loop.mjs` `drainRoute` — when no candidate resolves, dismiss-and-recheck ONCE before concluding the route drained (an open modal was masking siblings; only "dismiss changed nothing" is drained, so no loop). Causal attribution unchanged — the dismiss opens no causal window, forges no edge, and does not perturb the next act's window. New revert-proven guard `tests/live/overlay-acting.test.mjs` on new fixture `tests/fixtures/modal-block-app` (A opens a full-screen backdrop modal obscuring B; the loop studies the modal control M, closes the modal mid-walk via Escape, reaches B with its GET causally attributed; the in-window poll is still dropped at the modal depth; the dismiss forges no phantom request). Full suite 196 → 198 green.
- Built the READ-ONLY MUTATION FIREWALL (now PROVEN mandatory — a live authed stateful rawcaster run fired `POST /rawcaster/followandunfollow`, a real follow/unfollow, because the English name-based danger-floor missed the icon control; code Opus, cto + prior security review flagged this exact gap). New `lib/recon/read-only-firewall.mjs` (99 lines) is a SESSION-WIDE `page.route` write-firewall (distinct from the per-replay `reveal-firewall.mjs`): a read-only crawl is now STRUCTURALLY unable to commit a write on a live account WITHOUT aborting reads-over-POST (rawcaster's `listnuggets`/`getothersprofile`/`listevents`/`get_ad`/`getfollowlist` still fire, or content never loads — so a blanket "abort all non-GET" is wrong). The handler classifies each request THREE ways: (a) GET/HEAD/OPTIONS → continue; (b) a non-GET whose `toUrlPattern` PATH matches `WRITE_VERB_RE` (a curated mutation-verb regex, LEADING word-boundary so a compound segment like `followandunfollow` matches `follow` while `getfollowlist`'s glued `follow` does NOT) → ABORT + record `write-verb`; (c) any other non-GET → CONTINUE + record `non-get-allowed` (the read-over-POST + the honest benign-named-write residual, surfaced so the operator sees every write-shaped request NOT blocked). Aborting is at the NETWORK layer AFTER the probe recorded the fire and CDP saw `requestWillBeSent`, so the causal control→endpoint edge STILL records (the API map survives the abort — a blocked write shows in the map with a null status); only the server side-effect is prevented. Wired into `recon-run.mjs crawl()`: installed for the whole session when `--read-only` OR `--stateful` (default-ON for stateful — the exact live-mutation path), the default STATELESS path stays OFF (behavior unchanged); the run result surfaces `readOnly:{writeVerbBlocked,nonGetAllowed,refusedPatterns}` (would-be-mutations refused). Additive `mutationFloor(name)` in `danger-floor.mjs` (the SINGLE mutation-verb vocabulary `MUTATION_VERBS` lives here, dependency-free; the firewall imports DOWN to it) is the NAME-level twin, opt-in at click time via `actStep`'s new `refuseMutations` (set by the read-only `persistentStep`) — it covers the complement the URL-path gate cannot see (a mutation-NAMED control firing a benign-named endpoint), while an ICON control (no name → `unknown`) is NOT refused and falls through to the network gate (map preserved). It does NOT widen the always-consulted `REFUSED` set, so a benign `Follow`/`Like` on the default crawl is unaffected. New revert-proven live guard `tests/live/read-only-firewall.test.mjs` (a benign-named W fires `POST /api/followandunfollow` → ABORTED, `writeHits`=0, YET the W→endpoint edge records + `blocked` surfaces it; a read R fires `POST /api/listthings` → ALLOWED, `readHits`>=1; a mutation-named F fires benign `POST /api/x123` → refused at click under `refuseMutations` [`MUTATION_FLOOR`, `xHits`=0], and WITHOUT the name gate the URL-path firewall ALLOWS it [`xHits`=1, `non-get-allowed`] — the documented residual) + unit `mutationFloor` cases in `tests/unit/danger-floor.test.mjs`; both levers revert-proven (drop branch b → `writeHits`>0; drop the `refuseMutations` gate → F no longer rejects). Suite 198 → 200 green.
- INVERTED the read-only WRITE-FIREWALL to ABORT-BY-DEFAULT (CTO blocker-1, code Opus) — the safety-critical fix for the residual that let a benign-named write reach a live authed account (a live rawcaster run fired `POST /rawcaster/followandunfollow`; the OLD branch (c) CONTINUED any non-GET without a write verb in its path). `read-only-firewall.mjs` (99→125 lines) now classifies a non-GET four ways: (a) write-verb path → ABORT (`write-verb`, survives the operator override); (b) `reqKey` in the AGENT-JUDGED read-allowlist → CONTINUE (`read-allowed`); (c) OPERATOR OVERRIDE `--allow-benign-post` → CONTINUE (`non-get-allowed`, the old branch-c reach on a TRUSTED target, default OFF); (d) DEFAULT → ABORT (`write-blocked`). GET/HEAD/OPTIONS still continue. The allowlist is the ONLY widen: new `lib/recon/read-allowlist.mjs` (61 lines) reads/writes `state/read-allowlist.json` (`{ "POST /rawcaster/listnuggets": "read" }`, keyed by `reqKey`, honors `BUGHUNTER_STATE_DIR`) — `loadReadAllowlist` returns ONLY POST reads (a hand-edited `DELETE …: read` cannot open the gate; GET/HEAD/OPTIONS already pass). New `lib/recon/judge-endpoint.mjs` (76 lines, modeled on `observe.mjs`) is the thin agent CLI `--endpoint='POST /…' --class=read|write`: a SEMANTIC read/write verdict → the agent (model split), file-only, no graph/browser touch; a `read` opens the gate (POST only), a `write` is recorded but the gate stays closed. An aborted write STILL records its causal control→endpoint edge (the probe fire + CDP `requestWillBeSent` both precede `route.abort`; `actStep` `addTrigger` unchanged) — the API MAP survives, so the agent can judge from it. Requirement 4 (defense in depth): `statefulStep` now passes `refuseMutations: readOnly` into BOTH `actStep` calls (a Follow/Like/Delete-named control is refused at click time before its request fires, matching `persistentStep`). `--allow-benign-post` is OPERATOR-ONLY (parsed in `recon-run` argv, never settable by the agent). New/updated revert-proven guard `tests/live/read-only-firewall.test.mjs` (a benign-named `POST /api/dostuff` is ABORTED by default with an EMPTY allowlist — dostuffHits=0 — yet its causal edge records; once `judge-endpoint` allowlists it as a read the SAME act CONTINUES; a read-over-POST `POST /api/listthings`, once allowlisted, continues; the name gate refuses "Follow" at click; the benign `/api/x123` residual is CLOSED by default and re-opened ONLY under `--allow-benign-post`; the write-verb gate aborts even under the override) — levers: revert branch (d) to continue → dostuffHits>0; drop branch (b) → write-verb continues under the override; drop the `refuseMutations` gate → "Follow" no longer rejects. Blast radius (two --stateful fixtures adapted, documented): `stateful-app`'s reach control renamed "Save"→"Details" (a --stateful crawl is read-only, so its name gate now refuses a write-verb-named control — the depth-1 REACH fixture must isolate reach from that gate); `stateful-backtrack` marked a TRUSTED target via `--allow-benign-post` (it guards backtracking, not the firewall — its benign `POST /api/a2` is now aborted-by-default). Suite 200/200 green.
- Built STATEFUL REVEAL-PATH PROVENANCE stamping (Blocker-4 — location honesty / Phase-2 handoff, code Opus). The stateful in-session walk (`statefulStep`) reaches a modal/dropdown/panel control by ACCUMULATED state but never stamped a `reveal.statePath` on it, so `locationKey` (derived from `inst.reveal.statePath`) collapsed EVERY stateful-discovered control to `locationKey===route` — on a constant-URL SPA (rawcaster: onClick nav, URL never changes) the report showed ONE location for the whole app and `frontierInstanceStats.locations.discovered` read 1. Fix (additive; the default STATELESS path is byte-for-byte identical): `actStep` gained a `statefulProvenance` option that widens the reveal-stamp gate METHOD-AGNOSTICALLY (drops the `allGet || openerReplayable` requirement) — safety of any future replay is delegated to the read-only firewall (default-ON in stateful mode), and the stamp is marked `stateful:true` so a consumer tells a provenance breadcrumb from a replayable stateless path. `mergeSnapshot` threads `opts.stateful` into all THREE reveal-write sites (new-template, new-instance, and the panel-reach fill) so the marker rides the INSTANCE reveal too (the rawcaster nav-swap control has no node.reveal — its template already exists). `statefulStep` accumulates the breadcrumb in its factory closure: a per-route `chain` of opener hops (`{templateId, instanceKey}`, matching `revealPathFor`), reset when the live route changes, pushed after each in-place act — so a constant-URL SPA accumulates the WHOLE session (restoring distinct locationKeys) while a multi-URL app resets per navigation. `stateful-loop.mjs` and all causal/danger/firewall code untouched; `location-key.mjs` needed NO change (it already derives from `statePath`). Accepted residual (documented): the breadcrumb is ORDER-DEPENDENT and an over-approximation — a control reachable by two accumulated paths records only the first, and the chain is every in-place act since nav, not a minimized path; it is provenance, not a replayable route. New revert-proven live guard `tests/live/stateful-location.test.mjs` over a new `tests/fixtures/stateful-location-app` (a 2-deep reveal chain A→B→X on ONE URL, openers firing read-over-POST so `allGet` is FALSE — proving the method-agnostic widen): X sits behind `[A,B]` (statePath length 2, last hop its opener B), B behind `[A]`, `locations.discovered===3` (was 1), GET /api/info attributed to X, the poll never credited; three FAIL-ON-REVERT levers each red a distinct assertion (drop the `statefulProvenance` gate → no stamp; revert the `stateful:true` marker; delete `chain.push` → statePath length 1). Updated `tests/live/stateful-step.test.mjs` guard (b) (stateful mode now stamps a `stateful:true` provenance reveal, no longer asserts `reveal===undefined`). Suite 201/201 green.
- Closed the SSE / EventSource causal hole (Blocker-3, the symmetric follow-up to the WebSocket fix; code Opus, invariant #1). `probe.mjs` instrumented fetch+XHR+WebSocket but NOT EventSource: a fetch fired from an EventSource handler is script-rooted (no timer/parser/wsRooted), so `classifyInitiator` KEEPS it and it inherits `window.__bughuntCause` on a reused stateful page = a phantom causal edge (the class that killed bughunt-agents; on a long-lived authed run the SSE stream stays open the whole crawl). Mirrored the WS wrap for `EventSource.prototype`, REUSING the existing `__bughuntWsDepth` counter and `wsRooted` fire field (no parallel counter; `causal.mjs selectKept` untouched — an SSE handler on the stack stamps `wsRooted:true` and is dropped by the SAME filter). Hoisted the shared `wrapWs` depth-wrapper to IIFE scope (both blocks reuse it; also required so the ES block never references an undefined `wrapWs` when `window.WebSocket` is absent). SSE-specific correctness: the ES `addEventListener` wrap covers EVERY function listener type, NOT just `'message'` — per WHATWG HTML "server-sent events" / MDN, a server event with an `event:` field dispatches to a NAMED listener (`addEventListener('<name>')`), never `message`, so a `'message'`-only wrap (the WS restriction) would miss every named SSE data event; over-covering `open`/`error` is harmless (no control fetch runs inside an SSE dispatch). The `onmessage=` setter is wrapped too. New revert-proven live guard `tests/live/sse-cross-act.test.mjs` + fixture `tests/fixtures/sse-feed-app/` (an EventSource pushing an unnamed `data:` frame AND a named `event: feed` frame every ~80ms inside #b's ~600ms window): token-only keeps 9× `/api/sse-driven` + 9× `/api/sse-driven-named` (adversarial non-vacuous), real kept = only `/api/b`; lever A (`if (false && ESP)`) reds the message-driven assertion, lever B (restrict the ES wrap to `type === 'message'`) reds the NAMED-event assertion (the all-types proof). WS path behavior-unchanged (`ws-cross-act` still green). Residuals (identical to WS): `onopen`/`onerror` setters, object `handleEvent` listeners, and deferred (`setTimeout`/`await`) fetches out of the handler. `probe.mjs` 166→221 lines (over the 200 guideline; the causal init-script is one indivisible injected string that must not be split — a shared WS+ES installer is a reviewer-optional follow-up). Suite 201 → 202 green. Not committed — joins the uncommitted causal-substrate tree awaiting the consolidated review.
- Built the CHURN bucket + typed terminal for honest termination on a RE-RENDERING FEED (blocker-6 Part B, code Opus; Part A — the monotone-progress false-STALL fix — was already built and is untouched). PROBLEM: `instanceKey` is CONTENT-derived (`dom-snapshot rowKey`: nearest row's data-id, else its text), so a feed WITHOUT stable data-ids mints a NEW instanceKey each render; an UNEXPLORED representative that churns away before it is walked stays in the graph un-drainable → `frontierInstanceStats.remaining` never reaches 0 → the honest terminator can never declare DRAINED on a live feed (the rawcaster archetype), and the vanished row was marked plain `unreachable`, conflating feed-churn with genuine gaps. FIX (two additive parts): (i) `markInstanceChurned` (`graph-store.mjs`, write-once `inst.churned`, does NOT set `unreachable` OR `explored`) + a `churnSkipped` bucket in `frontierInstanceStats` that PEELS churned instances out of `walkable`/`remaining` (mirrors `drillSkipped`: counted, flagged, never walked) — so `remaining = walkable − walked − unreachable` reaches 0 on the STABLE control set while the churn is QUANTIFIED; `nextBatch` gained `inst.churned` to its drain predicate (a churned instance is the frontier-drain signal, staying UNEXPLORED so the peel is not net-neutral to `remaining`). `stateful-loop.mjs retireLeftovers` reclassifies a vanished LIST-ROW candidate (`node.listRow===true`) CHURNED instead of UNREACHABLE; a NON-listRow vanished control stays honestly unreachable. (ii) A typed terminal in `decideProgress` (`loop-control.mjs`): DRAINED carries the full residual and releases only when the stable set is drained AND `churnSkipped` growth has FLATTENED over K windows (a window count, clock-free — while churn still GROWS the verdict is 'continue', never declaring done while the feed is still spawning fresh churn). `frontier-cli.mjs` threads `churnSkipped` + `churnHistory` (new `trace.mjs readFrontierChurn`, mirroring `readFrontierProgress`) into the verdict. DEVIATION from the literal spec (documented in decisions.md): churned instances are deliberately left UN-explored (the `inst.churned` predicate drains the frontier), because marking them explored would make the peel net-neutral to `remaining` and vacate the required FAIL-ON-REVERT. AGENT PATH (`/recon whats-new`) unaffected — churnSkipped rides through frontier-cli/decideProgress as 0 by default; agent-path churn-marking is a documented follow-up. New revert-proven guards: `tests/live/churn-feed.test.mjs` + `tests/fixtures/churn-feed-app` (a feed re-renders after baseline — the representative "News 0" churns away, its positional selector proven stale — so the stable set drains to remaining===0 with churnSkipped===1 while a vanished NON-listRow #ghost stays unreachable; GET /api/status attributed to #stable, the in-window poll never credited; two levers red — remove the peel → remaining===1; drop the listRow branch → churnSkipped===0), plus unit guards in `tests/unit/frontier.test.mjs` (the peel arithmetic) and `tests/unit/loop-control.test.mjs` (the churn-flat release gate). Line counts: `trace.mjs` now 224 and `graph-store.mjs` now 291 (both over the 200 guideline — graph-store was already over pre-change; per the task neither is split). Suite 202 → 206 green. Not committed.
- Closed the read-only WRITE-FIREWALL gap on the LIVE `/recon` path + three related holes (security C1/H1/H2/M2, code Opus). A prior live authed crawl FIRED a real `POST /rawcaster/followandunfollow`; the security review found the firewall protected `recon-run` but was ABSENT on the `/recon` → `whats-new` path the operator actually runs. (C1) `whats-new.run()` now computes `readOnly` DEFAULT-ON when authenticated (`!!opts.readOnly || !!process.env.BUGHUNTER_STORAGE_STATE` — a storageState-authed crawl is always write-protected) and, when read-only, installs `installReadOnlyFirewall` on `page` immediately after `attach()` and BEFORE `gotoGated` (spans the whole short-lived process; torn down in `finally` before `release()`), threads `refuseMutations: readOnly` into its `actStep` call (the NAME-level Follow/Like/Delete gate), and surfaces `result.blocked = summarizeBlocked(...)` (the operator sees what was refused); `--read-only`/`--allow-benign-post` wired through `main`→`parseArgs`→`run`. On a FRESH authed run the read-allowlist is empty so POST-reads are also aborted until the agent judges them via `judge-endpoint` — intended multi-pass design; agent-path allowlist POPULATION into the `/recon` loop is a documented follow-up. (H1) `step.mjs actStep` gained a same-origin DANGER-ROUTE href gate — right after the off-origin check, `if (href && routeRefused(routeKey(href))) throw DANGER_FLOOR` — so an icon-only `<a href="/logout">` or `/item/42/delete` with no accessible name (which slips the name-floor and, as a GET, would navigate/delete on click) is refused BEFORE the click on EVERY actStep caller (persistentStep + whats-new + statefulStep). (H2) `recon-run.mjs crawl()` couples `readOnly` to authentication too (`|| !!process.env.BUGHUNTER_STORAGE_STATE`) — an authed run is read-only flags-or-not, so the operator can never forget the flag on a live account. (M2) `session.mjs contextOptions()` adds `serviceWorkers: 'block'` to the single context builder — Playwright's `page.route` does NOT intercept Service-Worker-originated requests, so a SW background-sync POST would bypass the firewall; blocking SWs closes that hole at BOTH newContext sites. The default STATELESS unauthenticated path is unchanged (`readOnly` false → no firewall installed → byte-identical); the causal control→endpoint edge survives every network abort (map preserved). New revert-proven guards: `tests/live/authed-firewall.test.mjs` (C1 whats-new: benign POST aborted/dostuffHits 0 + edge survives, Follow refused MUTATION_FLOOR/xHits 0; H2 recon-run env coupling: firewall installed from env alone, writeHits 0 — reuses `read-only-app` fixture), `tests/live/logout-link-gate.test.mjs` + new `tests/fixtures/logout-link-app` (H1 icon-only `<a href="/logout">` refused DANGER_FLOOR, logoutHits 0), and a unit assertion in `tests/unit/session-context.test.mjs` (M2 `serviceWorkers:'block'`); five levers each red a distinct assertion. Line counts: `whats-new.mjs` 246, `step.mjs` 278, `recon-run.mjs` 280 (all pre-existing over the 200 guideline — not refactored per task). Documented residuals: agent-path read-allowlist population (`judge-endpoint` wired into `/recon`) for later-pass POST-reads; M1 (a WS/SSE write channel bypasses the HTTP `page.route` firewall). Suite 206 → 210 green. Not committed.
- Wired READ-ALLOWLIST POPULATION into the authed `/recon` crawl — the productive half of the abort-by-default read-only WRITE-FIREWALL (code Opus). The firewall keeps a read-only authed crawl STRUCTURALLY unable to commit a write, but on an app whose CONTENT loads over POST-READS (rawcaster `listnuggets`/`getothersprofile`/`listevents`/`getfollowlist`) a FRESH run with an empty allowlist rendered SPARSE — those reads were aborted too, and nothing consumed the firewall's abort ledger, so the safe default was unproductive on the exact live target it protects. Now the crawl loads that content PROGRESSIVELY: (Part 2) `.claude/agents/recon.md` gains a step 5 READ-ALLOWLIST POPULATION — after each act the recon agent reads `result.blocked.refusedPatterns`, judges which aborted POSTs are READS (list/search/get/detail/feed) vs mutations (create/update/delete/save/like/follow/vote/pay/subscribe — same taxonomy as `--opener-replayable`), and runs `judge-endpoint --class=read` for the READS ONLY so the NEXT `whats-new` navigation (a fresh process → re-reads the allowlist at install) continues them; the ACT step's return doc now notes the top-level `blocked` field, and the step warns content loads PROGRESSIVELY so an early SPARSE frontier is NOT "done". (Part 3) `.claude/commands/recon.md` gains a POPULATION WARM-UP (step 6b): on an authed run whose baseline carries a non-empty `blocked.refusedPatterns`, the driver invokes the recon subagent ONCE to judge the baseline's blocked POST-reads (reads only, never writes), THEN re-runs the baseline `whats-new --url=<url>` so the now-allowlisted content loads and seeds the frontier; the loop (step 7c) notes the authed frontier GROWS across early iterations as content loads (the typed terminator's monotone-progress + churn signals handle "still discovering" vs "stalled" — an early small frontier is not drained); the Authenticated-mode section (item f) documents the crawl is READ-ONLY write-protected (firewall DEFAULT-ON when `BUGHUNTER_STORAGE_STATE` set) with POST-reads loading progressively via allowlist population. The read/write judgment stays in the AGENT (model split); the driver only orchestrates. (Part 1) Deterministic MECHANISM proof independent of the LLM — new `tests/fixtures/post-read-app` (main content over `POST /api/listitems`, a benign `POST /api/dostuff` write, a static control) + new `tests/live/read-allowlist-multipass.test.mjs`: pass 1 `whats-new.run({readOnly:true})` on an EMPTY allowlist renders SPARSE (item controls ABSENT, static control present, `result.blocked.refusedPatterns` carries `POST /api/listitems`) → `judge('POST /api/listitems','read')` → pass 2 CONTINUES the read and the item controls LOAD into the graph, while the never-judged `POST /api/dostuff` stays ABORTED (dostuffHits 0, surfaced refused). FAIL-ON-REVERT: skip the `judge(...)` call → pass 2 stays sparse → "the allowlisted read loads content on the next pass" reds. No causal/churn/terminator/firewall-gate logic touched; the default unauthenticated path is byte-unchanged (`blocked` absent → warm-up skipped). Suite 210 → 211 green. Not committed.
- ROUTE-MANIFEST SEEDER — turn the honest route denominator from the ~1 an a[href] harvest finds on a constant-URL onClick SPA into the ~N the app's OWN router declares, and collect the deep-linkable sections (Increment #1 of the 2026-07-17 whole-site-is-~7% investigation; code Opus). THE INVARIANT BEND (already an ADR): static analysis of the app's declared route LIST is allowed ONLY for the DENOMINATOR + CANDIDATE SEEDING — never for CLAIMING coverage (the anti-death-mode of the old static "brain": the brain HID the gap behind static confidence, this EXPOSES it). New `lib/recon/route-manifest.mjs`: `extractRoutes(page)` fetches the page's SAME-ORIGIN JS bundles via `page.request.get` (rides the same auth, SSRF boundary = same-origin only; cross-origin CDN scripts never fetched; ≤6 bundles, ≤15 MB each), REGEXES (never eval's) ONLY the React-Router config KEY `path:"<value>"` (absolute OR a v6 RELATIVE nested value normalized with a leading `/`), and returns `{routes, paramRoutes}` (static navigable paths vs `:param` patterns; `*` splat + asset + minified-`path:`-expression fragments filtered). PATH-KEY PRECISION (coordinator's independent live-bundle catch): a bare leading-slash string-literal fallback was DELIBERATELY NOT used — on the real 8.6 MB rawcaster bundle it inflated the denominator with 47 API-path/socket.io/redux-action fragments (`/accept` `/count` `/status` `/token` `/reactions` … — the static-brain inflation failure mode); measured, the `path:`-only extractor returns 83 CLEAN routes (60 static + 23 param) with zero noise, all real sections present (`/events` `/earnings` `/setting` `/membership` `/profile` `/nuggets` `/worship` `/sermons` …). `seedManifestRoutes(graph, routes, origin)` mirrors `route-frontier.harvestRoutes`' node shape EXACTLY + an additive `declared:true` flag (reporting/denominator only, never an identity input), skipping already-known and danger (`routeRefused`) routes; `seedParamPatterns` counts `:param` patterns as `unreachable:'param-pattern'` (never navigated). Metadata-only — no `addTrigger`, no causal window, no elements/edges (phantom-edge-safe by construction, exactly like `harvestRoutes`); every seeded route is still GENUINELY visited by the UNCHANGED `route-frontier.visitRoute` (or `markRouteUnreachable` on redirect/404), so coverage is claimed only from real visits. Wired into BOTH crawl paths: `recon-run.mjs` seed phase (default-ON, `--no-seed-manifest` to disable) and a new `route-cli.mjs --seed-manifest` action + a `/recon` step 6c. New `lib/recon/route-coverage.mjs` + `report.mjs --route-coverage` — the EXACT reached/declared route oracle (`reached of N declared sections · Y unreachable (redirect/404) · Z pending` + the LIST of not-reached declared sections), and a `Declared sections: X/N reached` line in the normal header so a bare DRAINED can never read "done" while declared sections are pending. New revert-proven guards: `tests/live/route-manifest.test.mjs` + `tests/fixtures/onclick-spa` (a constant-URL SPA whose served bundle declares 7 static `path:` values incl. a v6 RELATIVE "reports", 1 param, a danger, a minified `path:")".concat(…)` expr, + NOISE bare literals `/reactions` `/accept` NOT under `path:`, ONE a[href]): extractRoutes returns EXACTLY the 7 static paths (RELATIVE normalized, param split out) and REJECTS the bare noise + the expr fragment (path:-only precision); seedManifestRoutes seeds 6 (skips danger /logout), edge-free + zero-element (identity-safe); a full crawl VISITS /a /b /c /user/settings /reports, `markRouteUnreachable`s the redirecting /d, and `report --route-coverage` shows reached < declared; the differential (`crawl({seedManifest:false})`) collapses declaredManifest to 0 (the seeder is what expands the denominator). FAIL-ON-REVERT: disable the seeder → "/b was genuinely visited (not pending/unreachable)" reds; re-add the bare fallback → `/accept` + `/reactions` extracted → "a bare noise literal is NOT extracted" reds; drop the expr-char filter → "/)" extracted → the expr-fragment assertion reds. `route-frontier.mjs` UNCHANGED (visitRoute/markRouteUnreachable reused, not forked). `report.mjs` kept under the 200-line guideline by extracting the oracle to `route-coverage.mjs` (report 206). Residual (next increment): the ~5 declared sections that redirect-to-dashboard on direct nav need event-driven in-app nav; `:param` instances need within-section discovery. Suite 211 → 213 green. Not committed.
- feat(recon): menu-event sweep — `dom-snapshot` stamps `node.navControl` on `<nav>`/[role=navigation] controls (additive landmark signal, mirrors listRow, never identity), `frontier` navBatch front-loads them, `frontier-cli --emit` LEADS with the nav batch so a constant-URL onClick SPA's sections hydrate + collect before the general drain; agent acts each with `--opener-replayable`, section child reachable from a cold re-nav via reveal-replay (no graph.states{}). Test-first + revert-proven: tests/live/menu-sweep.test.mjs + tests/fixtures/menu-nav-app.
- feat(admin): failed acts render as calm categorized outcomes (safety/firewall/missing) + before-frame; strip moved into stage (screenshot width); Прогоны run→pages drill-down toggle with human-readable page names; page-filter view over strip+steps; graph-node detail modal; delete-run with confirm + server DELETE (loopback+token+path-contained+realpath).
- fix(report): edgeCount counts only `triggers` (causal) edges; structural nav edges surfaced separately as navEdgeCount (no longer inflate the Phase-2 causal-map header).

## 2026-07-17 (cont.)
- GOAL 4 built: `lib/recon/completeness.mjs` — Chao2 incidence mark-recapture completeness oracle over ≥2 independent crawl graphs (routes + templates), wired as `report --completeness g1 g2 …`; cross-graph-stable template key (templateSelector, never templateId); honest <2-sample refusal + converged flag. 9 unit tests (fail-on-revert).
- Security M1 documented (not fixed): write-firewall's `page.route('**/*')` covers HTTP(S) only — a WebSocket-frame mutation is not gated; TRANSPORT BOUNDARY note added to read-only-firewall.mjs header + decisions.md; danger-floor name gate still refuses mutation-named controls on any transport.
- Interactive-control collection (user-driven): (A) new `communication` danger class (video/voice call, go live, start meeting, broadcast) added to the hard-refused `REFUSED` set — initiating a call/livestream is never fired, the control is still mapped; (B) `--reveal-opener` (agent-judged) lets a mutation-NAMED form-opener (`Create post`) be clicked to COLLECT the revealed composer modal while the write-firewall aborts any actual submit. New compose-app fixture + reveal-opener live test (both levers fail-on-revert). recon.md doctrine + danger taxonomy updated.

## 2026-07-18
- GOAL 4 (compose-collection wired into the /recon loop; prompt-only, no code). The `--reveal-opener` MECHANISM was already built + reviewed (commit 6e5848e) but the /recon ORCHESTRATION never told the agent to use it, so a read-only authed walk left every mutation-named form-opener (`Create post` / `New message` / `Create group` / `Start chat`) refused and its composer UI uncollected — the exact gap the operator flagged ("agent never clicked chat / call / create-group / new-post buttons"). Fix, three doctrine edits: (1) `commands/recon.md` loop step 7b gains a COMPOSE-COLLECTION DIRECTIVE the driver injects into the recon subagent's prompt (open mutation-named form-openers with `--reveal-opener` to collect the composer as a read; the write-firewall aborts any write; never flag the SUBMIT control; communication/destructive/auth/payment stay hard-refused) plus an optional one-line FUNCTIONALITY-MEMORY summary (research: functionality-level history +15.92% coverage) so the agent prioritises novel surface; (2) a new boundary bullet stating compose-collection is now covered (openers mapped, submits never fired); (3) `.claude/agents/recon.md` danger taxonomy cross-references the step-4 `--reveal-opener` path from `safe` so a `Create post` is not left refused. No lib/test change (doctrine wires already-reviewed behavior).
