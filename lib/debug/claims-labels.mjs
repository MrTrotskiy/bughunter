// claims-labels — the WHITELIST half of the claim registry (see claims.mjs).
//
// Every operator-visible sentence longer than WORD_FLOOR words in the view modules
// (walk-view / pipeline-view / pipeline-shell / coverage-view) must be DECLARED, or the completeness gate
// (tests/unit/viewer-truth.test.mjs) reds. A sentence is declared in ONE of two places:
//   - as a data-CLAIM (claims.mjs) when it asserts something FALSIFIABLE about the run — it
//     then carries a licence + (optionally) a contradiction predicate and is checked, or
//   - as a LABEL here when it is a pure UI label, a KPI caption/tooltip, an empty-state, or a
//     stub SELF-description (Stage 3: "a stub may describe its own absence"). A label makes no
//     falsifiable claim about run data, so it carries no predicate — but it is still LISTED, so
//     adding a new operator sentence is never silent: it forces an edit here or in claims.mjs.
//
// These strings are the EXACT scanned runs (whitespace-collapsed), so completeness matches them
// verbatim. They are NOT auto-derived from source — deriving them would make the gate vacuous
// (any new sentence would cover itself). Editing a view sentence reds the gate until it is
// re-declared here, which is the whole point.
export const LABELS = [
  // row-vocabulary.mjs — the two policy-verdict refusal/permit sentences that run past WORD_FLOOR
  // (the rest of the per-kind vocabulary interleaves interpolations and stays under it).
  "ЗАПРЕЩЕНО — уходит к живому человеку или за пределы приложения, откатить нечем",
  "разрешено — дополняющее действие над чужим контентом, правится с откатом",
  "Вкладка «Граф» показывает те же связи как сеть: контрол",
  "Выбери слева прогон, помеченный не как «пустой», — или запусти новый",
  "Выбери строку слева — здесь покажу, что это был за шаг, сколько он занял и из чего состояло это время.",
  "Для этого шага кадр не снят. Всего в прогоне снято",
  "Заход на страницу записывается — и не одним событием, а четырьмя",
  "Здесь будут аномалии как находки, а не как шум",
  "Итоги обхода (изучено, недостижимо, страниц) считаются по завершении прогона — он ещё идёт",
  "Итоги обхода не записаны: прогон завершился, не подведя их",
  "Кадры в этом прогоне не снимались — ни на одном из",
  "Каждая строка — один шаг конвейера: что сделано, за сколько и из чего сложилось это время.",
  "Клик не обращался к серверу, но открыл",
  "Клик не состоялся, поэтому ни запросов, ни новых элементов от него быть не может — почему именно, сказано выше.",
  "Клик ничего не дал — ни запроса на сервер, ни новых элементов на странице.",
  "Код ответа доезжает и всегда доезжал: у каждого причинно привязанного запроса записаны статус, тип ресурса и длительность. Их не рисовала строка запроса — теперь рисует, и аномальный код помечен прямо в шаге во вкладке «Прогоны». Выключенные контролы (класс",
  "Не построен именно экран, поэтому раздел пуст, а не данные.",
  "Не хватает ЭКРАНА, который сводит находки в один список с привязкой к вызвавшему их акту, — это дефект отрисовки, нового прогона он не требует. Чего действительно нет в захвате: объявленные лимиты полей не снимаются, потому что нет инкрементального изучения формы (пустая",
  "Не хватает ЭКРАНА: события есть, проекция их не теряет, рисовать их некому. Это дефект отрисовки, а не пробел захвата — нового прогона он не требует. Пока пользуйся «Прогоны»: левая панель уже разбивает прогон по страницам, а «Конвейер» показывает все четыре вида событий в порядке времени.",
  "Первый шаг прогона: до него ничего не было, поэтому длительность не измерена. Ноль здесь означал бы «мгновенно» — это неправда.",
  "Подряд, один и тот же исход, одна и та же длительность",
  "Порог «медленно» и признак аномалии посчитаны по этому же прогону.",
  "Порога нет: в прогоне слишком мало шагов, чтобы у длительностей было распределение. Показывать здесь число значило бы выдумать его.",
  "Связи собраны — их пишет причинный захват (токен + классификатор инициатора) и рендерит",
  "Совпадение длительности при совпадении исхода — это один системный эффект (так выглядит фиксированный таймаут), а не",
  "Ссылка ведёт на другой сайт — по ней намеренно не переходили, за пределами приложения изучать нечего.",
  "Шаг заявил больше времени, чем показали часы — расхождение показано, а не спрятано.",
  "Это свойство ПРОГОНА, а не этого шага: съёмка включается режимом",
  "Это сравнение с соседями, а не наш выбор — прежний фиксированный «бюджет» в",
  "Этот шаг не сообщил о себе ни одной стадии — вся его длительность попала в",
  "а не как работа модели и не как навигация.",
  "а не наша поломка и не задача, которую мы тут чиним. Раздел их фиксирует и приписывает вызвавшему акту, не более.",
  "в трейле нет ни одной стадии решения модели: маршрут выбирает скрипт",
  "верхний разброс» этого прогона — это не «медленно», а выброс",
  "весь маршрут выбирает скрипт, поэтому неучтённое время помечено как",
  "время внутри шагов, которое ни одна объявленная стадия не покрывает — это не навигация",
  "где ждали 200, выключенные контролы, объявленный и не соблюдённый лимит",
  "доля времени в переходах между страницами (только строки-переходы)",
  "на сервер, но ничего нового на странице не появилось.",
  "порог взят из распределения ЭТОГО прогона, а не выбран заранее",
  "почему выбрали именно её и что прошли мимо)",
  "с превышали 287 шагов из 288 и не значил ничего.",
  "среднее по актам, которые ДОШЛИ до конца; сбойные сюда не входят",
  "строк после свёртки повторяющихся переходов и одинаковых сбоев",
  "сумма всех шагов конвейера — ровно время прогона",
  "там, где ожидался 200; контрол, который объявляет себя и не даёт себя нажать; поле, объявившее лимит и не соблюдающее его. По",
  "чем заход кончился: сколько актов, был ли он пустым) и",
  "что на ней было сделано и чем каждое действие кончилось. Сейчас тот же материал виден только плоской лентой актов во вкладке «Прогоны».",
  "что осталось несделанным). Дерево из этого строится полностью.",
  "экземпляров не попали ни в одну группу — считать долю по этим числам нельзя.",
  "эндпоинт — это её жёлтые рёбра, а клик по узлу открывает карточку с методом, статусами и вызывающими контролами.",
  "эндпоинт»: какой контрол какой запрос вызвал, с каким ответом, и какие эндпоинты не вызвал никто.",
  "это данные о приложении, а не наш сбой.",
  "это самое ценное, что может найти обход.",
  // coverage-view.mjs — the «Покрытие» screen. The bucket GLOSSES (what the sampling policy declined and
  // why), the two owner-bucket sentences (failure / churn / owed), the drill-down + empty-state notes,
  // and the rail tooltip. Each is a pure description of OUR crawler's policy, so all are LABELS — the run
  // numbers they sit beside are the falsifiable part and live in frontier.mjs (asserted in
  // coverage-screen.test.mjs), not here. The «…не попали ни в одну группу…» residual line is shared with
  // the KPI strip and already declared above.
  "почему не нажал остальное — разбиение по владельцу",
  "изучаем ОДНОГО представителя (или по одному на каждый",
  "последняя), остальные строки сюда — посчитаны, не пройдены",
  "попап фреймворкового виджета (список опций селекта, переключатели дейтпикера) — это хром, а не поверхность приложения",
  "у открывашки проходим до 8 экземпляров; всё сверх лимита сюда — посчитано, не пройдено",
  "поимённого списка для этого прогона нет — считается только количество",
  "Поэкземплярное покрытие для этого прогона недоступно — нет снимка графа.",
  "до контрола не дошли — путь-раскрытие не воспроизвёлся или он остался невидим",
  "строка ленты перерисовалась и исчезла раньше, чем мы успели её нажать",
  "контрол в поле зрения и не исчерпан — его ещё предстоит изучить",
];
