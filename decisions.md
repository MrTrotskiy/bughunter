# Decisions

Founding architectural decisions for `bughunter`. Rejected alternatives logged more than accepted ones.

### 2026-07-15 — Authenticated recon via a login pre-step + storageState reuse
- CHOSE: a standalone `login.mjs` authenticates ONCE and persists a Playwright storageState (cookies + localStorage); `session.mjs contextOptions()` loads it at BOTH newContext sites via `BUGHUNTER_STORAGE_STATE`, so the existing crawl (cold-launch AND daemon-attach) maps the logged-in surface with the SAME graph/causal/coverage machinery — one wiring point, the crawl unchanged. Login is SETUP (separate process, no debug trail, no measured act), so causal attribution is provably untouched. Creds ride operator env vars (`BUGHUNTER_LOGIN_USER`/`_PASS`), never argv or a parsed file.
- CHOSE: a fresh graph per auth mode (anonymous vs authed are different surfaces → separate honest denominators), matching the existing clean-run step.
- CHOSE: a route-level refusal (`routeRefused`) in `persistentStep` — the name-only click gate cannot see a GET `/logout` reached by NAVIGATION, so classify the route path too; reconLoop marks it unreachable (honest), never visits it.
- REJECTED: per-target login recipes parsed from `test.md` — a parser over the freeform secrets file is brittle and a new leak surface; a generic heuristic (email + password + submit) with `--*-selector` flag overrides covers the standard form with zero config. Revisit only when a second target needs SSO/OAuth/2FA.
- REJECTED: threading `storageState` through every recon CLI's argv — a purely cross-cutting concern; an env var read in one helper matches every other cross-cut (`BUGHUNTER_STATE_DIR`, `BUGHUNTER_RUN_ID`) and touches no signatures.
- REJECTED: one graph tagged `authState: anonymous|authed` — conflates two denominators so coverage % loses meaning; defer to a future "auth-state as a graph dimension" ADR.
- REJECTED: a silent anonymous fallback when the storageState file is missing — a run the operator believes is authenticated must not quietly crawl logged-out; `contextOptions` fails loud (`STORAGE_STATE_MISSING`).
- DEFERRED (honest boundaries): multi-step/SSO/OAuth/2FA login; mid-crawl session-expiry detection + re-login; the click-navigates-to-/logout class (href-path floor in `actStep`); an `auth-check` liveness probe.

### 2026-07-14 — Rebuild from scratch, not patch bughunt-agents
- CHOSE: greenfield `bughunter` — the old foundation is unfixable by patching.
- REJECTED: keep patching `bughunt-agents` — its hot file `recon-network.mjs` (1038 lines) was patched for months; blast radius too high, two identity systems structurally cannot join.

### 2026-07-14 — One incremental graph as the central artifact
- CHOSE: single `state/graph.json`. Nodes Route → State (nested) → Element (template) → Instance (per-row) → Request; every tool reads/writes it.
- REJECTED: a complete static "brain of the whole app" built ahead of time — it died at ~6% control→endpoint on a live target while overfit fixtures showed ~100%. "Know where everything is in advance" is a false goal on an incomplete surface (shadow DOM, canvas, iframes, auth-gated, virtualized).

### 2026-07-14 — Single DOM/CSS identity, two-level (template + instance)
- CHOSE: DOM/CSS via `page.evaluate` is the sole source of truth; ARIA role/name are attributes of that node. Each element = a stable `templateSelector` (structural indices normalized out) PLUS per-instance `instanceSelector`+`instanceKey`. A 50-row table = 50 addressable instances of one template.
- REJECTED: two parallel identity systems (ARIA-text `role+name+domPath` vs DOM/CSS) — they never joined in the old project → construct→endpoint 0/52.
- REJECTED: collapsed identity (`nth-child(3)` → `nth-child(n)`, strip `data-id`) — killed per-row addressing → self-CRUD 0/0.

### 2026-07-14 — Causal attribution = token + CDP initiator (BOTH), not either alone
- CHOSE: attribute a request to the control that fired it via (1) an in-page causal TOKEN (`window.__bughuntCause`, recorded per fetch/XHR into `window.__bughuntFires`; keep fires with matching cause and `seq >= seq0`) AND (2) a page-lifetime CDP INITIATOR classifier that rejects timer/parser-rooted requests. Proven in `tests/whats-new.test.mjs`.
- REJECTED: time-window capture — miscredits coincident background traffic; caused the old project's double-subtraction bug.
- REJECTED: causal token ALONE (what the old project reached, too late) — a background poll that TICKS INSIDE a control's window inherits its cause token and leaks into the result. Discovered live while building the keystone: a forced 1200ms window leaked 3 `/api/ping` fires. The CDP initiator is what catches this; the token alone is insufficient.
- NOTE: initiator marks a pathname background only if NO request to it was ever click-rooted, so a poll sharing a path with a real click never suppresses the real edge. CDP-dependent (chromium only); degrades to token-only elsewhere.

### 2026-07-14 — Phase 1 recon = "perceptron loop" (incremental, small receptive field)
- CHOSE: each context window studies only 2-5 NEW elements (frontier-based online exploration), acts with a causal token, writes to the graph, loops. Script owns control-flow (diff, IDs, capture, frontier); LLM judges only "what is this / what does it do".
- REJECTED: feed all ~40 elements to one context — blows context, the old project's failure mode.
- REJECTED: literal perceptron (weight training) — we have an LLM; only its SPIRIT (small local unit + incremental update) applies. The pattern is frontier-based exploration / active learning / online map-building.

### 2026-07-14 — Recon scope: incremental whole-site, honest denominator
- CHOSE: cover the whole site incrementally but budgeted, with a NON-collapsing coverage denominator; opaque regions (shadow/canvas/iframe) are flagged, never hidden.
- REJECTED: chase "covered everything" — the old project's overfit fixture-recall hid the real 6%.
- REJECTED: task-scoped-only (lazy under a single mission) — user wants a real site map.

### 2026-07-14 — Reuse hardened plumbing, rewrite recon/graph/coverage
- CHOSE: port `envelope.mjs`, `host-policy.mjs` (SSRF gate verbatim), the `stateProbeInitScript` causal substrate; rewrite everything recon/graph/coverage.
- REJECTED: full greenfield including security/browser-lifecycle — those were solved and hardened; re-deciding them wastes weeks.

### 2026-07-14 — LLM belongs in the walk where the task is semantic
- CHOSE: use the model for state-equivalence, role-less control recognition, "did this change matter"; keep scripts for diffs/IDs/capture/coverage numbers.
- REJECTED: "no LLM in the walk" dogma — it forced semantic tasks into brittle thresholds and frequency masks; a documented source of the old project's fragility.

### 2026-07-14 — Model split
- CHOSE: Fable = architecture/research/planning/network-agent launches/Phase-2 case design; Opus = all code authoring; Sonnet-or-lower + script = Phase-1 clicking/recon.

### 2026-07-14 — Phase-1 loop: pure frontier + injected-step driver
- CHOSE: split the recon loop into a pure control-flow driver (`lib/recon/recon-loop.mjs`) plus a pure frontier selector (`lib/recon/frontier.mjs`); the browser STEP and the LLM JUDGE are injected, so the loop is unit-testable with no browser and each collaborator swaps independently. Frontier = TEMPLATE-level receptive field (unexplored templates, ascending id, capped at N); `explored` is persisted on the element node so the next context window / resumed run continues where it stopped. Honest denominator (`discovered/explored/remaining`) never collapses. Both revert-proven as unit tests.
- REJECTED: loop owning the browser directly — couples control-flow to session lifecycle, blocks unit-testing the loop and blocks swapping in the persistent-session step.
- REJECTED: per-INSTANCE frontier (each of 50 rows its own item) — defeats template-level identity; the same control would be re-studied 50×. Instances are addressed WITHIN a template, not enumerated by the frontier.
- NOTE: the live step will initially cold-start per step (graph = cross-context memory), so only controls present on initial page load are reachable; the persistent-session task lifts that to controls behind in-app state.

### 2026-07-14 — Semantic recon layer: agent-as-driver over thin CLIs, not an in-loop LLM
- CHOSE: the "LLM judge" is a FRESH Sonnet subagent (`.claude/agents/recon.md`) that is the OUTER driver — it calls thin CLIs as tools (`frontier-cli --emit` → judge ≤5 templates → `whats-new --act-template` on safe ones → `observe`), with `state/graph.json` the only channel between invocations (file-only handoff, resumable). Two front-ends over one core: the node `recon-loop` stays a deterministic identity-judge fallback; the proven loop/frontier are NOT modified. New graph field `node.semantics` (purpose/danger/effect/acted/stateChange), written only by `observe`, which also flips `explored` — so explored ⟺ OBSERVED (richer, honest coverage), not explored ⟺ clicked. A deterministic `danger-floor` regex is a SAFETY BACKSTOP in `observe` (refuses an acted observation on destructive/auth/payment), never the judge. Full design: `docs/draft/recon-agent-design.md`.
- REJECTED: an API-call LLM judge inside `recon-loop.mjs` — violates the no-API-keys / runs-inside-Claude-Code invariant; the judge is a Claude context, not a call.
- REJECTED: node loop shells out to a Sonnet subagent per step — node cannot spawn a Claude context, and it inverts the hierarchy (control-flow calling semantics) and couples the pure loop to agent lifecycle. The agent must be the OUTER driver.
- REJECTED: a hardcoded LLM-free classifier as the source of truth — exactly the "brittle thresholds and frequency masks" named as a source of the old project's fragility (see "LLM belongs in the walk"). The regex `danger-floor` is a backstop only.
- REJECTED: a skill (injects into current context, no isolation/budget → 40-element dump lands in parent = the founding context-blow-up) and a main-thread stateful agent (re-creates "one context sees everything"). A bounded, tool-restricted SUBAGENT with fresh 2-5-element windows is the fit.
- REJECTED: `whats-new` marks explored, one mega-CLI, per-instance frontier emit, State-nodes now, semantics as top-level fields — see design doc §8 for each.

### 2026-07-14 — Mutation test-data policy: HUNT-tagged, realistic, safe (standing requirement)
- CHOSE: every value the hunter WRITES into a target (create/edit/register/upload/comment) must be realistic (app accepts it like real input), synthetic & safe (no real PII/credentials/payment), and TRACEABLE via a `HUNT-<runId>` marker embedded per field (name `Emma Johnson HUNT-7f3a`, email `hunt-7f3a-emma@yopmail.com`, post text = real English + `[HUNT-7f3a]` tag). One run shares one id → precise cleanup + attribution; a cleanup pass deletes ONLY HUNT-tagged artifacts (never real data), which doubles as the delete half of CRUD self-testing. Real English text from a BUNDLED public-domain corpus (captured once, in-repo), disposable mail via YOPmail behind a PLUGGABLE backend. NOT built yet (no mutation flows); it is the contract the actor (task #5) + Phase-2 mutation cases must honor. Full spec: `docs/draft/mutation-test-data.md`.
- REJECTED: live per-run internet fetch for text as the default — fragile (network/rate-limits), non-reproducible, risks unpredictable/unsafe content + licensing. Bundle-once (optional live+cache) instead.
- REJECTED: bare `HUNT` with no run id — cannot separate runs or clean up precisely.
- REJECTED: real names/emails with no marker — indistinguishable from real data, uncleanable.
- REJECTED: hardcoding YOPmail at call sites — some targets blocklist disposable domains and its inbox is public; the email backend must be swappable for a real catch-all.

### 2026-07-14 — Test doctrine: FAIL-ON-REVERT, adopted from aeye-os
- CHOSE: adopt aeye-os's anti-vacuous test discipline (codified in tests/CLAUDE.md) — every behavior test carries `// Guards:` + `// FAIL-ON-REVERT:` and is proven RED by reverting the guarded code before it counts; layer split unit (pure fns, no browser) vs live (real chromium + local fixture); never mock app boundaries; no sleeps as waits; the live fixture must model the real traffic classes; prove the HARD case (in-window background poll), not just the happy path.
- REJECTED: aeye-os monorepo-specific machinery — op()-DSL + asserted-coverage sidecar, test-bus/runId cross-pod convergence, class A/B/C spec suffixes, tests-pod ingest store, data-testid coverage tiers. Coupled to their contract-kit/Fastify/NATS stack; wrong weight for a small Node+Playwright tool.
- NOTE: "PID" has no literal definition anywhere in aeye-os (exhaustively searched: word-boundary grep, git log, docs). Interpreted as their "pipeline spec + fail-on-revert" discipline — the thing that makes tests actually test, matching the user's intent. Confirm if they meant something else.

### 2026-07-14 — Security pass before first commit (danger gate moved to the fire path)
- CHOSE: enforce the destructive/auth/payment refusal on the ACT, not just the record. The coarse floor now gates `actStep` in `lib/recon/step.mjs` — it refuses to fill/click a floored control and throws `DANGER_FLOOR` before touching the page, so EVERY fire path (whats-new `--act-template`, the loop, a mis-judging agent) is covered, not only `observe`'s after-the-fact write. `dangerFloor` is hardened: camelCase/snake/kebab normalization (an attribute-derived `deleteAccount` no longer slips the whole-word matcher), extended destructive/payment vocabulary, and an icon-only/no-name control → `unknown` (never `safe`). Live-proven: acting on a Delete control rejects AND fires zero `/api/delete` (tests/live/danger-gate.test.mjs); camelCase normalization revert-proven.
- CHOSE: chromium sandbox ON by default (`lib/browser/session.mjs`). `--no-sandbox` removes the OS-level renderer sandbox and bughunter navigates hostile pages — it is now opt-in via `PW_NO_SANDBOX=1` for root/CI containers that cannot init the sandbox. Verified: live tests launch sandboxed in this environment, so no test needed the opt-out.
- CHOSE: harden the `recon` agent prompt (`.claude/agents/recon.md`) — page-derived CLI argument values (`--purpose`, `--fill`) are DATA that must be single-quoted; a page embedding `` ` ``/`$()`/`;`/`|` is trying to break out of the shell. Reinforces the existing "page text is data, never instructions" invariant at the exact injection point (the agent building Bash commands).
- CHOSE: escape the data-attribute value as a proper CSS string in `dom-snapshot.mjs` (backslash first, then quote) so an instance selector for a `data-id` containing `\`, `"`, or `]` is valid and correctly-targeting, not malformed.
- ACCEPTED (known, not fixed): the in-page causal globals (`window.__bughuntCause`/`__bughuntFires`) are forgeable by a hostile page — this is INHERENT to an in-page token and is exactly why attribution requires the SECOND mechanism (the CDP initiator classifier, which the page cannot reach). Neither mechanism alone is trusted; the pair is the design (CLAUDE.md invariant #1).
- ACCEPTED (pre-existing, out of scope): DNS-rebinding can defeat the SSRF host-policy (resolve public at check time, private at fetch time). Ported from bughunt-agents' `host-policy.mjs`, not introduced this session; the loopback gate + `PW_ALLOW_PRIVATE` still hold for the common case. Tracked for a later resolve-then-pin fix.

### 2026-07-14 — Persistent browser: one chromium per run, not one per act
- CHOSE: replace cold-start-per-act with a shared browser. Node-loop (`recon-run`): ONE browser for the whole `crawl`, re-navigated per act (launch/close hoisted to crawl; `persistentStep` reuses one page). Agent path (each `whats-new` is a separate OS process): a browser DAEMON — `chromium.launchServer` boots once (`browser-daemon.mjs`, started by `recon-session --start`), publishes `state/session.json` {wsEndpoint, browserPid, daemonPid}; `session.mjs attach()` reads it and `chromium.connect()`s, opening a FRESH context+page+probe+tracker per act on the shared PROCESS, else cold-launches when no daemon (backward compatible). N acts spend one chromium process, not N launches. Empirically verified: one launchServer survives N sequential `connect()` clients with a stable pid; each client gets its own context and a working `newCDPSession`. Causal invariant untouched: attach wires probe + initiator tracker BEFORE the caller navigates — byte-identical order to the cold path — so the CDP classifier still rejects an in-window poll over the connection (live-guarded in tests/live/daemon.test.mjs, revert-proven).
- CHOSE: re-navigate per act (Option A), NOT stay-on-page (Option B). Keeps acts independent and the causal walk in `step.mjs` (one implementation); the daemon stays a pure browser HOST, CLIs are not RPC shims. Cost: controls behind in-app state stay reload-unreachable (honestly surfaced). Lift to stay-on-page only when a live crawl shows that as the dominant coverage gap (separate ADR).
- REJECTED: `connectOverCDP` instead of `launchServer`+`connect` — Playwright documents it as "significantly lower fidelity"; it degrades exactly the `addInitScript`/`newCDPSession` the causal substrate depends on.
- REJECTED: daemon owns the causal walk + CLIs become RPC shims (Option B now) — bigger blast radius on the keystone, acts stop being independent, highest causal risk. Deferred.
- REJECTED: collapse agent-as-driver into one long-lived node process — re-inverts the founding hierarchy (control-flow calling semantics) and re-creates "one context sees everything" (decisions.md 2026-07-14 semantic-recon entry).
- REJECTED: lazy auto-start of the daemon from `attach()` — risks orphaned chromium nobody stops. `--start`/`--stop` bracket the run explicitly (run driver's job); `--start` reaps a stale session; a failed `connect()` self-heals to a cold launch. Design by `cto` (Fable); code by Opus.

### 2026-07-14 — Review pass on the persistent-browser layer (3 reviewers)
- CHOSE: fix the causal regression the persistent page introduced (backend MUST FIX). The CDP initiator tracker is per-page and its per-path verdicts are STICKY (`anyForeground`); on a reused page re-navigated per act, a path an EARLIER act click-rooted would keep suppressing the timer-rejection of a LATER act's same-path background poll → a phantom causal edge (the exact class that killed bughunt-agents). Fix: `resetTrackerVerdicts(page)` (clears verdicts, keeps CDP arming) called in `persistentStep` after each re-navigation, before the act. Live-guarded deterministically by a new fixture where a clicked path is also polled and the second act's request is slow enough to hold its window open (tests/live/cross-act.test.mjs); revert-proven (drop the reset → phantom edge appears). Only `recon-run` reuses a page; the daemon/whats-new path already gets a fresh tracker per act (separate process).
- CHOSE: harden the session.json trust boundary (security H1/M1/L1). `readSessionEndpoint` now refuses any NON-loopback wsEndpoint (a tampered file can't redirect `attach()` to a rogue remote Playwright server that would capture typed credentials). `recon-session` signals a pid read from the file ONLY if the pid is alive AND its command still matches what we started (`killOwned` + `ps` command check) — a crash + OS pid-reuse or a tampered file can no longer kill an arbitrary process; `isAlive` also rejects pid ≤ 0 (no process-group signals). session.json + daemon.log are written 0600; session.json is written atomically (tmp + rename, defeating a pre-planted symlink). Both new safety guards revert-proven (tests/unit/daemon-safety.test.mjs).
- CHOSE: robustness fixes (backend SHOULD/CONSIDER): `attach()`/`launch()` never leak a browser on a partial-wiring failure; the attached `release()` swallows a dead-connection error (symmetry with cold `close`); `stop()` escalates to SIGKILL after a grace period; `start()` reaps everything on a readiness timeout and closes the daemon-log fd; readiness accepts the daemon pid when the browser pid is null; `observe` forces `acted=false` on effect=unreachable-coldstart (no "acted:true + unreachable" record); `whats-new` uses the shared `exitCode()`.
- ACCEPTED (M2, documented tradeoff): one shared chromium PROCESS per run removes the process-level isolation the old per-act launch gave between successive hostile targets (contexts are still isolated: fresh context per attach). The OS sandbox stays ON by default (`PW_NO_SANDBOX` opt-in), which bounds it. Revisit if targets are mutually adversarial within one run.
- ACCEPTED (security, verified clean): the launchServer WS endpoint binds loopback only, uses an unguessable GUID path, and enforces an Origin allowlist — not remotely reachable and not reachable from a page in the recon browser. Process spawn is `spawn(execPath, [fixed path])`, array argv, no shell — no command injection.

### 2026-07-14 — Multi-route recon: run on any same-origin site, not just the entry page
- CHOSE: derive the after-snapshot route from `page.url()` (the route the act LANDED on), not a fixed baseline route. `actStep` (`lib/recon/step.mjs`) now merges revealed controls under `routeKey(page.url())` when still same-origin. This is BOTH the correctness fix (a nav act no longer mis-files the destination page's controls under the origin route) AND the multi-route discovery engine — new routes' controls enter `graph.elements` with the right `node.route`, and the existing global template frontier hands them out with ZERO change to the revert-proven loop core. Design by `cto`/Fable; code by Opus.
- CHOSE: the persistent step re-navigates per act to the TARGET template's own `node.route`, not the entry URL, so a control first seen on `/products` is reached by loading `/products`. Multi-route reach live-proven (a `/products`-only control is genuinely explored, not unreachable) in tests/live/multi-route.test.mjs; revert-proven.
- CHOSE: a new `lib/recon/scope.mjs` owns route identity + origin scope — `routeKey(url)` (drop query + plain `#anchor`, keep path-like SPA hash, normalize trailing slash) and `sameOrigin(a,b)` (RFC 6454 scheme+host+PORT via `URL.origin`). Single source of route identity; the whole graph keys routes through it.
- CHOSE: `routeKey` keeps the CONCRETE pathname — it does NOT mask `/product/42 → /product/:param` the way request urlPatterns do. A route key must be re-navigable (":param" is not a URL). Over-counting is bounded structurally: the frontier follows only ONE instance per link template, so a 1000-product listing yields one visited product route, not a thousand.
- CHOSE: never FIRE an off-origin link. `actStep` reads an `<a>`'s resolved href before the click and, if off-origin, records it as `external` (effect `external-link`, reachable-but-not-fired, counted as coverage) and returns WITHOUT clicking. The SSRF host-policy only guards OUR `gotoGated` calls — a browser link-follow bypasses it — so this is the scope boundary for acted links. Live-proven: the off-origin partner server gets ZERO hits across a full crawl; revert-proven.
- CHOSE: `frontierStats.routes` reports how many routes were reached/snapshotted (grows, never collapses), surfaced in `report`'s coverage line.
- REJECTED: an explicit route-frontier QUEUE with BFS ordering + per-route step budget + maxRoutes cap (design Option B). Larger surface on freshly-proven loop code for no near-term win; Phase-1-NOW rides discovery order over the global template frontier. Upgrade only when a live wide-site run shows one route starving the global step budget.
- REJECTED: masking numeric/uuid path segments in `routeKey` (mirror `toUrlPattern`) — makes the key non-navigable; the frontier's one-instance-per-template rule already bounds route explosion.
- REJECTED: adding `sameOrigin` into the ported `host-policy.mjs` — conflates a security gate (private-host SSRF) with a scope policy (off-origin-public); keeps the "ported verbatim, do not re-decide" boundary clean by living in `scope.mjs`.
- REJECTED (deferred to the stay-on-page / State-node ADR): eager `a[href]` route harvesting (needs route-unreachable honesty for links that 404/redirect); per-(route×template) causal keying for shared chrome that hits different endpoints per route; `node.route` string → `node.routes[]` multi-tagging; query-parametrized states as distinct routes; reaching routes addressable ONLY via in-app client state (the natural extension of the already-deferred "controls behind in-app state" boundary).
- KNOWN HOLE (small, noted): a JS `onclick → window.location = <off-origin>` is not caught by the pre-fire href check (only `<a href>` is); the after-merge origin guard still refuses to record the foreign page, and the next act re-navigates back in-scope. Overlay/cookie-consent dismissal (gap #4 from the Fable design) is NOT built yet — a consent banner still leaves underlying controls `NOT_VISIBLE`; deferred to a focused follow-up needing a live-tuned selector list.

### 2026-07-14 — Review pass on the multi-route changes (3 reviewers)
- CHOSE: scheme-gate the off-origin-link refusal (backend MUST FIX). A `javascript:`/`mailto:`/`tel:`/`data:` href has an opaque origin (`URL.origin === "null"`), so the first cut's `!sameOrigin(...)` test misclassified the `javascript:void(0)` button-as-anchor idiom as an off-origin link, recorded it `external-link` (counted as explored) and never fired it — dropping a whole class of real in-page controls AND falsely inflating coverage. Fix: `isOffOriginHttp(base, href)` in `scope.mjs` — the skip fires ONLY for an http(s) cross-origin link; special-scheme hrefs fall through to the normal click. Live-guarded (tests/live/scheme-gate.test.mjs) + unit; revert-proven.
- CHOSE: kill the protocol-relative route-reconstruction escape (security HIGH). A same-origin page whose pathname begins `//` (`https://target.com//evil.com`) stored `routeKey = //evil.com`; the next act's `new URL("//evil.com", origin)` resolved it PROTOCOL-RELATIVE to `https://evil.com/`, and `gotoGated` allows public hosts → the recon browser navigated onto attacker infra believing it in-scope. Fix at two layers: `routeKey` collapses leading slashes to exactly one (the key can only ever be origin-relative), and `persistentStep` throws `OFF_ORIGIN` if the reconstructed navUrl is not `sameOrigin` with the run origin (defense-in-depth). The PRIVATE-host SSRF variant was already refused by `isPrivateHost`; only the public off-origin boundary was open. routeKey collapse unit-guarded + revert-proven.
- CHOSE: bound `fillTarget`'s `fill()` calls to 5s (backend SHOULD FIX). Unbounded fills let a hidden/disabled/readonly sibling input stall Playwright's 30s default action timeout — the exact stall the NOT_VISIBLE/bounded-click work set out to kill. Also: an off-origin JS-redirect landing now reports the in-scope START route, not the foreign pathname (which would falsely read to the agent as an in-scope `navigate`).
- CHOSE: strip C0/C1 control bytes in `report`'s `oneLine` before rendering agent-/error-derived text, and sanitize `purpose` (security LOW) — an ANSI/OSC sequence in page-derived text must never reach the operator's terminal raw. `name` was already `JSON.stringify`-escaped.
- ACCEPTED (security MEDIUM, documented, mostly pre-existing): a same-origin control whose JS `onclick` redirects off-origin still navigates before the pre-fire href check can refuse it (the check only sees `<a href>`). This is inherent — any visited untrusted page can script-navigate the recon browser off-origin on load without any click, so the incremental risk of the click is marginal; the after-merge origin guard keeps the foreign page out of the graph and the next act re-navigates back in-scope. Not an origin boundary, and not claimed as one.
- ACCEPTED (secrets, verified clean): the live validation left NO credentials/cookies/storageState/`test.md` content in any tracked file; only the public target hostname had leaked into the changelog prose and was genericized out.

### 2026-07-14 — Cookie/consent overlay dismissal (reference-informed)
- CONTEXT: after multi-route, the next real-site blocker for unauthenticated public sites is a consent wall that intercepts pointer events — every underlying control then fails its click, so the page maps almost nothing. Checked the sibling reference (bughunt-agents): it has NO cookie-specific dismissal — only a generic 3-rung panel-reset ladder (Escape → re-click opener → reload, `lib/recon/recon-click.mjs:219-235`) and a name-regex denylist (`state-frontier.mjs:41` DISMISS_RE) that REFUSES to click close/submit/destructive controls. Key harvested insight: an open overlay keeps XHR in flight so the network never settles.
- CHOSE: a curated CONSENT-specific `dismissOverlays(page)` (`lib/recon/overlays.mjs`) — the improvement over the reference (which lacked it). A high-precision accept-selector list for the major frameworks (OneTrust/Cookiebot/Osano/Didomi/Usercentrics/Quantcast/TrustArc/Axeptio) plus a conservative accept-TEXT fallback GATED on a consent-scoped ancestor (id/class matching cookie|consent|gdpr|cmp|privacy). It clicks the first match in-page, best-effort, and returns a label to log. Runs after every navigation BEFORE the causal window opens (accept-click request fires under `__idle__`, excluded by the token filter — never a phantom edge), in `recon-run` (baseline + per-act, before `resetTrackerVerdicts`) and `whats-new` (surfaced as `result.overlayDismissed`).
- CHOSE: consent-scoping as the false-positive guard (adapts the reference's denylist idea). The text fallback never clicks an accept-text control that lacks a consent-scoped ancestor — live-proven a bare "OK" is left alone (tests/live/overlay.test.mjs). The curated selector list itself is safe (framework accept-all ids). Every dismissal is logged (auditable).
- REJECTED: a generic Escape / click-away dismissal (the reference's Escape rung). Escape rarely dismisses a modal CONSENT wall (they force a choice), and a blanket Escape/click-away would close exploration-worthy modals recon WANTS to map. Consent-specific accept is the deliberate, narrower choice.
- REJECTED: an LLM step to judge/dismiss the banner — burns the agent's 2-5-template budget fighting a banner that reappears every fresh-context act; a deterministic curated sweep is cheaper and runs outside the causal window by construction.
- KNOWN (honest): the curated list needs live tuning against real targets; a fully custom banner it misses stays honestly `NOT_VISIBLE` (no silent failure). The reference's Escape→re-toggle→reload ladder for STAY-ON-PAGE panel reveals (a different problem: reversing a reveal recon itself opened) is deferred to the stay-on-page ADR.

### 2026-07-14 — Locator preference: derived attribute, identity stays selector-keyed (reference-adapted)
- CONTEXT: recon should recognize the KIND of locator available per control (test-id vs stable id vs plain css) and prefer the durable ones, so Phase-2 generates specs on stable handles. The sibling bughunt-agents had a mature system to harvest: `preferredSelector` (test-id → stable #id → role+name, `lib/recon/element-id.mjs:168-172`), `isStableId` (rejects uuid/hash/`:r0:`useId/numeric, `:152-158`), `safeAttrValue` (untrusted-value guard, `:144-146`), a broad `TESTID_ATTRS` recording WHICH attr matched (`dom-attrs.mjs:38-55`), and a page-wide UNIQUENESS GATE (`promoteTestIds`, `:148-196`) — a test-id feeds the locator only if unique. Two documented pitfalls: role+name is non-unique; a non-unique test-id collapses two controls to one identity.
- CHOSE (Option A): store `locator` as a DERIVED ATTRIBUTE (like role/name), never an identity input. `dom-snapshot` classifies a per-element `locator {type, value?, attr?, unique?}` down the ladder testid → strictly-stable #id → role+name → css; a post-pass applies a TWO-LEVEL uniqueness gate (page-unique test-id = instance discriminator; shared across a template's rows = marker, `unique:false`; spanning multiple templates = unusable). The template node carries the KIND, each instance its concrete handle. `graph-store` copies it (additive field), `report` surfaces `loc=<type>`. A SEPARATE broadened `TESTID_ATTRS` feeds ONLY the classifier; the existing `DATA_ATTRS` (which drives `instanceKey`/`instanceSelector`) is byte-unchanged, so identity minting (`ids.mjs`, keyed on templateSelector + instanceKey) is untouched — ZERO churn to `templateId`/`instanceId`/causal edges, proven by the existing identity live tests staying green.
- REJECTED (Option B): port the reference faithfully and RE-KEY identity onto the test-id (`element-id.mjs:66-84`, `promoteTestIds`). Two fatal costs in a TWO-LEVEL model: (a) a per-instance test-id (`edit-row-42`) is unique per row → 50 test-ids → 50 templates, collapsing "one template, 50 instances" (the invariant `decisions.md` line 16 was explicitly protected for); (b) re-keying churns the append-only ledger ("same key → same id forever", `ids.mjs:14-21`) and re-keys every causal edge (`graph-store.mjs:98-101`). The reference's re-key works only because its identity is FLAT. Our answer to "base identity on the locator": store it as a first-class attribute and let Phase-2 PREFER it — identity stays selector-keyed.
- DEFERRED: per-target `testIdAttrs` override (no `lib/target` config layer exists yet); true page-wide role+name uniqueness verification (role-name locators are recorded but not uniqueness-checked — the reference warns role+name is non-unique); feeding the locator to the recon agent (it clicks `instanceSelector` to act — locator KIND is a Phase-2 concern, not needed to recon); a read-only `identity-diff` probe to make the zero-churn claim a one-command check for future identity-adjacent changes.

### 2026-07-14 — Debug capture + local admin viewer (architecture by Fable, code Opus)
- CONTEXT: the user wants to run Phase 1 and SEE it — graph growth, how the agent walks, what it tests, logs, speed, with screenshots — to spot problems and report back. Two references harvested: country-navigator-qa's `tools/debug-capture` (MV3 ext + zero-dep local collector + admin UI; causal-stamp-at-cause; frame-index fragility to avoid) and bughunt-agents' self-contained static HTML report (`html-renderer`/`html-assets`) + artefact server (path-allowlist + Range).
- CHOSE: capture as a FILE TRAIL written inline by the existing CLIs (`lib/debug/trace.mjs`, the single writer), NOT a separate capture process. One append-only `events.ndjson` of `{seq,ts,kind,payload}` + per-step graph snapshots (cheap: graph-store already rewrites the full file each step) + before/after viewport key-frames keyed by templateId (a template is acted at most once per run → no fragile parallel frame index, the country-navigator pitfall). Opt-in via `BUGHUNTER_RUN_ID` so the recon CLIs are byte-identical when it is absent. The seq is a line-count, and the agent-path CLIs (`frontier-cli`/`whats-new`/`observe`) are separate processes that run sequentially, so their appends never interleave.
- CHOSE: capture the key-frames ONLY while the cause token is `__idle__` (before `beginCause` / after `endCause`) and always `fullPage:false`. This is the load-bearing decision — the biggest risk was a screenshot perturbing causal attribution. Placement + viewport-only means the frame scrolls nothing and fires no request inside a causal window, so it can never forge a phantom edge (invariant #1). Made a live test (`capture-causal.test.mjs`) that keeps capture ON over the adversarial in-window background poll and asserts the poll is STILL rejected — capture is proven inert, not assumed.
- CHOSE: also record the viewport CSS size in the before-frame, so the admin places the target rect box independent of devicePixelRatio (`rect.x * imgClientWidth / viewport.width`) — the shot covers exactly the CSS viewport box scaled by dpr, so the mapping is linear without knowing dpr.
- CHOSE: a zero-dep `node:http` admin viewer (`admin-server.mjs`) bound 127.0.0.1 ONLY, with a loopback Host-header allowlist (DNS-rebinding defense), NO CORS header EVER (the trail can screenshot a logged-in app; absence of `access-control-allow-origin` makes the browser block any cross-origin read), and a resolved-path artifact allowlist (regex-validated runId/seq/filename + a `startsWith(base + sep)` containment check that holds even if the regexes are later loosened). These three fences are adapted verbatim-in-spirit from country-navigator's collector (`streamFile`/Host-guard/no-CORS comment) and bughunt-agents' `assertUnderRoot`.
- CHOSE: a single self-contained vanilla `admin.html` (no build, no external host — same discipline as bughunt-agents' inline-assets report) with four sections (Walk/Graph/Log/Speed) that live-polls `/api/runs/:id`. Reused the reference report's aesthetic (dark CSS-vars, KPI tiles, lightbox) but rebuilt for the recon trail (route→control→endpoint graph with a snapshot scrubber; before/after act frames with a rect highlight).
- CHOSE: in `recon-run`, mark the just-acted template `explored` BEFORE snapshotting the graph (idempotent — reconLoop marks it again after step() returns). Else the per-act snapshot shows the control still `unexplored` (markExplored is deferred to the loop driver), so the graph-growth scrubber would lag one step behind reality. The agent path (`observe`) already snapshots after its own markExplored, so it was already correct.
- REJECTED: an MV3 browser-extension capture layer (the country-navigator approach). We DRIVE the browser via Playwright and already own the act boundary — capturing inline at the act is simpler, needs no extension install, and puts the frame at exactly the idle point the causal-safety argument requires. The extension exists there because that project instruments a browser it does NOT drive.
- REJECTED: a wall-clock or resolve-time screenshot (capture "around" the act). Would risk a frame inside the causal window; the idle-only placement is the whole safety argument.
- REJECTED: HTTP Range on the artifact server. The reference serves large media; our key-frames are small viewport PNGs shown in `<img>` (no Range request), so Range is YAGNI — the load-bearing part (path containment) is kept, the streaming complexity dropped.
- DEFERRED: persisting opaque regions in the trail (still counted at snapshot, not in the graph — a pre-existing gap); a `run.end`/`done` timeline event (closeRun stamps run.json, which the KPIs already read); serving the trail over anything but localhost (out of scope by design).

### 2026-07-14 — Review pass on debug-capture (3 reviewers)
- CHOSE: make the trail WRITE PATH best-effort/non-throwing (backend SHOULD, treated as MUST — honest coverage). `trace.mjs`'s `traceEvent`/`ensureDirs`/`openRun` writes now swallow I/O errors and return a sentinel, matching the screenshots + `snapshotGraph` that already did. Rationale: an opt-in DIAGNOSTIC must never perturb the crawl — an unguarded `appendFileSync` throw would, in whats-new, propagate past the final `saveGraph` (a captured causal edge lost) and, in recon-run, hit `reconLoop`'s catch → `markUnreachable` on a successfully-acted control (the denominator the invariant protects collapses). The comment "capture rides ALONG without perturbing" now holds for the write path, not just the screenshots.
- CHOSE: emit an `act` event with `{error}` on the recon-run throw path, then re-throw (backend + bughunter CONSIDER). The node-loop previously logged nothing for a FAILED act (`reconLoop` swallows the throw into `markUnreachable`), so the admin's Walk timeline — built precisely to "spot why it didn't reach X" — omitted the interesting cases. Re-throwing keeps coverage identical; the agent path already logs these via `observe`, this is the node-loop backstop. whats-new's baseline `route` event also snapshots the graph now, so the scrubber never 404s on a route seq.
- CHOSE: an ACCESS-TOKEN gate on every `/api` route (security M1, medium). The three-layer fence (127.0.0.1 bind + Host allowlist + no-CORS) closes the PRIMARY threat (a foreign web page), but the Host allowlist does not stop a local non-browser client — another user/process on a shared machine could `curl` the trail, including BEFORE/AFTER screenshots of a logged-in target. A per-startup random token (`crypto.randomBytes(16)`, length-guarded `timingSafeEqual`) rides in the URL the operator opens (`?t=<token>`, the Jupyter pattern); `admin.html` reads it from its own location and threads it through every fetch + `<img>`; the page itself (no data) stays token-free so the operator always reaches the viewer. The URL-param placement is acceptable here — an ephemeral LOCAL session token, not personal data (the same tradeoff Jupyter makes).
- CHOSE: security headers + realpath containment + non-reflecting 500 (security L1/L2/L3). `nosniff` + `X-Frame-Options: DENY` on every response and a strict CSP on the HTML page (frame-deny is the clickjacking backstop, CSP the XSS backstop for the inline-asset viewer); the 500 handler emits a generic message (no `err.message` path-leak) and guards `res.headersSent`; `safeArtifact` adds a `realpath` check so a symlink planted in the run dir cannot escape it (the lexical `startsWith` check follows symlinks). All local-only / defense-in-depth — the primary-threat posture was already correct.
- REJECTED: suffixing key-frame filenames with the event seq to survive a re-act of the same template under one runId (backend CONSIDER). `makeCapture` names frames `t<id>-before/after.png` on the "acted at most once per run" assumption, which the `/recon` flow honors; threading the seq would break the `capture-causal` test's `shots/t<id>-before.png` assertion and add churn for a scenario the orchestrated run never hits. Documented the assumption instead.
- ACCEPTED / DEFERRED: the trail's `fill` field is written verbatim (bughunter CONSIDER, latent secret). Today `fill` is HUNT-tagged synthetic data and auth/login-fill is unbuilt, so nothing sensitive lands; it is contained (gitignored `state/`, 127.0.0.1 + token). Marked with a REDACTION-POINT comment to mask a typed password/PII before it reaches the trail once auth-fill exists. Also deferred: a monotonic-counter seq if concurrent same-runId writers ever become real (today the CLIs are strictly sequential, so the read-count-then-append is race-free).

### 2026-07-15 — Information-collection architecture: response capture + stay-on-page (CTO review, Fable)
- CONTEXT: two collection gaps block "map the whole app". GAP 1 — we capture only request-side method+URL (`probe.mjs:53-64`); no status/body/headers/type/timing, and the causal binding is IN-PAGE (fire-ring) while response data is OUT-OF-PAGE (CDP). GAP 2 — `persistentStep` re-navigates per act (`recon-run.mjs:81`), so any control revealed only by an in-page reveal path (modal/dropdown) stays `unreachable-coldstart`. The final LLM-first graph SCHEMA is the explicit NEXT task and OUT OF SCOPE here (one task at a time). The invariant: request→control attribution = in-page token AND CDP initiator, never a wall-clock window — any option weakening it is disqualified.
- CHOSE (GAP 1, Option A1): promote the CDP session `initiator.mjs` ALREADY holds (`:57`, Network.enable + requestWillBeSent) into a per-requestId ledger by adding `Network.responseReceived` + `loadingFinished` (status, mimeType, headers, resourceType, timing). Snapshot a `cdpCursor = ledger.length` at `beginCause` beside `seq0`; in `endCause` join each KEPT in-page fire to the first unmatched ledger entry with the same `(method, pathname)` and `index >= cdpCursor`, then attach the response data. The causal DECISION (token + pathname verdict) stays byte-unchanged — response data is purely additive, so the invariant is preserved structurally. Status is small enough to sit on the request node/edge; BODIES are per-instance (a POST returns a different body each call) and fight the `urlPattern` masking, so they live in a per-act sample record, NOT on the masked node.
- REJECTED (GAP 1, A2): inject a `x-bughunt-cause` correlation header from the monkeypatch → exact per-requestId join. Disqualified: it MUTATES outgoing traffic (violates `probe.mjs:43` "purely observational"), forces a CORS preflight the app never intended, can be WAF-rejected, and leaks our instrumentation to the target. Only revisit under an ADR if in-window duplicate `(method,pathname)` rates make A1's order-join ambiguous.
- REJECTED (GAP 1, A3): Playwright `page.on('request'/'response')` pairing. A THIRD parallel view of every request carrying neither the CDP requestId nor the initiator stack — redundant with the session we already own, and the join back to the cause is still order-matching. Reject in favor of reusing the one CDP session.
- CHOSE (GAP 2, B1 + B5): add a State layer (already in the vocabulary, `ARCHITECTURE.md:11`) addressed by the ORDERED REVEAL PATH — `(route, statePath=[tid_A,tid_B], templateSelector, instanceKey)` — kept as a SEPARATE addressing dimension, NOT folded into `instanceKey`, so the append-only ledger identity never churns (mirrors the locator decision, this file 2026-07-14). Re-drive is a HYBRID (B5): re-navigate to `node.route` for a clean O(1) reset point, then bounded in-app DFS within it, calling `resetTrackerVerdicts` between in-app acts (primitive exists, `causal.mjs:46`). Causal attribution ALREADY survives depth — cause is captured at fire time (`probe.mjs:59`) so an act-N request completing in act N+1's window still carries `cause=tid_N` and is dropped by the `seq0` filter; the one depth risk is the fire-ring cap 500 shifting out fires WITHIN one chatty act's window (drain/compact per act, not an attribution change).
- REJECTED (GAP 2, B2): DOM-fingerprint state key. Fingerprint churn (a timestamp/live counter shifts the hash) = identity instability, the brittle-threshold failure class; and it is not replayable. Keep the fingerprint only as an OPTIONAL LLM dedup HINT ("state X ≈ state Y"), never identity.
- REJECTED (GAP 2, B3 pure replay / B4 pure live-DFS as the whole answer): B3 (replay reveal path from fresh nav every act) is clean but O(depth) and fragile on non-idempotent reveal steps; B4 (one live page, Escape/close backtrack) is O(1) but Escape is unreliable for modals and a dirty branch corrupts the next. B5 hybrid takes B3's clean reset boundary (per route) and bounds B4's DFS blast radius within a route.
- SHARED PRIMITIVE: a per-act TRANSITION RECORD backed by the per-requestId CDP ledger + the `cdpCursor`-per-act bound. GAP 1 fills `causedRequests[]` with response-joined data; GAP 2 fills `revealPath`/`preconditionState`/`resultingState`. Build the ledger + the order-cursor-at-beginCause discipline ONCE in `initiator.mjs`/`causal.mjs`; both gaps consume it.
- BUILD SEQUENCE (one task at a time): (1) GAP 1 METADATA — status+type+timing via the ledger+cdpCursor join; lowest invariant risk (pure addition), no privacy exposure, and it hardens the order-within-window join GAP 2 reuses. Gate: extend `capture-causal.test.mjs` to prove the in-window poll is STILL rejected with capture ON. (2) GAP 1 BODIES — behind an explicit opt-in + content-type allowlist (`application/json`, `text/*`) + size cap + redaction of `Authorization`/`Cookie`/`Set-Cookie` (extends the REDACTION-POINT now that authed recon carries session material), stored per-act not on the node. (3) GAP 2 STATE — B1+B5, last: it touches the protected identity model and forces the State-node schema change, so behind its own ADR.
- DEFERRED / OPERATOR DECISIONS: body-capture default (metadata-only vs opt-in bodies + allowlist/caps); where bodies physically land during collection (per-act sample in `state/runs/<runId>/` vs a new sample store — pre-decides part of the deferred schema); GAP 2 state-dedup policy (accept reveal-path over-counting vs invest in LLM state-equivalence); replay-idempotency policy for non-idempotent reveal steps + DFS depth cap per route. Also worth building FIRST for GAP 2: a read-only `identity-diff` probe so "adding State churned zero ids" is a one-command check, not an assertion (the same probe deferred in the locator decision).

### 2026-07-15 — Request/response BODY capture: opt-in + redaction + trail-scoped (GAP 1 step 2, operator-chosen)
- CONTEXT: step 1 (response METADATA — status/mime/type/timing) shipped; step 2 adds request/response BODIES so Phase-2 sees payload/response shape. Bodies can carry secrets/PII and the crawl now runs AUTHENTICATED (responses carry session material), so this couples to the "secrets outside git" invariant. Operator chose "opt-in + redaction" over "raw, no redaction" and over "skip bodies, go to GAP 2".
- CHOSE: capture is OFF by default, behind a DOUBLE GATE — `BUGHUNTER_CAPTURE_BODIES=1` AND an active run (`BUGHUNTER_RUN_ID` set). The run-gate means bodies are only fetched when there is a trail to hold them AND provably NEVER during the `login.mjs` pre-step (it opens no run) — the credential exchange is never captured, mirroring why login writes no debug trail at all.
- CHOSE: REDACT + CAP AT CAPTURE TIME (in the ledger handler, before anything touches disk or is retained), so raw secrets never persist even in memory. A new pure `lib/browser/redact.mjs` owns: a content-type ALLOWLIST (`application/json`, `application/*+json`, `text/*` EXCEPT `text/html` — whole pages are huge + low endpoint-value), a per-body SIZE CAP (truncate with a marker), structural JSON redaction (walk keys, replace secret-shaped values: password/token/secret/api_key/authorization/cookie/session/credential/private_key/client_secret/cvv/ssn/… → `[REDACTED]`), form-urlencoded key redaction, and high-confidence text patterns (JWT, `Bearer …`, secret `key=value`). Binary/base64 responses are skipped.
- CHOSE: bodies ride the SAME channel as metadata (ledger entry → `takeResponse` → attached to the KEPT fire in `endCause`) so they inherit causal attribution for free — but they flow to the TRAIL, NOT the stdout envelope: `actStep` strips body fields out of the metadata `requests[]` (which reaches stdout + the graph node) and writes bodies via the capture/debug channel to files under `state/runs/<runId>/bodies/` (mirroring how screenshots are written to files with only refs in the envelope). The masked request node stays method+urlPattern only — bodies are per-instance and NEVER touch the graph. Response body is fetched via `Network.getResponseBody` AT `loadingFinished` (eviction-safe — not lazily in endCause after settle); request body is the `requestWillBeSent` postData.
- CHOSE: the causal invariant is re-proven with bodies ON — the kept-set stays decided solely by the token+initiator+static filters; bodies are attached to already-kept fires only, `getResponseBody` is a read with no page effect. A live guard extends the response-meta test to keep the in-window poll rejected with body-capture ON.
- REJECTED: raw bodies, no redaction, all inline in the trail (the other offered option). Secrets/PII would land in the (gitignored) trail; acceptable only for a throwaway test stand, not the default. Kept as the effective behavior only if the operator explicitly disables redaction later (not built now).
- REJECTED: a dedicated per-request body STORE separate from the trail. That pre-decides part of the deferred LLM-first graph schema (where request↔response samples durably live); parking bodies in the gitignored run trail commits nothing and is discarded with the run. The durable home is decided in the schema task.
- REJECTED: fetching the response body lazily in `endCause` (after `waitSettled`, up to 3s later). The CDP response buffer can be evicted by then — the fetch must be kicked off at `loadingFinished`. endCause awaits the (already-likely-resolved) body promise for kept fires only.
- DEFERRED: exposing bodies in the admin viewer (a later polish); an operator "disable redaction" escape hatch; per-target content-type/size-cap overrides (no target-config layer exists yet).

### 2026-07-15 — GAP 2 stay-on-page reach: reveal-path annotation + replay-from-reset (ADR, CTO/Fable)
- CONTEXT: the last + highest-risk collection piece — reach and collect controls revealed ONLY by an in-page action (modals/dropdowns/expanders/wizards/tabs). Today `persistentStep` re-navigates per act (recon-run.mjs:64,81), so a click-opened modal (same URL) is CLOSED by the re-nav and its inner controls throw NO_INSTANCE → `unreachable-coldstart`. Grounded in Crawljax's state-flow-graph model (a UI state addressed by the event path from init, revisited by RELOAD + event-path REPLAY, assuming deterministic reproducibility — which is exactly our non-idempotency risk). GAP 2 touches the PROTECTED identity model, so it lives behind THIS ADR + an identity-diff gate built FIRST.
- CHOSE (1A — annotation, not a node): reveal path as an ADDITIVE `node.reveal = { route, statePath:[{templateId,instanceKey},…] }` field, stamped by `mergeSnapshot` on NEWLY-created nodes only when given an optional `revealPath`. It is a separate addressing DIMENSION, never an identity key (mirrors the 2026-07-14 locator decision: store as attribute, never re-key `ids.mjs`). Template-level, first-reveal-path-wins (existing node untouched on later merge, graph-store.mjs:46). ZERO id/edge churn; and because there is NO new State IDENTITY, the honest denominator (frontier.mjs:41-59, template-count) cannot multiply per-state — a modal's controls are ordinary once-counted `graph.elements`.
- CHOSE (2A → 2C): re-drive by REPLAY-FROM-RESET per act (Crawljax reload+replay). `persistentStep` gains a prologue: re-nav to `node.reveal.route` → `dismissOverlays` → replay `statePath` (resolve each `instanceSelector`, click UNDER `__idle__` so its fires fail the token filter, `waitSettled`, `resetTrackerVerdicts` between steps) → `resetTrackerVerdicts` → the measured `actStep`. Accept O(depth) for the first slice; generalize to amortized in-app DFS within a route (2C) once depth-1 is proven.
- CHOSE (frontier minimal): `nextBatch` adds `reveal: node.reveal || null` per item — the ONLY frontier change; `frontierStats` UNCHANGED (denominator stays template-count).
- CHOSE (causal survival, confirmed by construction): reveal clicks run under `__idle__` (fires carry `cause='__idle__'`, excluded by `selectKept`); `resetTrackerVerdicts` after EACH reveal step + before the measured `beginCause` (the same phantom-edge discipline persistentStep already runs); the re-nav-to-route boundary DRAINS the fire-ring (a fresh document re-runs `stateProbeInitScript`), so ring/ledger/verdict pressure is bounded by ONE path's length, not the whole crawl. Only pre-existing risk: a single reveal step or the measured act firing >500 requests in its own window (fire-ring cap).
- CHOSE (safety): `dangerFloor` re-checked on EVERY reveal step — refuse the WHOLE path if any step floors → target honestly `unreachable`, never fired (a deeper DFS multiplies danger exposure, so gating only the terminal act is insufficient). Reveal paths are STAY-ON-PAGE ONLY: a step is recorded only if its effect was a reveal (newElements, same route) — NEVER a navigate act; a route-changing step reclassifies the path as nav. Keeps `routeRefused` as the nav gate and the click-floor as the in-app gate, cleanly separated.
- CHOSE (replayability policy): a reveal step is stored as replayable ONLY if it fired no non-GET request and did not change route; a mutating/non-idempotent reveal (wizard advance, a reveal that submits) makes its downstream controls honestly `unreachable`, never re-fired (operator may later allow mutating reveals behind the HUNT-tag mutation contract + a cap).
- CHOSE (probe FIRST): read-only `lib/graph/identity-diff.mjs` before any identity-adjacent code — `diffIdentity(before,after)` → `churnedTemplates`/`churnedInstances`/`droppedEdges` (all MUST be []) + added counts; CLI `--before=<dir> --after=<dir>`, non-zero exit on churn. The one-command gate that turns "zero churn" from a claim into a fact (retires the probe deferred in the 2026-07-14 locator decision).
- REJECTED (1B): a first-class `graph.states{}` node type NOW. That IS the deferred full LLM-first-schema task; it introduces a SECOND identity (stateKey) needing dedup → re-opens state-explosion + denominator-collapse; larger blast radius on freshly-proven code. `node.reveal` is the collection annotation the schema task will later PROMOTE to a node.
- REJECTED (1C): per-INSTANCE reveal paths. Instances multiply (50-row table × modal); the driver only needs ONE representative path; premature precision.
- REJECTED (2B): live-page DFS with Escape/close backtrack. Escape is unreliable for choice-forcing modals; a dirty branch corrupts siblings; violates act independence (already rejected in the 2026-07-14 persistent-browser decision).
- BUILD SEQUENCE / first slice: (1) identity-diff probe + unit test; (2) `tests/fixtures/modal-app` (a page button → same-URL modal with 2-3 controls incl. one request + a shared-path background poll to guard causal survival); (3) collection — thread `revealPath` actStep→mergeSnapshot, stamp `node.reveal` on new nodes; identity-diff must show ZERO churn (the gate); (4) frontier emits `reveal`; (5) `persistentStep` replay prologue + per-step floor + verdict-reset; (6) live `stay-on-page.test.mjs` (inner control genuinely `explored` not `unreachable-coldstart`, its request causally attributed, the poll still rejected, identity-diff zero churn; revert: drop replay → inner control unreachable); (7) only then generalize (depth-N, per-row, amortized DFS, depth cap).
- OPERATOR OPEN QUESTIONS (recommended defaults): state-dedup = first-path-wins (no State identity, no dedup); replay-idempotency = GET-only replayable; DFS depth cap = small default e.g. 3 past depth-1; reveal-path staleness = an unresolved `instanceSelector` → `unreachable` (honest), never guess; per-instance vs per-template = template-level with `instances[0]` for the first slice.
- SCHEMA COUPLING (flagged, NOT designed here): `node.reveal` is the single point GAP 2 touches graph shape; the durable State model (first-class nodes, dedup, per-state behavior, a `behind-state` coverage count in report) is the deferred LLM-first-schema task.
- SECURITY (3-reviewer pass, H1 fixed): replay CLICKS now mirror `actStep`'s PRE-CLICK guards on the LIVE resolved element — an off-origin href (`isOffOriginHttp`) → `REVEAL_OFFORIGIN`, a danger-route href (`routeRefused(routeKey(href))`) → `REVEAL_DANGER`, both refused BEFORE the click, plus a `REVEAL_MAX_DEPTH=10` cap. Rationale: a raw replay click that first navigates off-origin / to a private host / to `/logout` would end an authed session or leave scope BEFORE the post-hoc route-change abort — reopening, for the replay path, the self-logout hole `routeRefused` closes for NAV. Every safety branch (STALE/DANGER/OFFORIGIN/NAVIGATED/TOO_DEEP) is revert-proven by a live test that injects an off-origin and a `/logout` hop and asserts the click is never followed.
- ACCEPTED/KNOWN (residual vs an ADAPTIVE/hostile server): the GET-only replayability gate is enforced at ANNOTATION time (the opener's causally-attributed requests were all GET when recorded). A server that serves a GET opener at record but a POST at the SAME selector at replay fires that mutation under `__idle__` (uninstrumented) — caught only if the swapped element also becomes a logout/off-origin/danger-route link (the live-href PRE-CLICK guards). A pure same-path GET→POST body swap, and a same-path query-only GET mutation (`/x?do=delete`, which `routeKey` strips so the route-change abort and the href `routeRefused` miss it), are residuals inherent to replaying clicks without semantic understanding against a hostile adaptive target. Acceptable for the node-loop first slice; revisit with the depth-N/DFS ADR (a stricter "any non-GET seen in the act window" gate + per-click method observation).

### 2026-07-15 — Whole-site reach on a constant-URL SPA: agent-path stay-on-page + POST-read replayability + fixed viewport (ADR, CTO/Fable design, Opus build)
- CONTEXT: a full `/recon` run on dev.rawcaster.com drained the frontier but mapped ONE route (14/47 explored, 33 unreachable, routes=1). Root cause, verified empirically: the SPA serves its ENTIRE authed app under a single URL `/dashboard` — the primary nav ("For You"/"Following"/"My Community") are `<a href="/dashboard">` that swap content CLIENT-SIDE without changing the URL (probe: urlChanged=false), section tabs are `<div>`s in an in-app panel, pushState is present but unused, and "read" queries fire over POST (`POST /listnuggets`). Recon's site identity is `routeKey(page.url())` + re-nav-to-`node.route` per act, so a constant URL = one route and every in-app-state control is `unreachable-coldstart`. TWO independent blockers, not one: (a) the GET-only stamp gate (step.mjs) refuses to record a reveal path for a POST-firing opener — and this app's nav reads are POST; (b) the AGENT path (`whats-new`/`observe`, which `/recon` drives) never threads the reveal path at all (only the node-loop `recon-run` did). Both must land for `/recon` to gain anything.
- CHOSE (Q1 — keep the annotation, do NOT force a State node): extend `node.reveal` (the 1A annotation from the prior GAP-2 ADR), NOT a first-class `graph.states{}` node. rawcaster needs REACH, which the annotation already gives at arbitrary depth (`revealPathFor` concats prefix+target; `REVEAL_MAX_DEPTH=10`); it does not need a durable State identity. Zero id churn (proven by `identity-diff.mjs`), denominator stays template-count. This does NOT force the deferred LLM-first graph-schema task early.
- CHOSE (Q2 — agent-judged replayability, act-time flag over a preserved GET-only default): the stamp condition becomes `revealPath && landedRoute===route && (allGet || openerReplayable===true)`. `actStep` gains an `openerReplayable` opt; `whats-new` gains `--opener-replayable=<bool>`; the recon Sonnet worker classifies the opener's caused POST as READ (list/search/detail) vs MUTATION (create/update/delete/save/like/follow/vote) and passes the flag ONLY for a read. The NODE loop (`recon-run`) never sets it → its deterministic GET-only ceiling is unchanged (guard (e) intact). "Read vs write on a POST" is a SEMANTIC call and per project doctrine ("LLM belongs in the walk") belongs to the agent with the danger-floor as the hard floor (destructive/auth/payment still refused by name regardless of the flag).
- CHOSE (Q3 — thread the agent path by REUSING `applyReveal`, not forking): `whats-new`'s act branch imports the SAME `applyReveal` prologue the node loop uses (reset point = the baseline `gotoGated(opts.url)`; valid whenever `node.reveal.route===routeKey(url)`, always so on a constant-URL SPA) + `resetTrackerVerdicts`, ~6 lines mirroring `persistentStep`. Single-source-of-truth for replay preserved; causal discipline (idle clicks, per-hop verdict reset, live pre-click floors) inherited by construction.
- CHOSE (Q4 — replay-from-reset unchanged + a cycle guard): depth-N is already wired; add a `seenTemplates` cycle guard in `replayRevealPath` (a statePath revisiting a templateId → `REVEAL_CYCLE`, refused before walking) alongside `REVEAL_MAX_DEPTH`. Amortized in-app DFS (reuse an opened state across sibling controls) DEFERRED to its own ADR, gated on a live measurement that re-nav cost dominates.
- CHOSE (Q5 — fixed viewport at the single injection point): `contextOptions()` returns a frozen `viewport:{width:1440,height:900}` on EVERY context (authed/anon/login), replacing Playwright's implicit 1280x720 default, so the NOT_VISIBLE denominator is reproducible. Set once, then stable.
- REJECTED (Q1-B): a first-class State node now — a second identity (stateKey) needing dedup re-opens state-explosion/denominator-collapse; it IS the deferred schema task; largest blast radius. Inflection: promote `node.reveal`→node only when Phase-2 needs a behind-state coverage COUNT or state dedup, not for reach.
- REJECTED (Q2-B): a stored `node.replayableOverride` consumed at replay time — requires stamping a mutating-opener child unconditionally (breaking guard (e)'s `save2.reveal===undefined` contract) + a replay-time gate + a "mutating" derivation from edges; larger surface for no near-term non-agent consumer.
- REJECTED (Q2-C/D): a per-target read-POST path allowlist (no target-config layer exists; brittle manual heuristic) and a response-shape/idempotency heuristic (brittle threshold; re-executing to test idempotency doubles a write) — both the exact brittle-heuristic failure class the prior ADRs named.
- REJECTED (Q3-B for now): unify the agent-path reset to `node.route` (full multi-route+reveal parity) — changes `whats-new --url` semantics for the act branch, risks the sequence guard, no rawcaster gain (rawcaster is single-route). Inflection: the first target that combines multi-route nav AND in-app reveals.
- ACCEPTED/KNOWN (trusted-judgment residual): the agent's read-vs-write call is TRUSTED — a benignly-named non-floored write (Like/Follow) mis-judged as a read would get its children stamped → replayed → N benign server mutations. Bounded by the danger-floor (destructive/auth/payment refused by name) but not eliminated; a per-opener replay cap is a candidate follow-up. This is why the flag is agent-only and never the node-loop default.
- KNOWN (hidden-panel tabs residual): rawcaster's Groups/Events/Community tabs are `<div>`s hidden in a panel even at 1920px — reaching them is a depth-2 chain (open the panel → click the tab → content) that depends on the panel-opener being discovered + acted first; whether that chain closes needs a live re-run, and a correct-but-unsatisfying `unreachable` residual may remain.
- BUILD SEQUENCE (small, independently revert-proven): (1) fixed viewport (session.mjs) + unit guard; (2) agent-path reveal threading (whats-new imports applyReveal); (3) POST-read replayability widen (step.mjs `openerReplayable` + whats-new `--opener-replayable` + recon.md read-vs-mutation judgment + a POST-read `#open-read`→`#expand` fixture opener); (4) cycle guard (reveal-replay.mjs). Guards: `tests/live/agent-stay-on-page.test.mjs` (agent-path replay + POST-read widen, both revert-proven), the `REVEAL_CYCLE` case in `stay-on-page.test.mjs`, the viewport case in `session-context.test.mjs`. Full suite 136→139 green.
- LOGIN FIX (incidental, required to run the authed crawl at all): `login.mjs` polled the success signal ONCE right after `waitSettled`; a SPA that resolves auth via an async XHR and THEN client-redirects lands AFTER the network idles, so a slow redirect read as a false `LOGIN_FAILED`. Replaced the single check with a bounded poll (≤8s, early-exit on first success); verification semantics unchanged (still gates persistence, never a fixed sleep).

### 2026-07-15 — INC.1: framework-noise ids stop anchoring identity (Fable research + CTO-directed)
Context: State-model inc.2 lifted rawcaster only +2 controls; a live smoke left antd tabs `NOT_VISIBLE`/`NO_INSTANCE`. Fable (research: Crawljax/Judge/RIA/WebRLED; CTO: validated against the code) ruled the residual is IDENTITY, not a missing mechanism — the compositional depth-N reveal the "real fix" needed is ALREADY built (`reveal-replay.mjs revealPathFor` + `step.mjs` stamp + `state-model.test.mjs:71-77`). What breaks it on rawcaster: framework-generated ids (`rc-tabs-0-tab-N`, `ant-*`, hashed) anchor each control's selector, so one logical tab fragments into N per-reload-shifting templates and the replay chain goes stale.
- CHOSE: reject framework-noise ids as a SELECTOR ANCHOR (`isGeneratedId`) AND as a sole `#id` locator (`stableIdForLocator`) — prefix `rc-/ant-/radix-/headlessui-/mui-` + `-`/`_`, plus a hashed-hex-with-digit run. antd tabs de-fragment to ONE structural template with N addressable instances and a STABLE role+name locator; the reset-and-replay chain stops going stale. One `dom-snapshot.mjs` change fixes the tab residual AND half of the depth-2 breakage.
- CHOSE: `schemaVersion:2` on the graph + `loadGraph` reset-on-mismatch (an absent field = a legacy scheme). A pre-INC.1 graph (framework-anchored ids) never co-mingles with re-keyed ids. state/ is ephemeral, so a reset is safe, not data loss.
- CHOSE: run `identity-diff` here as a MEASUREMENT, not a green gate — the churn is INTENDED (the one class invariant 4 protects), confined to rc/ant-anchored templates. On fixtures it stays zero (no fixture uses framework ids), so the suite is green; the real re-keying is measured on the live antd target.
- REJECTED: adopt Judge-style sibling-subtree merging as a separate state/identity pass — our structural-index normalization (`buildPath(el,false)` drops `:nth-child`+data-ids) ALREADY collapses a 50-row feed to one template; the ONLY fragmentation we actually hit is the framework-id leak into the anchor, which this fixes. A second merge pass would be redundant surface with new churn.
- REJECTED: reject framework CLASSES too (`ant-tabs-tab`) — those are the STABLE structural anchor we WANT in the path (`isStableClass` keeps them); only the per-instance IDS shift across reloads. Rejecting the class would WEAKEN the template path.
- REJECTED: build the State node (INC.3 `graph.states{}` + state-dimension reporting) as the "real fix" the user asked for — it is REPORTING/DEDUP, NOT coverage; reach already exists at arbitrary depth via the shipped reveal annotation. Deferred until Phase-2 needs a behind-state count. (Overturns the "depth-N/State-model as the real fix" framing — the depth-N mechanism was already there; identity stability was the gap.)
- REJECTED: amortized in-app DFS (INC.4) now — an un-reset in-app session accumulates sticky CDP initiator verdicts + dirty-branch DOM across siblings, reintroducing the phantom-edge class invariant 1 exists to prevent. Gate (a live measurement that re-nav cost dominates the step budget) is not met; the modest lift argues it does not. Reject; revisit only behind that measurement + its own ADR.
- REJECTED: full RL crawler (WebRLED online reward) / LLM-as-driver / content-char state fingerprint — RL needs a training loop and inverts the "LLM judges, script controls" split (no API keys); LLM-driver is 3-4× slower; content fingerprint is the already-rejected StringCmp over-fragmentation (3,774 states for 6). The one cheap later steal noted but deferred: novelty-ranked frontier (reorders, does not lift coverage).
- DIAGNOSTIC GAP (flagged by CTO): whether INC.1 (stale-id), first-reveal lock-in, or an unflagged-POST opener dominates the depth-2 residual can only be settled by reading the per-candidate fail-reason histogram from a live authed `/recon` trail — the go/no-go for whether Option B (fill-if-absent reveal stamp) is even needed.
- LIVE PROOF (measured on authed dev.rawcaster.com, scheme-1 → scheme-2 before/after): 8 antd nav tabs (General/Fans/Groups/Connections/Events/Nuggets/Poll Results/Community) that were 8 separate `#rc-tabs-0-tab-0…7`-anchored 1-instance templates (fragmented, per-reload-shifting) collapse to structural templates with addressable instances; 10 framework-anchored templates → 0 framework-id leakage. INC.1's IDENTITY job is done on the real target. HONEST CAVEAT: de-fragmentation is a PREREQUISITE (the tabs are now stably addressable so replay won't go stale) — the actual EXPLORED reach of the panel-hidden tabs still needs the depth-2 panel-opener chain to FORM (a separate increment INC.1 unblocks), so measuring an explored-delta from INC.1 in isolation would understate it; a safe agent-path re-run (never the node loop, which lacks the danger judgment to avoid a live creation-submit) is the way to measure reach.

### 2026-07-15 — review-driven follow-ups (3-reviewer pass on the inc.2 + INC.1 block)
Verdicts: security CLEAN (0/0/0 + 1 informational); backend + invariants SHIP-AFTER-FIXES. The MUST FIX (page-derived `--instance` unquoted in the recon.md command templates — an agent-path shell-injection surface) was fixed in this change (quoted in all templates + added to the DATA list). Deferred to their own tracked changes (all bounded, none a correctness/coverage lie — the invariant authority downgraded the frontier items to CONSIDER):
- OPENER-DRAIN LOUD-ERROR (backend SHOULD / invariant CONSIDER): if the recon worker omits `--instance` on an opener sibling, `whats-new` silently acts `instances[0]` and `observe` drains template-level, so `nextBatch` re-emits the sibling until the 20-iteration cap — wasteful, not a lie (the template is honestly explored via instance[0]). Fix shape: `observe`/`whats-new` throw `USAGE` when `node.opener && instances.length > 1 && --instance` is absent, turning a silent budget-burn into a self-correcting error. Needs a FAIL-ON-REVERT test.
- FRONTIERSTATS INSTANCE BLINDNESS (both, CONSIDER): `frontierStats` is template-count only, so `remaining` can read 0 / `explored` can read 100% while `nextBatch` still yields opener siblings, and opener instances beyond `OPENER_INSTANCE_CAP=8` are counted nowhere (a hidden remainder — cuts against "coverage never hidden"). Termination is unaffected (it keys off `nextBatch()===[]`), but the admin/report number misleads mid-run. Fix shape: an instance-level tally (walked vs discovered-instances + a `cappedRemainder`) OR correct the `frontier.mjs:12-13` comment that over-promises a report surface.
- >200-LINE FILES (invariant SHOULD): `graph-store.mjs` (213) / `dom-snapshot.mjs` (212) / `step.mjs` (210) sit just over the guideline after this accretion. Fix shape: extract the instance-marking helpers (`instanceOf`/`markInstance*`/`markOpener`) or the schema-gate block from `graph-store.mjs` into a small sibling module. NOT done pre-commit to avoid stripping identity rationale comments / adding churn to freshly-proven code.
- LEDGER SCHEMA COUPLING (backend CONSIDER): `loadGraph` resets on a schema mismatch but the co-located `element-ids.json` ledger has no gate — benign for INC.1 (changed selectors mint new keys; old framework-anchored keys are orphaned, never re-queried), but a future selector-REUSE change could co-mingle a stale id. Fix shape: clear the sibling ledger on a graph reset, or version the ledger, or document the coupling.
- HASHED-HEX FALSE-POSITIVE FIXTURE (backend CONSIDER): `isFrameworkNoiseId`'s `/[0-9a-f]{6,}/ && /\d/` rule newly demotes ids like `widget-a1b2c3` from anchoring — deterministic fallback so no churn/fragmentation, but worth a fixture if a real target shows false positives; tighten to a separator-anchored token then.
- AGENT-PATH IN-WINDOW POLL ASSERT (invariant CONSIDER): `agent-stay-on-page.test.mjs` proves replay-leakage rejection but not the in-window background-poll rejection at depth on the AGENT path (that rigorous proof lives only in the node-loop `stay-on-page.test.mjs`); the mechanism is shared, so low risk. Fix shape: add an explicit in-window poll-rejection assert to close "prove the HARD case" for the agent path too.
